# I-0 independent contract review (Terra)

## Scope and evidence

READ: `src/gigai/scout_find_jobs_contracts.py`; `tests/behaviors/scout_find_jobs/test_contracts.py`; all eight I-0 fixture JSON files; Amendment 02 Rev 3; active find-jobs roadmap; and the current `run-details.schema.json` mapping target.  I did not read the coordinator note, as instructed.  The worktree was already materially dirty; no source, fixture, test, or unrelated file was edited by this review.

EXECUTED: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_contracts.py -q` (after allowing access to the existing uv cache): **22 passed in 0.03s**.  This is focused source-test evidence only; it is not installed, UI, provider, or live-run evidence.

## Findings

| id | severity | file:line | finding | fix |
| --- | --- | --- | --- | --- |
| T1 | blocker | `src/gigai/scout_find_jobs_contracts.py:771-802` | `NodeReceipt` cannot be mapped losslessly onto current `run-details` `goal_details`: it has `node_slug`/`operation_key`, but no canonical `goal_id`, `goal_version`, `executor`, `outcome`, or `evidence`/result reference.  It also represents `interrupted`, but omits legitimate stored goal states `ready`, `waiting_for_gate`, and `verifying` (`run-details.schema.json:121-152`). I-2/C-1 would have to invent a lossy transform despite D4 requiring an additive receipt/details seam. | Freeze either an explicit `NodeReceipt <-> goal_details` mapping with all required fields and state translation, or add the missing identity/evidence/status fields; fixture and negative tests must prove it. |
| T2 | blocker | `src/gigai/scout_find_jobs_contracts.py:519-552,627-667,809-838` | The acquire-to-assess handoff is not sealed. `AcquireOutput.selected_postings` and `AssessInput.selected_postings` are independently shaped lists, with no `acquire_batch_ref`/input digest/output digest or selected-posting record ref linking them; `SelectionReason` is another unbound list, and the parser enforces neither one reason per posting nor a cap/eligibility relationship. `SelectedPosting.content_sha256` is nullable even though D6 requires selected identities and digests. B-1/B-2 must invent provenance and validation. | Make selected entries immutable refs/identities with required content digest; bind `AssessInput` to the acquisition output/batch digest and key reasons to each posting; validate uniqueness, exact correspondence, role match, new-or-edited eligibility, and cap. |
| T3 | major | `src/gigai/scout_find_jobs_contracts.py:896-918` | `PresentPayload` is C-4's frozen input but has no `pinned_resume`; it therefore cannot show the D7 `record_id`/`revision_id`/digest identity promised by roadmap C-4. `ConfigResponse.resume_preview` is not a substitute once the run has pinned a particular revision. | Add the run-pinned `PinnedResume` (or an explicit unavailable state) to the present/run-results projection and fixture a non-empty payload. |
| T4 | major | `src/gigai/scout_find_jobs_contracts.py:686-715,842-872` | The UI needs every unselected candidate to say why it was not assessed (D6/model-off visibility), but `NotAssessedRow` accepts only a bare `SelectedPosting`, has no link to a row outcome or selection input, and there are no exhaustiveness/uniqueness checks. The fixture omits reasons for unchanged/duplicate/failed rows and cannot prove the declared model-denied/unavailable variants. | Define the source-row identity and require a complete, non-overlapping accounting of assessed vs not-assessed eligible rows; model unavailable/denied must be concrete fixtures, with expected reason coverage. |
| T5 | blocker | `src/gigai/scout_find_jobs_contracts.py:1056-1059` | `diff_url_sets` accepts only URL iterables and hardcodes `edited=()`, so it cannot implement the roadmap's content-hash edit detection. The acquire fixture even reports the same URL as both `added` and `edited`, which is semantically impossible for a set diff and is accepted. | Change the A-4 signature to compare URL-to-content-digest observations (or split URL-set and content-diff DTOs), make categories mutually exclusive, and add edited/non-edited fixtures and tests. |
| T6 | major | `src/gigai/scout_find_jobs_contracts.py:67-72`; `tests/behaviors/scout_find_jobs/fixtures/fixture-acquire-batch-v1.json:1` | The roadmap specifies an acquire-batch fixture with `new`, `pending`, `failed`, dedup, and content-hash outcomes; `RowOutcome` has no `pending`, and the fixture has no pending row. A-6/progress rendering therefore has no frozen representation for the declared state. | Resolve whether `pending` is a row outcome or progress state, encode it in the appropriate DTO, and include it in the canonical fixture/negative tests. |
| T7 | major | `src/gigai/scout_find_jobs_contracts.py:905,931-935` | Aggregate status fields in `PresentPayload`, `PresentOutput`, `RunResponse`, and `RunStatusResponse` are raw strings. `aggregate_status()` has the correct D4 precedence, but DTO parsing accepts arbitrary status values and does not require payload/output agreement; C-1/C-2 can emit a value that violates D4. | Introduce and validate a closed aggregate-status enum; test each precedence value and payload/output consistency. |
| T8 | major | `tests/behaviors/scout_find_jobs/test_contracts.py:39-61` | The normative freeze test does not reject drift per DTO: all eight fixtures only round-trip one happy sample, and unknown/missing/wrong-type/bad-enum mutations exist only for top-level `FindJobsConfig`. Nested DTOs, consent, receipts, client signatures, request/response types, and all API route request/response mappings remain unprotected. | Generate/table-drive negative coverage for every public DTO and nested object, every enum, all required/unknown fields and key invariants; assert route method/path/status **and** request/response types. |
| T9 | major | `tests/behaviors/scout_find_jobs/fixtures/fixture-assessment-v1.json:1`; `fixture-present-payload-v1.json:1` | The documented fixtures do not contain the fields/variants they promise: assessment output has no pinned resume, selection cap/reasons, or model-denied variant; present payload is entirely empty and has no rows/matrix/questions/not-assessed/node receipts/pinned resume/action response. C-1/C-4 cannot build or be tested against the claimed surface. | Supply representative success, model-off/denied, and failure fixtures, including non-empty present data and explicit API request/response samples. |
| T10 | minor | `src/gigai/scout_find_jobs_contracts.py:1010-1053`; `tests/behaviors/scout_find_jobs/test_contracts.py:86-97` | URL helpers are scarcely tested: `parse_board_url` has only happy hosts/one rejection; `normalize_url` and `diff_url_sets` have no direct test. `normalize_url` also discards ports while normalizing, creating identity collisions between the same host/path on distinct ports. | Add malformed/userinfo/port/query/fragment/tracking/dedup cases; preserve an explicit non-default port or reject it, rather than silently coalescing it. |

## Row readiness

I-1 can import the graph/context types, but must invent the run-details receipt mapping (T1). A-1/A-3/A-5 have usable basic row/client/watchlist shapes; A-6 lacks a valid edit-diff/progress-state contract (T5/T6). B-1/B-2 lack a sealed, provenance-bearing acquire-to-assess selection and complete not-assessed accounting (T2/T4). C-1 lacks the run-details mapping and constrained aggregate status (T1/T7); C-2/C-4 lack the run-pinned resume and representative present/API fixtures (T3/T9). 

The D1 domain enum, D2 targets/no-fallback vocabulary, D5 closed consent envelope, D9 config fields, D11 real effect names, route paths, and base watchlist shape match the amendment. The mismatches above are the remaining D4/D6/D7-facing freeze defects.

## Verdict

**rework** — do not dispatch wave 1b against this interface. The focused suite passing establishes only its limited current samples, not the normative drift barrier required by Amendment 02 Rev 3.
