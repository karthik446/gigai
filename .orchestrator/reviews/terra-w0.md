# Terra independent review — Find Jobs functional plan, Revision 2.1

**Scope:** read-only review of `v0.1.8-find-jobs-functional-roadmap.md` and
`P2-FREEZE-04-amendment-02-find-jobs-functional.md`, after the three named W0
reviews. **READ:** both planning documents, the prior W0 reviews, and source
anchors below. **EXECUTED:** `git status`, `rg`, `sed`, and `nl` only; no tests,
network/provider/model calls, or implementation.

| id | severity (blocker/major/minor) | doc:line | finding | evidence file:line | suggested fix |
| --- | --- | --- | --- | --- | --- |
| T1 | blocker | roadmap:44-48,62-65; amendment:98-119 | M1 cannot execute real nodes under the proposed DAG. I-3 only binds callables in `scout_materialization.py`; I-2 promises effects/receipts but neither row specifies a callable-dispatch contract or a replacement for the deterministic executor. The current scheduler rejects every executor except two local placeholder capabilities and exactly `['write_workpad']`, then `_execute_goal` writes `gigai-offline-ok:<goal>` instead of calling a bound node. The proposed named effects are also not valid effect values, and no row owns the common/graph/manifest schema and authority changes needed to seal them. | `src/gigai/run.py:3279-3356,3438-3460,3546-3561,2350-2400`; `src/gigai/schemas/common.schema.json:148-163`; `src/gigai/schemas/goal-graph.schema.json:122-148`; `src/gigai/graph_set.py:238-262` | Before dispatch, make I-2 explicitly own the execution-admission design and every required authority/schema file (or an additive versioned contract): sealed callable identity/lookup, allowed executor/effect mapping, graph-set ceiling, manifest effect propagation, invocation context/result/receipt publication, and fail-closed error/cancellation behavior. Make I-3 depend on I-2 as well as A-6/B-2/C-2; add end-to-end focused tests proving a real callable is reached and an undeclared effect is refused. |
| T2 | blocker | roadmap:62,92-102; amendment:73-95 | I-0 is not freezable as written. It names DTOs and broad narratives, but does not state field names, types, optionality/defaults, versioning/canonical serialization, invariant/error codes, reference shapes, or the exact A-client signatures/return row schemas and present API method/path/request/response/status JSON it claims to freeze. For example, D9 lists config concepts but does not define their types/enums or canonical digest input; `NodeContext`, `AcquireOutput`, and `PresentInput/Output` remain prose lists. Parallel packets would still invent incompatible wire shapes. | `docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md:60-77,90-102`; `docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md:73-95`; compare existing strict graph executor shape at `src/gigai/schemas/goal-graph.schema.json:122-148` | Amend I-0 with a normative field table/schema for each DTO and every fixture, plus named callable signatures and a route table (`method`, path, request/response JSON, response/error status). State canonicalization/digest and compatibility/version rules. Do not dispatch I-0 until its test can reject a field/type/route drift. |
| T3 | major | roadmap:25,47,75-77,126; amendment:109-113,173 | The local API process contract has no owner. C-2 owns a new module and C-3 owns Vite configuration, but neither document names the Python server entry point, bind/port/lifetime, CORS/proxy rule, route that calls `run.launch_run`, or how the UI reads/polls terminal node state. Existing `InterviewHTTPServer` is a short-lived token server on an ephemeral port, not the promised local application server. Thus `yarn dev` + local API in M1 is not reproducibly launchable. | `src/gigai/run.py:142-159`; `src/gigai/proposal_interview.py:627-656,783-804`; amendment:170-173 ownership table | Assign C-2 an explicit server entry/CLI file (and test) or name an existing host to extend. Freeze its fixed loopback bind/port, lifecycle command, Vite proxy/CORS policy, run-start endpoint, consent peer check, run-status/read endpoints, and terminal/error payloads in I-0. |
| T4 | major | roadmap:24-27,47,126 | The M1/live-consent sequence contradicts the functional goal. M1/Goal requires real public postings and the click path says acquisition declares `network_public_acquisition` under UI consent, but wave 4 says to prove M1 and *then* show consent/obtain direct consent for one real public traversal. A real M1 acquisition needs its scoped consent before allocation, not afterward. | roadmap:24-27,47,126; amendment:38,98-113; current redemption occurs before allocation at `src/gigai/run.py:280-303` | Define M1 as the already-consented real public traversal, with the UI displaying and redeeming scope before allocation. Keep any separately consented post-M1 live/provider receipt distinct and remove the misleading “then obtain” ordering. |
| T5 | major | amendment:166-183; roadmap:62,79-86,100-102 | Ownership is not actually consistent between the two contract authorities. The roadmap assigns `tests/behaviors/scout_find_jobs/fixtures/*.json` to I-0, but Amendment 02's Integration exact-owned-files row omits it while claiming the packet sets are disjoint. The amendment also calls the four broad packets disjoint while its Integration row combines I-0/I-1/I-2/I-3 files; that is not a dispatchable per-row ownership boundary. | roadmap:62,79-86,100-102; amendment:166-183 | Update Amendment 02 to list the fixtures directory under I-0 and reproduce the roadmap's four Integration subrows/dependencies (including the I-2 → I-3 edge). Make Amendment 02's per-row ownership table authoritative rather than only packet-level ownership. |
| T6 | major | roadmap:121-126 | Four full `make test` gates before M1 (waves 1a, 1b, 2, and 3) are unbounded ceremony against D10's “working ASAP” objective. The document already gives focused per-row commands and correctly treats installed proof as after-M1; repeating a full suite at every wave neither proves the real UI traversal nor preserves the stated narrow proof boundary. | roadmap:121-132,143; amendment:192-198 | Keep focused row and integrated-seam checks as M1 gates; move one full-suite run to the post-M1 integration/installed decision (or make it a non-blocking parallel confidence lane). Do not let four serial broad suites block M1. |
| T7 | minor | amendment:62-67 | One source citation is too short for its claim: `scout_report_readers.py:315-331` ends before the asserted `if "complete" in states` and precedence behavior at lines 332-337. The roadmap's broader `:295-342` anchor is accurate. | `src/gigai/scout_report_readers.py:315-337` | Change Amendment 02's citation to `src/gigai/scout_report_readers.py:315-337` (or `:295-342`). |

**Citation sample:** 16 source-citation occurrences checked across both documents; **15 accurate, 1 wrong** (T7).

**Verdict:** **fix first** — do not dispatch I-0 until its normative DTO/signature/route freeze and the real-node execution/authority path are specified.
