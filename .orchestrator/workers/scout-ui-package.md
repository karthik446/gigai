# scout-ui-package worker report

**State:** done, ready for review/commit by coordinator. Packet A of 4 for `gigai scout run` (U13).

## Files changed (owned files only)

- `src/gigai/scout/ui/.gitignore` (modified) — un-ignores `dist/`; `node_modules/` stays ignored.
- `src/gigai/scout/ui/dist/**` (new, generated by `yarn build`) — `index.html` + `assets/index-BSWoZtM8.css` + `assets/index-lxtSZ0ZQ.js`, 248K total.
- `.gitignore` (repo root, **not** in the original owned-files list — see "Unplanned companion edit" below).
- `pyproject.toml` ([tool.setuptools.package-data] only) — added `"ui/dist/*", "ui/dist/assets/*"` to the `gigai.scout` entry.
- `src/gigai/scout/find_jobs/present_api.py` (static serving only) — new static-file GET path; `/api/*` routing/behavior untouched.
- `.github/workflows/pull_request.yaml` (new job only) — `scout-ui-freshness` ("Scout UI bundle is up to date").
- `tests/behaviors/scout_find_jobs/test_present_api_static.py` (new) — 10 tests.
- `tools/verify_installed_scout_ui.py` (new) — wheel/sdist resource verifier, auto-discovered by `tools/run_ci_tests.py`'s `verify_installed_*.py` glob.
- `.orchestrator/workers/scout-ui-package.md` (this file, new).

Not touched: `src/gigai/cli.py`, `src/gigai/workpad.py`, `.orchestrator/decisions.log`, `.orchestrator/workers/specs/scout-run-command.brief.txt` — these show as modified in `git status` (other packets' pending changes in this shared worktree); I did not read into or modify those diffs beyond confirming they weren't mine.

## Unplanned companion edit: repo-root `.gitignore`

Not in my owned-files list, but un-ignoring `dist/` in `src/gigai/scout/ui/.gitignore` alone wasn't enough: the repo-root `.gitignore` has an **unanchored** `dist/` rule (packaging-output block, originally line 17) that matches at any depth and still shadowed `src/gigai/scout/ui/dist`. Without a negation there, "commit the built UI" is impossible. Added:

```
!src/gigai/scout/ui/dist/
!src/gigai/scout/ui/dist/**
```

right after the packaging-output block, with a comment explaining why. Verified with `git status --porcelain` that the dist files now show as untracked (`??`), not ignored. Flagging this since it's outside my stated file ownership — coordinator can revert/relocate if a different fix is preferred.

## What I did (READ vs EXECUTED)

1. **READ**: `.orchestrator/workers/specs/scout-run-command.brief.txt`, `v0.1.8-uat.md` rows U6/U13/U14/U15/U23, `src/gigai/scout/ui/{package.json,vite.config.js,src/api.js}`, `src/gigai/scout/find_jobs/present_api.py` (full file), `pyproject.toml`, `tools/release_check.py` (full file — confirmed it checks wheel/sdist **metadata** name+version only, not package-data contents, so it needed no changes), `tools/verify_installed_schemas.py` (pattern source for the new verifier), `tools/run_ci_tests.py` (confirmed `verify_installed_*.py` auto-discovery), `Makefile` (confirmed `test`/`test-wheel` exclusion applies), `.github/workflows/pull_request.yaml` (full file, for the job pattern), existing `tests/behaviors/scout_find_jobs/test_present_ui.py` (the existing present_api test file — reused its `running_server`/`serve()` fixture pattern).

2. **EXECUTED — Build (1)**: `yarn --frozen-lockfile install && yarn build` in `src/gigai/scout/ui`. Output: `index.html` (0.40 kB), `assets/index-*.css` (4.98 kB), `assets/index-*.js` (237.96 kB), gzip ~74 kB total. Checked for absolute dev URLs: only match was the string `"127.0.0.1:8765?"` inside an error message in `api.js` (not an actual fetch target — `api.js` calls relative `/api/...` paths per the brief's facts). Verified build determinism: ran a from-scratch `rm -rf dist node_modules && install && build` cycle twice; `diff -rq` between the two `dist/` outputs was empty (identical hashes both times: `index-BSWoZtM8.css`, `index-lxtSZ0ZQ.js`).

3. **EXECUTED — Package data (2)**: added `"ui/dist/*", "ui/dist/assets/*"` to the existing `gigai.scout` package-data entry (same mechanism the pre-existing `data/ui/*.html` entries already use for a non-package data dir under `gigai.scout`; `ui/` has no `__init__.py` so it isn't its own resource anchor). `uv build --wheel` confirmed only `gigai/scout/ui/dist/{index.html,assets/index-*.css,assets/index-*.js}` are added — no `ui/src`, `node_modules`, or `package.json`.

4. **EXECUTED — present_api.py (3)**: added `_ui_dist_root()` / `_resolve_static_resource()` / `_static_content_type()`, and a `_handle_get_static()` branch in `do_GET` for any non-`/api/` path. Resolution is anchored via `importlib.resources.files("gigai.scout").joinpath("ui", "dist")` (works identically from source and from an installed wheel — verified both, see Acceptance below). Traversal: `posixpath.normpath()` + reject any remaining `..` segment or empty segment before joining, so `/../../../etc/passwd`, `/assets/../../../../etc/passwd`, and percent-encoded variants all resolve to `None` → 404 (never read outside `dist/`). Unknown non-`/api` paths → **404** (chose 404 over SPA index.html fallback: the UI has no client-side router — grepped `src/**/*.jsx` for `Router`/`useLocation`/`history`, found none — so a fallback would just mask a real 404 as a blank app shell with no routing to make it meaningful). Missing dist (source checkout, unbuilt): `/` → 503 `{"error":{"code":"ui_not_built","message":"...run \`yarn build\`..."}}`; other static paths still 404 when dist is missing (not 503). `/api/*` behavior is unchanged — routing now checks `path.startswith("/api/")` first and falls through to the existing exact-match logic untouched; confirmed via the existing 26 `test_present_ui.py` tests still passing.

5. **EXECUTED — PR CI freshness check (4)**: new `scout-ui-freshness` job, `if: needs.changes.outputs.code_changed == 'true'` (same gate as `behavior`/`wheel`), `actions/setup-node@v5` pinned to Node 22 LTS (package.json has no `engines` field yet, noted in a workflow comment), same `yarn --frozen-lockfile install && yarn build`, then fails on `git diff --exit-code src/gigai/scout/ui/dist`. Job id `scout-ui-freshness`, display name "Scout UI bundle is up to date" (matches the brief's suggested name); did not touch any existing job id.

6. **EXECUTED — wheel resources (5)**: read `tools/release_check.py` in full — it verifies wheel/sdist **metadata** (name/version match `pyproject.toml`) and checksums, not package-data file contents, so it needed no changes. Instead followed `tools/verify_installed_schemas.py`'s pattern: new `tools/verify_installed_scout_ui.py`, auto-discovered by `tools/run_ci_tests.py`'s `verify_installed_*.py` glob (part of `make test-wheel`/`test-installed`, which I did not run per exclusions). It does **not** pin exact file digests (Vite hashes change every UI edit by design — the freshness job is what keeps them honest); it checks `ui/dist` exists and is non-empty, `index.html` exists, `assets/` exists and is non-empty, every `/assets/...` reference in `index.html` resolves to a real, non-empty shipped file.

## Acceptance evidence

- `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_present_api_static.py -q` → **10 passed**.
- Existing `tests/behaviors/scout_find_jobs/test_present_ui.py` (the file I touched, present_api's existing test file) → **26 passed**, no regressions.
- `uv build --wheel` then wheel contents (`python3 -m zipfile -l`) show:
  ```
  gigai/scout/ui/dist/index.html                 403 bytes
  gigai/scout/ui/dist/assets/index-BSWoZtM8.css  4980 bytes
  gigai/scout/ui/dist/assets/index-lxtSZ0ZQ.js   237967 bytes
  ```
- Freshness check run locally twice: `rm -rf dist node_modules && yarn install && yarn build` produced **byte-identical** `dist/` both times (`diff -rq` empty); this is what `git diff --exit-code src/gigai/scout/ui/dist` will see as clean once the build is committed.
- Installed the built wheel into an isolated `uv venv` (network only for dependency resolution, no live Exa/ATS/provider calls) and ran `tools/verify_installed_scout_ui.py` against that installed interpreter directly (not via `make test-wheel`) → passed, confirming `importlib.resources` resolution works from a real installed wheel, not just source. Also directly exercised `_ui_dist_root()`/`_resolve_static_resource()` from that installed interpreter → both resolved correctly.
- `make unit-tests` → **872 passed, 1249 deselected**, no regressions.

## What's left (for the other 3 packets / coordinator)

- U6 (installable Scout via CLI), U14 (active-gig-id), U15 (resume record wrapper) are other packets — not touched here.
- README "Scout: install and approve" (U7) still needs updating to the one-command flow once the other packets land; out of scope for this packet (static UI serving only).
- `gigai scout run` itself (the CLI command that starts this API + opens the browser, background/foreground, `gigai scout stop`) is a different packet; this packet only ships the UI in the wheel and makes `present_api.py` able to serve it once something starts the server.
