TARGET (p1-secrets-home): the secrets fallback ignores --home. secrets_store.get(name, home_root=None) falls back to GIGAI_HOME/~/.gigai, but callers never pass the home the operator chose: credentials.resolve_reference_value (credentials.py; used by adapters/http.py), the Exa key lookup (scout/find_jobs/exa_client.py _require_api_key), discovery's OpenAI key (scout/find_jobs/discovery/openai_source.py _api_key) and interview prep's key lookup (scout/interview_prep/, grep). So `gigai secrets add exa --home X` then `gigai scout run --home X` can't find the key unless GIGAI_HOME is also set.
Context: PR #37 review P1s the operator wants BEFORE UAT (.orchestrator/reviews/pr37-review-findings.md). REPRODUCE FIRST with a failing test, then fix, then show it passing. Ask questions ONLY via `orca orchestration ask`. TEST BUDGET: exact test files only (list them) + `make unit-tests`; never a whole directory. No git add/commit/stash/reset/clean/push. Say READ vs EXECUTED.
CHANGE: thread home_root from each entry point (the CLI --home / API process --home / run child) down to every key lookup: add an optional home_root parameter to resolve_reference_value, _require_api_key, _api_key and the prep lookup, and pass the real home at every call site (trace them: market_acquisition acquire → Exa client; the discovery run; interview prep; the http adapter's credential_resolver). Environment variables still win; default (no home passed) behaviour is unchanged. Keep signatures backward compatible.
OWNED FILES: src/gigai/credentials.py, src/gigai/adapters/http.py (only if its resolver needs the home), src/gigai/scout/find_jobs/exa_client.py (the key lookup + its callers' plumbing), src/gigai/scout/find_jobs/market_acquisition.py (passing home to the Exa client only), src/gigai/scout/find_jobs/discovery/openai_source.py and discovery/__init__.py (key plumbing only), src/gigai/scout/interview_prep/** (key plumbing only), tests/behaviors/secrets/test_secrets_resolution_order.py, tests/behaviors/scout_find_jobs/test_exa_client.py, test_discovery_openai.py, a new tests/behaviors/secrets/test_secrets_home.py, .orchestrator/workers/p1-secrets-home.md. Don't touch present_api.py or run_supervisor.py (other workers).
ACCEPTANCE: a test that stores a key with `gigai secrets add exa --stdin --home X` (a temp X, GIGAI_HOME unset or pointing elsewhere) and then the Exa client (and the discovery and prep lookups) called with home_root=X find it; an end-to-end CLI check: `gigai secrets add exa --home X` → `gigai scout run --home X --no-browser` → the API child resolves the key (assert via a debug-safe path; never print the value) → stop. Env still wins. Existing tests pass (list the files).
