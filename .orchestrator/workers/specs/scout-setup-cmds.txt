TARGET (scout-setup-cmds): packet B of 4 for `gigai scout run` (U6, U14, U15). The one-step setup commands that `gigai scout run` (packet C) will call.
CONTEXT: operator-approved brief .orchestrator/workers/specs/scout-run-command.brief.txt (read it) and the UAT log /Users/kar/orca/workspaces/gigai/v0.1.8-uat.md rows U6, U13, U14, U15, U23 (read-only). Goal of the whole feature: from a clean GIGAI_HOME + `gigai setup` + `gigai init --target <dir>`, `gigai scout run` reaches a UI that loads config and resume and can start a run, from the INSTALLED package: no Python helper, no manual TOML edit, no `record create`, no source checkout. It is a thin first slice of v0.2.0 roadmap Workstream 3 (D-C, `gigai <gig> run`). Operator decisions (2026-09-24): commit ui/dist + a PR CI freshness check; background run with stop/status (+ --foreground); a starter find-jobs.json + UI guidance on first run (no UI resume upload yet). Rules: uv only in docs and output (never pip); don't add new core→scout imports where a registration seam works, and if you must, list each as debt (the existing `cli.add_command(<scout group>)` block in cli.py is the accepted pattern for now).
FACTS (coordinator, READ; re-verify): default_init.py:100-101 default_inventory() "Scout remains authoring-only"; initialize_defaults at default_init.py:584; scout_candidate_inventory at scout/template.py:257 (the UAT workaround was initialize_defaults(inventory=scout_candidate_inventory())); `gigai approve` at cli.py:2605-2620; NoActiveGigError at workpad.py:74; the active gig lives in <target>/.gigai/project.toml active_gig_id, which no CLI command sets (run.py:165-186, workpad.py:262-276); find-jobs needs the resume as a records/record_*/revisions wrapper (family g45_reference) around a reference (run.py:196-232).
CHANGE:
1. Generic, not Scout-specific (core): `gigai gig use <gig_id> [--target DIR]` sets active_gig_id for the project, validating that the gig is installed and approved (clear error naming `gigai scout install` / `gigai approve` otherwise). Put it where project/workpad commands live; reuse existing project.toml read/write code. Also make NoActiveGigError's message name the fix (`gigai gig use <id>`).
2. New Scout CLI group `gigai scout` in a new module src/gigai/scout/scout_cli.py, registered in cli.py next to the other scout groups (accepted existing pattern; record it as debt):
   - `gigai scout install [--target DIR]`: install Scout into the bound project if missing (the initialize_defaults + scout_candidate_inventory path, through a proper function, not a copy of the UAT workaround), approve it, make it active. Idempotent: rerunning when already installed/approved/active reports that and changes nothing. Don't change default_inventory() for other callers unless you explain why.
   - `gigai scout resume add <file> [--target DIR]`: one step: add the reference (kind resume) AND create the g45_reference record wrapper find-jobs needs. Idempotent for the same file bytes. Print the reference and record ids.
   - Starter config: a function (used by packet C, and by `gigai scout install`) that writes a commented starter <target>/find-jobs.json if missing, which validates against the find-jobs config schema and has obvious placeholders; never overwrite an existing file.
   - JSON output (--json) consistent with the other CLI groups; plain output names the next step.
OWNED FILES (nothing else): src/gigai/scout/scout_cli.py (new), src/gigai/cli.py (the `gig use` command + one registration line), src/gigai/workpad.py (NoActiveGigError message, and a small active-gig setter if none exists), src/gigai/default_init.py or src/gigai/scout/template.py (only a helper for installing Scout, if needed), tests/behaviors/scout_find_jobs/test_scout_setup_cli.py (new), .orchestrator/workers/scout-setup-cmds.md. Packet A owns present_api.py and pyproject.toml; don't touch them.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_scout_setup_cli.py -q` passes, using a temp GIGAI_HOME and temp target: from `gigai setup` (non-interactive, as release.yml's smoke step does) + `gigai init --target`: scout install → installed + approved + active; rerun is a no-op; `gig use` errors for an unknown gig; resume add → the wrapper exists and run.resolve_newest_resume (or the function find-jobs uses) resolves it; the starter config validates. Run the existing tests for any function you change (list the exact files) and `make unit-tests`. Paste `gigai scout --help` and `gigai gig use --help`.
EXCLUSIONS: no schema or storage migration (if the record wrapper needs a schema change, STOP and ask); no git add/commit/stash/reset/clean/push; no live Exa/ATS/provider calls; temp homes only, never the operator's ~/.gigai; focused tests only. Say READ vs EXECUTED. Write .orchestrator/workers/scout-setup-cmds.md, then worker_done.
