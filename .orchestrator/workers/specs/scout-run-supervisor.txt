TARGET (scout-run-supervisor): packet C of 4: `gigai scout run | stop | status` (UAT U13 + U23), plus the misleading missing-config message. Read .orchestrator/workers/specs/scout-run-command.brief.txt (incl. both ADDENDA) and your predecessors' reports: .orchestrator/workers/scout-ui-package.md (A: the API serves the packaged UI at /) and .orchestrator/workers/scout-setup-cmds.md (B: src/gigai/scout/scout_cli.py has `scout install` / `resume add`; src/gigai/scout/template.py has install_scout() and write_starter_find_jobs_config()).
OPERATOR DECISIONS: background by default, with stop/status, plus --foreground; logs to a known path; a starter config + UI guidance on first run (no UI resume upload yet); uv only in output/docs.
CHANGE:
1. `gigai scout run [--target DIR] [--home DIR] [--port N] [--no-browser] [--foreground] [--json]` in scout_cli.py (logic in a new src/gigai/scout/run_supervisor.py):
   - Ensure Scout is installed/approved/active (install_scout, idempotent) and the starter find-jobs.json exists (write_starter_find_jobs_config; never overwrite).
   - If a live instance for this project is already running (state file + pid alive + health check OK), print its URL and exit 0; don't start a second one. A stale state file (dead pid) is cleaned up and reported.
   - Otherwise start the API (present_api's server entry, same process args it takes today: --home/--target/port) as a DETACHED child (subprocess.Popen with start_new_session=True, stdin=DEVNULL, stdout+stderr appended to the log). State file + log live under the gigai home, per project, e.g. <home>/run/scout/<project_id>.json and <home>/logs/scout-<project_id>.log (local only; don't put them in the target). Bind 127.0.0.1 only (existing rule). Port: default 8765; if busy, a clear error naming --port (or pick a free port and say so; your choice, say which).
   - Wait for health (poll a cheap GET, e.g. /api/config or a new /api/health, up to ~15s). On failure: print the log path + the last lines, stop the child, exit non-zero.
   - Print the URL and the log path; open the browser (webbrowser.open) unless --no-browser. --foreground runs the server in this process (Ctrl-C stops it) and still logs.
   - `gigai scout stop [--target] [--home]`: SIGTERM, wait up to ~5s, then SIGKILL; remove the state; idempotent when not running. `gigai scout status [--json]`: running / stopped / crashed (state present but pid dead) + url, pid, log path, started_at.
2. Missing config/resume messages (UAT addendum): present_api /api/config must return a specific error when find-jobs.json is missing (e.g. 404 with code "config_missing" and a message naming the file path and `gigai scout install` / `gigai scout run`, which write a starter), and the UI (src/gigai/scout/ui/src: api.js's 404 text "That run could not be found." is being used for config errors) must show that message instead of "run could not be found". If no resume is saved, the UI must say so with the exact command `gigai scout resume add <file>` (check where the API reports the resume today; add a small field or error code if needed, additively). Rebuild ui/dist (`yarn --frozen-lockfile install && yarn build`) and commit-ready: the PR CI freshness job compares it.
OWNED FILES (nothing else): src/gigai/scout/scout_cli.py, src/gigai/scout/run_supervisor.py (new), src/gigai/scout/find_jobs/present_api.py (config/resume error codes + optional /api/health only; don't touch static serving), src/gigai/scout/ui/src/**, src/gigai/scout/ui/dist/** (rebuilt), tests/behaviors/scout_find_jobs/test_scout_run_supervisor.py (new), tests/behaviors/scout_find_jobs/test_present_ui.py (config-missing cases only), .orchestrator/workers/scout-run-supervisor.md. Other workers are editing filters.py, ats_board_clients.py, contracts.py, market_acquisition.py, proposal_execution.py, selection.py, pyproject.toml, uv.lock: don't touch them.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_scout_run_supervisor.py tests/behaviors/scout_find_jobs/test_present_ui.py tests/behaviors/scout_find_jobs/test_present_api_static.py -q` passes. The supervisor tests use a temp GIGAI_HOME, a temp non-git target and an ephemeral port, and really start and stop the child: run → status running → GET / returns the UI html and GET /api/config works → second run reuses → stop → status stopped; a stale state file is cleaned; health failure → non-zero exit with the log path; no orphan process left (assert the pid is gone). `make unit-tests` passes. Paste `gigai scout --help`, and the output of a real `gigai scout run --no-browser` / `status` / `stop` in a temp home.
EXCLUSIONS: no live Exa/ATS/provider calls (don't start a run in tests; config/UI serving only); never touch the operator's real ~/.gigai (temp homes only); no git add/commit/stash/reset/clean; kill every process you start; focused tests only. Say READ vs EXECUTED. Write .orchestrator/workers/scout-run-supervisor.md, then worker_done.
