TARGET (secrets-core-r1): coordinator review of your secrets-core packet. The catalog (exa/openrouter/openai, all used in code), the CLI shape, the resolver change and the 0600 handling are accepted. Two bugs were REPRODUCED by the coordinator in a temp GIGAI_HOME, plus one hardening fix:
1. BUG, `$` mangled: `echo 'a$b${HOME}c' | gigai secrets add exa --stdin`, then secrets_store.get("EXA_API_KEY") != the input. dotenv_values() interpolates ${VAR} by default. Read every value with interpolation OFF (`dotenv_values(path, interpolate=False)`; check the installed python-dotenv signature). Also check that set_key's quoting round-trips values containing quotes, `#`, spaces, `=` and a trailing backslash; fix what doesn't.
2. BUG, empty value counts as set: a `.env` line `EXA_API_KEY=` makes `gigai secrets list` print `exa: set (.env)`. Treat an empty or whitespace-only stored value as unset in names_set() AND get() (get → None). Likewise, an empty environment variable must not count as "set (environment)" in `list` (os.environ.get(...) falsy → not set). The resolver already rejects empty values; keep that.
3. HARDENING: set_key/unset_key write a temp file and then replace, and the file is re-chmodded to 0600 only afterwards, so there's a brief window with the default umask (0644). Run the writes under a restrictive umask (os.umask(0o077), restored in a finally) so the file is never world-readable, and keep the post-write chmod.
Add tests for each: a round-trip of those special-character values; an empty value → unset in list/json and get() is None; an empty env var → not "set (environment)"; the mode after write stays 0600 (keep the existing test).
OWNED FILES (nothing else): src/gigai/secrets_store.py, src/gigai/secrets_cli.py, tests/behaviors/secrets/test_secrets_store.py, tests/behaviors/secrets/test_secrets_cli.py, tests/behaviors/secrets/test_secrets_resolution_order.py, .orchestrator/workers/secrets-core.md (append r1).
ACCEPTANCE: the 3 secrets test files pass; tests/behaviors that touch resolve_reference_value/adapters.http (the same 2 files you ran before) pass; make unit-tests passes. Paste the new test names.
EXCLUSIONS: never read or print the real ~/.gigai/.env or any real secret; temp homes only. No git add/commit/stash/reset/clean; no new dependencies; focused tests only. Say READ vs EXECUTED. Then worker_done.
