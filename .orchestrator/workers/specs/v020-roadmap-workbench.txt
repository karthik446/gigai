TARGET (v020-roadmap-workbench): write a PROPOSED, NOT ACCEPTED v0.2.0 roadmap: gigai as a personal gig workbench. Brief (operator-approved 2026-09-23, relayed by the v0.1.8-worktree session): .orchestrator/workers/specs/v020-roadmap-workbench.brief.txt. Read it first; it has the operator decisions D-A..D-G, workstreams 0-7, the Workstream 0 evidence and the open items. Follow it.
PATH: docs/development/v0.2.0/roadmaps/v0.2.0-gig-workbench-roadmap.md (new). First line after the title: **Status:** Proposed — not accepted. Match the style of docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md (read it) and the evidence discipline of the v0.2.0 spikes (every claim cites file:line or a command actually run; READ vs EXECUTED).
MUST HAVE:
- Operator decisions D-A..D-G recorded, not reopened. D-B (monorepo for now) amends S21 decision 1; S21 is being revised in parallel to say so. Cite S21 and don't edit it.
- Workstreams 0-7 + "Slimmer S20", each with: goal, size, dependencies, what "done" is observably (a command or test an operator could run), which spikes it draws on (S16/S17/S19/S20/S21), and an explicit "Pull into 0.1.9?" line with a reason. Workstream 0 (connect secrets) is the brief's candidate: give it a concrete packet breakdown (files touched, tests), still labelled proposed.
- Re-verify every Workstream 0 evidence line in the brief before citing it (setup.py:47-48, :199; credentials.py:55-72, :12; private_transfer.py:102; exa_client.py:38,66-73; M1 runbook :151,:162). Correct any drift and say so. NEVER read secret values: for ~/.gigai/.env you may only confirm existence and mode (`stat`) and list variable NAMES (e.g. `cut -d= -f1`), nothing else. If you can't do that without seeing values, skip it and say so.
- Dependency order (0 early; 1 → 2 → 3; 4/5/6 after 1; 7 last) as a small table or DAG, and the open items from the brief in one "Open questions" section.
- A "Relationship to spikes" section: this roadmap sequences S16/S17/S19/S20/S21; it doesn't replace them.
OWNED FILES (nothing else): docs/development/v0.2.0/roadmaps/v0.2.0-gig-workbench-roadmap.md (new), docs/development/v0.2.0/README.md (add one link line to the roadmap, marked proposed), .orchestrator/workers/v020-roadmap-workbench.md. Another worker is editing the S21 spike in parallel; don't touch docs/development/v0.2.0/spikes/.
ACCEPTANCE: the file exists with the sections above; the citations spot-check with sed -n; no text phrases an open item as decided; `git status --short` changes only your owned files (plus whatever the S21 worker changes in spikes/).
EXCLUSIONS: docs only; no code, schema or test edits; no git add/commit/stash/reset/clean; no live/provider runs; no secret values read or printed. Say READ vs EXECUTED. Write .orchestrator/workers/v020-roadmap-workbench.md on finish or on a question, then worker_done.
