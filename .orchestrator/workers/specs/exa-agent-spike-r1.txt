TARGET (exa-agent-spike-r1): coordinator review of S23 (docs/development/v0.1.9/spikes/S23-scout-setup-interview-exa-agent.md). Accepted: the API findings (incl. input.exclusion objects), the question list, the query template, the strict schema, the $2 cap, the spend reconciliation ($0.759, recounted), and the metrics table (the coordinator recounted usable boards from every *.boardcheck.json and they match).
FIX:
1. Line ~316-317 says "a decisive, 100x-plus spread (auto $0.146 vs low $0.0042)". That's ~35x; the Recommendation already says 3x-35x. Correct it, and soften "decisive" to what 5 single runs show.
2. Replication (LIVE, authorized, same guards; ≤ $0.15 total for this r1): the recommended cell (v4 board-URL-explicit, strict schema, low effort) ran once. Run it 3 more times: (a) once more with the same exclusion list (repeatability: does it return a similar count and usable %?); (b) twice as a "week 2" simulation: the exclusion list = the original 3 + every company returned by all runs so far (as input.exclusion objects), to see whether novelty holds when the known list grows (new companies count, usable %, $/new usable board). Board-check each with check_boards.py (free). Save redacted fixtures; append to spend.jsonl.
3. Update the Results table (new rows), the Recommendation (does low still hold? what does week 2 say about weekly cadence and the expected yield?), Open questions, and Change log. Recount totals from spend.jsonl.
OWNED FILES: the S23 doc, research/exa_agent_spike/** (new fixtures/inputs, spend.jsonl), .orchestrator/workers/exa-agent-spike.md (append r1).
EXCLUSIONS: same as before: key only via secrets_store, never printed; no product code; no git add/commit/stash/reset/clean. Say READ vs EXECUTED. Then worker_done.
