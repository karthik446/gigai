TARGET (secrets-core): v0.2.0 roadmap Workstream 0, PULLED INTO 0.1.9 by the operator (2026-09-23). Read docs/development/v0.2.0/roadmaps/v0.2.0-gig-workbench-roadmap.md "Workstream 0 — Connect secrets" first (evidence + packets P0-P2). Build P0+P1+P2: a secrets store, a known-service map, the `gigai secrets` CLI, and env-then-.env lookup in the credential resolver. P3 (Exa) is a separate packet after this one; P4 waits for Workstream 1.
FACTS (coordinator, READ): home root = $GIGAI_HOME or ~/.gigai (src/gigai/setup.py:47-48). ~/.gigai/.env exists (mode 0600) but NO code reads it (only private_transfer.py:102 excludes it from transfers). credentials.py:55-73 resolve_reference_value resolves only kind="environment" via os.environ (line ~68); its caller is src/gigai/adapters/http.py:11,32. gigai has 5 deps and no .env parser.
CHANGE:
1. Dependency: use python-dotenv; don't hand-roll a .env parser (operator rule: don't make up what a standard library already does). `uv add "python-dotenv>=1.0"` (updates pyproject.toml dependencies + uv.lock). Read/write with its API (dotenv_values, set_key, unset_key; check their signatures in the installed version, don't guess).
2. src/gigai/secrets_store.py (new): the path (home root/.env, respecting GIGAI_HOME), get(name) -> str | None, set(name, value), remove(name), names_set() -> set[str]. Create the file with mode 0600 if missing; after EVERY write, enforce 0600 (python-dotenv may rewrite via a temp file); keep other lines and comments. Never log or print values.
3. src/gigai/secrets_catalog.py (new): a small map of logical service → env var, only for services gigai actually uses today (grep the code for *_API_KEY env reads and the credential refs; e.g. exa → EXA_API_KEY, openrouter → OPENROUTER_API_KEY). Unknown service → a clear error listing the known ones. Don't invent services the code doesn't use.
4. src/gigai/secrets_cli.py (new) + register it in src/gigai/cli.py next to the other groups: `gigai secrets add <service>` (hidden prompt, no echo, confirm; add `--stdin` to read one line from stdin for scripts/tests), `gigai secrets list` (every catalog service → set/unset, and whether it comes from the environment or .env; NEVER a value), `gigai secrets rm <service>`. Follow the existing CLI conventions (read a neighbouring *_cli.py; --json output if the others offer it).
5. Resolution order in credentials.py: for kind="environment", os.environ first, then secrets_store.get(). Keep the existing error type and message shape when neither has it. Don't change kind validation or the secret-manager kinds.
6. Roadmap placement: in the roadmap's Workstream 0, change "Pull into 0.1.9?" to record "Pulled into 0.1.9 by the operator on 2026-09-23 (P0-P3; P4 waits for Workstream 1)" and add a Change log line. Nothing else in the roadmap.
OWNED FILES (nothing else): pyproject.toml (dependencies only), uv.lock, src/gigai/secrets_store.py, src/gigai/secrets_catalog.py, src/gigai/secrets_cli.py, src/gigai/cli.py (registration lines only), src/gigai/credentials.py, tests/behaviors/secrets/__init__.py, tests/behaviors/secrets/test_secrets_store.py, tests/behaviors/secrets/test_secrets_cli.py, tests/behaviors/secrets/test_secrets_resolution_order.py (all new), docs/development/v0.2.0/roadmaps/v0.2.0-gig-workbench-roadmap.md (step 6 only), .orchestrator/workers/secrets-core.md. Don't touch src/gigai/scout/** (the next packet owns exa_client.py).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/secrets/test_secrets_store.py tests/behaviors/secrets/test_secrets_cli.py tests/behaviors/secrets/test_secrets_resolution_order.py -q` passes. Tests use a temp GIGAI_HOME (monkeypatch) and assert: the file mode stays 0600 after add/rm; other lines survive; no value appears in CLI stdout/stderr or --json output; env wins over .env; .env is used when env is absent; neither → the existing error. Also run the existing credential/http tests that touch the resolver (grep tests for resolve_reference_value / adapters.http; list the exact files you ran) and `make unit-tests`. Paste the `gigai secrets --help` output.
EXCLUSIONS: NEVER read, print, or copy the operator's real ~/.gigai/.env or any secret value; all tests use temp homes. No schema or storage migration; no git add/commit/stash/reset/clean/push; no live/provider calls (network only for `uv add` resolving from PyPI); focused tests only (the selectors above, plus the exact existing files you list, plus make unit-tests). Say READ vs EXECUTED. Write .orchestrator/workers/secrets-core.md on finish or on a question, then worker_done.
