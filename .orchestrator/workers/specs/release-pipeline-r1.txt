TARGET (release-pipeline-r1): rework of your release-pipeline packet after coordinator review. Your report: .orchestrator/workers/release-pipeline.md. Three fixes to .github/workflows/release.yml.
CHANGE:
1. BLOCKER, caller permissions: the ci-docs-skip worker made pull_request.yaml's `changes` job request `permissions: {contents: read, actions: read}` (it looks up the previous PR run via gh api). release.yml calls pull_request.yaml via `uses:` from two jobs, `ci` and `post-release-compatibility`, and release.yml's top-level permissions are `contents: read` only. GitHub refuses to start a called workflow whose jobs ask for more than the caller grants, so the release would fail at startup. Add `permissions: {contents: read, actions: read}` to both calling jobs (job level; don't widen the top-level block).
2. MAJOR, retry the install itself: the JSON API (`/pypi/gigai/<v>/json`) can show a version before the simple index that uv installs from does. Polling JSON then installing once keeps a narrower version of the same race. In both verify-testpypi and verify-pypi, retry the actual `uv tool install --refresh …` (same args as now) in a bounded loop: e.g. up to 20 attempts, 30s apart, with a clear message on each failure and on the final give-up. Drop the curl poll, or keep it only as a cheap first gate; the install retry is what counts. Uninstall/clean between attempts only if uv needs it (check `uv tool install --help`; `--force` may be the right flag).
3. Timeouts: raise `timeout-minutes` of verify-testpypi and verify-pypi to 20 so the ~10 min retry window plus setup/doctor fits.
Also add to your job-graph test: `ci` and `post-release-compatibility` grant `actions: read` (extend your hand-rolled parser minimally, or do a scoped block check; say which).
OWNED FILES (nothing else): .github/workflows/release.yml, tools/release_notes.py, tests/behaviors/installed_release/test_release_check.py, tests/behaviors/installed_release/test_release_notes.py, .orchestrator/workers/release-pipeline.md (append an "r1" section).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/installed_release/test_release_check.py tests/behaviors/installed_release/test_release_notes.py -q` passes; system `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` OK; paste the final retry snippet and both calling-job permission blocks.
EXCLUSIONS: no git add/commit/stash/reset/clean/push, no tags, no gh release edits, no workflow_dispatch, no live PyPI calls; focused tests only (exact selectors above). Say READ vs EXECUTED. Update the report file, then worker_done.
