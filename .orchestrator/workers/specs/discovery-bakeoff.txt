TARGET (discovery-bakeoff): spike S24, "company discovery bake-off". The operator judged S23's Exa Agent (low) too unreliable (identical input returned 6, then 0 companies; 1 run in 4 found nothing usable) and wants other approaches compared on the SAME task. SPIKE ONLY: docs + scripts under research/, no product code under src/.
READ FIRST: docs/development/v0.1.9/spikes/S23-scout-setup-interview-exa-agent.md (task, interview answers, query template, strict schema, metrics, results incl. repeatability) and research/exa_agent_spike/ (spend_guard.py, redact.py, check_boards.py, run_agent.py, fixtures). REUSE S23's spend guard pattern, redaction and board check; don't rewrite them.
BUDGET (operator, hard): $10 TOTAL for this bake-off (separate from S23's $0.83), at most $2 per call. A tally file research/discovery_bakeoff/spend.jsonl; refuse any call whose worst case would exceed either cap; cheap settings first. You don't have to spend it all.
CANDIDATES (operator-chosen; no others):
1. Exa Websets (Exa's list-building product; read Exa's Websets docs for the endpoint, criteria/enrichment, pricing and cost controls; EXA_API_KEY).
2. OpenAI web search via the Responses API web_search tool with structured output (read OpenAI's docs for the current tool name, models that support it, and pricing; pick the cheapest model that returns a usable strict-schema list and say why; OPENAI_API_KEY).
3. Tavily (search + extract; read Tavily's API docs; you assemble the company list from results; TAVILY_API_KEY).
4. A non-agent baseline: public US H-1B data (DOL OFLC LCA disclosure data, the latest fiscal-year file): employers with certified LCAs for software/backend-type SOC codes in the US → a candidate company list with official sponsorship evidence (case count + a link to the source file) → probe Greenhouse/Ashby/Lever boards for each (slug guesses from the company name, verified by a real board poll) → the same board check. Free; the file is large: download it to a gitignored/scratch location, NEVER commit it; commit only a small derived sample (≤ 200 rows) plus the script.
5. Reference: S23's Exa Agent low numbers (don't re-run unless needed for a same-day comparison; if you do, it counts against the $10).
SAME TASK FOR ALL: the operator's UAT answers (roles staff/senior backend; US; remote or Denver, CO; visa sponsorship needed = yes), the exclusion list (Coupang, ClickHouse, Gen Digital + every company found in S23's fixtures, to measure NOVELTY), and S23's strict output fields where the provider supports structured output.
RUNS: 3 per candidate (repeatability), H-1B baseline once (it's deterministic) plus a board-probe pass.
METRICS per run (same definitions as S23; recount from rows): cost, latency, companies returned, % new vs exclusions, % usable board (free ATS poll with ≥1 matching US posting), % sponsorship claims with a VERIFIED source (fetch it), run-to-run overlap/variance, $ per new usable board (and blended across the 3 runs). For H-1B: how many sponsors → how many resolve to a usable board, and the effort/cost to do that at scale.
KEYS: read every key via gigai's secrets lookup (`from gigai import secrets_store; secrets_store.get("OPENAI_API_KEY")`, etc.; env first, then ~/.gigai/.env). NEVER print, log, echo, save or commit a key or request headers; redact every saved response. Don't open ~/.gigai/.env yourself. If a key is missing, skip that candidate and say so.
DELIVERABLES: docs/development/v0.1.9/spikes/S24-company-discovery-bakeoff.md (spike ticket format like S23: Status / Problem / Operator decisions / Tasks / Acceptance / Investigate / Results (one comparison table + per-candidate notes) / Recommendation (which approach, or which combination, e.g. H-1B list + agent for novelty) / Open questions / Non-claims / Change log); an S24 bullet in docs/development/v0.1.9/spikes/README.md; research/discovery_bakeoff/** (scripts, redacted fixtures, spend.jsonl, small derived H-1B sample, README).
OWNED FILES (nothing else): research/discovery_bakeoff/**, docs/development/v0.1.9/spikes/S24-company-discovery-bakeoff.md, docs/development/v0.1.9/spikes/README.md (one bullet), .orchestrator/workers/discovery-bakeoff.md. Also add the H-1B download directory to .gitignore if it lives under the repo (then .gitignore is owned too, for that line only).
ACCEPTANCE: the comparison table's totals match spend.jsonl (recount); total ≤ $10 and no call over $2 (show the tally); fixtures redacted (grep for authorization/x-api-key/api_key header values); no large data file tracked (git status shows only small files); every claim cites a doc URL or a command run.
EXCLUSIONS: no product code; no Claude web search (operator has no key); no other providers; no git add/commit/stash/reset/clean; never print a key. Say READ vs EXECUTED. Write .orchestrator/workers/discovery-bakeoff.md (with the running spend) on finish or on a question, then worker_done.
