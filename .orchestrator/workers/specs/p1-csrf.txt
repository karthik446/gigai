TARGET (p1-csrf): protect every state-changing route of the Scout local API (src/gigai/scout/find_jobs/present_api.py) against cross-site requests: POST /api/run, POST /api/discover, PUT /api/setup, and any other POST/PUT/PATCH/DELETE route (grep them all). Today they only check that the client address is loopback, which a malicious web page in the operator's browser can pass with a no-cors POST.
Context: PR #37 review P1s the operator wants BEFORE UAT (.orchestrator/reviews/pr37-review-findings.md). REPRODUCE FIRST with a failing test, then fix, then show it passing. Ask questions ONLY via `orca orchestration ask`. TEST BUDGET: exact test files only (list them) + `make unit-tests`; never a whole directory. No git add/commit/stash/reset/clean/push. Say READ vs EXECUTED.
CHANGE: for every state-changing request, require (1) Content-Type application/json (so a browser must send a CORS preflight, which the server never approves), and (2) an Origin header, when present, equal to the served origin (http://127.0.0.1:<port> or http://localhost:<port> for the bound port); Host must also match the bound host:port (DNS-rebinding guard). Reject with 403 {"error":{"code":"forbidden_origin"|"unsupported_media_type",...}} using the existing error helper. Never add Access-Control-Allow-Origin. GET routes unchanged. Make sure the bundled UI (src/gigai/scout/ui/src/api.js) already sends Content-Type: application/json on its writes; if not, fix api.js (and rebuild ui/dist).
OWNED FILES: src/gigai/scout/find_jobs/present_api.py (request-guard only), src/gigai/scout/ui/src/api.js + ui/dist (only if needed), tests/behaviors/scout_find_jobs/test_present_csrf.py (new), .orchestrator/workers/p1-csrf.md.
ACCEPTANCE: tests using a real server on an ephemeral port: a cross-origin POST with Origin: https://evil.example → 403; a text/plain (no-cors style) POST → 403 (415 or 403, say which); a Host-header mismatch → 403; the UI's own request shape (same origin, application/json) → accepted for /api/run, /api/discover and PUT /api/setup (stub the heavy backends as the existing tests do). Re-run tests/behaviors/scout_find_jobs/test_present_setup_discover.py, test_present_ui.py and test_present_api_static.py.
