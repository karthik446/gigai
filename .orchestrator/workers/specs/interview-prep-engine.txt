TARGET (interview-prep-engine): pull Scout interview prep into v0.1.9 (operator, 2026-09-24): when the operator marks a posting "interview scheduled", Scout prepares them for that interview. Backend + CLI in this packet; the UI prep card is a follow-up after the Scout UI work (S2-B) lands.
READ FIRST: docs/development/v0.1.9/spikes/S18-scout-interview-prep-graphs.md (esp. §1 existing pieces, §4 sources, §5 minimal first slice, open questions); the existing pieces: src/gigai/application_events.py (the "interview_scheduled" event, :30), src/gigai/scout/interview.py, interview_records.py, interview_cli.py, research.py / research_v3.py / research_inputs.py, src/gigai/scout/data/goalgraphs/prepare-interview.md and research-role.md; the committed discovery package src/gigai/scout/find_jobs/discovery/ (REUSE its OpenAI web_search client, its budget guard, its source-URL verification and its secrets handling; don't write a second client).
COORDINATOR DEFAULTS (S18's open questions; the operator can override): question prediction = CATEGORIES only (behavioural / system design / coding-in-their-stack / domain), each with a one-line "why" tied to the posting and the resume, no per-question probabilities; agent path = CLI + the local API later (no MCP now); no Jev; no outcome recording yet.
CHANGE (Scout-only; core never imports it):
1. Trigger: when an application reaches interview_scheduled for a posting (read how application events are recorded; reuse that path), Scout can build a prep for that posting. Provide `gigai scout prep <posting-or-application-id> [--json]` (foreground; prints a summary and where the prep is stored) and a function the API/UI will call later. Idempotent per (posting, resume revision); `--refresh` re-runs.
2. The prep (one record per posting, stored the way interview/research records are today; reuse the existing record kinds/schemas where they fit, and if a NEW record kind or schema is required, STOP and ask):
   a. Company research via OpenAI web_search with sources (engineering blog, stack, product, recent news, interview-process reports). Every claim carries a source URL, and sources are verified (resolve) as discovery does. Budget: at most $0.50 per prep (reuse the guard); a missing OpenAI key → the company-research section is skipped with the fix named, and the rest still builds.
   b. Role research from the posting text Scout already has (no search).
   c. Likely question categories (above), grounded in the posting + resume + company research, produced with the run's configured model target (find-jobs default_model_target → the same resolution assess uses); no silent provider fallback.
   d. Prep notes: resume points to lead with and gaps to prepare an answer for, reusing assess's requirement × resume matrix for that posting when it exists.
3. Privacy: the resume never goes to the web-search call (only to the configured assess model, as assess already does); say this in the code and the report.
OWNED FILES (nothing else): src/gigai/scout/interview_prep/** (new package) or additions in src/gigai/scout/interview*.py if that's the cleaner home (say which), src/gigai/scout/scout_cli.py (the prep command only), tests/behaviors/scout_find_jobs/test_interview_prep*.py (new), small fixtures, .orchestrator/workers/interview-prep-engine.md. Don't touch present_api.py or ui/ (the UI card is a follow-up).
ACCEPTANCE: focused tests pass with NO live calls (MockTransport + fixtures for web search; a stand-in model for categories): trigger/idempotency, a missing key → partial prep, budget guard, source verification, no resume in the search request (assert on the captured request), categories grounded (each cites the posting/resume/company item it came from). `make unit-tests` passes. One live smoke run is authorized: `gigai scout prep` on ONE posting in a temp home, ≤ $0.50, keys via secrets_store (never printed); paste the summary. TEST BUDGET: exact files only.
EXCLUSIONS: no schema/storage migration without asking; no commits; temp homes only; never print keys. Say READ vs EXECUTED. Write .orchestrator/workers/interview-prep-engine.md, then worker_done.
