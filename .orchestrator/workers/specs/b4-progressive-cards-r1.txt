TARGET (b4-progressive-cards-r1): coordinator review of B4. Accepted: the progress/ layer design, /api/runs/{id}/progress, the card UI, live step status without touching run.py/bindings, deterministic dist.
ONE FIX (your own contract: "progress must never break the sealed run"): only the ProgressWriter CONSTRUCTION is guarded (_progress_writer / _assess_progress_writer catch Exception → None). Every later write (start_step, finish_step, posting_acquired, assessment_started, assessment_finished, not_assessed, cap_known → _append_line / _replace_json) can raise OSError (disk full, permissions, a removed run dir) straight into acquire_node / assess_node and fail the sealed run. Worse, acquire calls progress.finish_step("acquire", ok=False) inside `except BaseException:`, so a failing progress write there would replace the real exception.
CHANGE: make every public ProgressWriter method best-effort: catch Exception (not BaseException), record the first failure once (e.g. a `disabled` flag + one warning to the run's existing log/stderr, never raising), and stop writing after the first failure. Keep the readers tolerant of truncated/garbled last lines (check _read_jsonl already skips a bad trailing line; add it if not). Add tests: a writer whose directory becomes unwritable mid-run (chmod or monkeypatched open raising OSError) → acquire_node and assess_node still complete with correct sealed outputs; an exception raised inside acquire is the one that propagates even when finish_step fails; a truncated last jsonl line is skipped by the reader.
OWNED FILES: src/gigai/scout/find_jobs/progress.py, src/gigai/scout/find_jobs/market_acquisition.py and src/gigai/scout/proposal_execution.py (only if a call site needs adjusting), tests/behaviors/scout_find_jobs/test_progress.py, your worker file (append r1).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_progress.py tests/behaviors/scout_find_jobs/test_m1_end_to_end.py -q` passes; make unit-tests passes. TEST BUDGET (operator rule, you ran the whole scout_find_jobs directory last time): exact files only, never a directory.
EXCLUSIONS: no git add/commit/stash/reset/clean; no live calls. Say READ vs EXECUTED. Then worker_done.
