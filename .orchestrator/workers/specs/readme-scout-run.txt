TARGET (readme-scout-run): packet D part 1 for `gigai scout run`: rewrite the README's Scout sections to the one-command flow (UAT U7 said "Scout: install and approve" was wrong). Docs only.
READ FIRST: the reports .orchestrator/workers/scout-setup-cmds.md, scout-run-supervisor.md, scout-ui-package.md, secrets-core.md, secrets-exa.md (what now exists), and `uv run --locked gigai scout --help`, `gigai scout run --help`, `gigai secrets --help`, `gigai gig use --help` (EXECUTE these for exact flags).
THE FLOW TO DOCUMENT (verify each step yourself in a temp GIGAI_HOME and a temp non-git target; use `gigai setup --non-interactive ...` as release.yml's smoke step does, since the interactive setup needs a TTY):
  uv tool install gigai → gigai setup → gigai init --target <dir> → gigai secrets add exa → gigai scout resume add <file> → gigai scout run (opens the UI; edit find-jobs.json from the starter) → gigai scout status / stop.
CHANGE (README.md only): replace the "Scout: install and approve" section and the "For agents" / start-the-API instructions that still describe `python -m gigai.scout.find_jobs.present_api`, `yarn dev`, manual approve, `record create` or manual TOML edits, with the flow above. Keep a short "from a source checkout (contributors)" note for the Vite dev server if it's still useful, clearly labelled. uv only, never pip. Mention where logs and state live (from the supervisor report) and that EXA_API_KEY comes from `gigai secrets add exa` (env still wins). Don't document features that don't exist yet (the setup interview and "Discover companies" are future work; don't mention them).
OWNED FILES (nothing else): README.md, .orchestrator/workers/readme-scout-run.md.
ACCEPTANCE: every command in the new README sections was EXECUTED in the temp home (paste the transcript, trimmed; stop the server at the end and confirm no process is left); `grep -nE 'pip install|python -m gigai.scout.find_jobs.present_api|yarn dev|record create' README.md` finds nothing outside the labelled contributor note.
EXCLUSIONS: temp homes only (never the operator's ~/.gigai); no live Exa/ATS/provider calls (don't click Run); no code changes; no git add/commit/stash/reset/clean; no tests needed. Say READ vs EXECUTED. Write .orchestrator/workers/readme-scout-run.md, then worker_done.
