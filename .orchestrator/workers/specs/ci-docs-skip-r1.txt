TARGET (ci-docs-skip-r1): rework of your ci-docs-skip packet after coordinator review. Your report: .orchestrator/workers/ci-docs-skip.md.
CHANGE:
1. BLOCKER, caller permissions: your `changes` job now requests `actions: read`. Every workflow that calls pull_request.yaml via `uses:` must grant it, or GitHub refuses to start the called workflow (the nightly and the release both call it). Add `permissions: {contents: read, actions: read}` at job level to the `compatibility` job in .github/workflows/compatibility_job.yaml. (release.yml's two calling jobs are being fixed by the release-pipeline worker; don't touch release.yml.)
2. Add a test in tests/behaviors/ci_tooling/test_ci_changes.py: for every .github/workflows/*.y*ml job that `uses: ./.github/workflows/pull_request.yaml`, the job grants `actions: read`. Parse job blocks minimally (no PyYAML in the test env; a scoped 2-space/4-space block parser like tools/release_notes.py's `parse_workflow_job_needs`, or reuse that function if it fits). It will FAIL on release.yml until the other worker lands; that's expected. Report it and don't touch release.yml.
3. Minor: filter the run lookup to pull_request runs (`&event=pull_request` on the runs query), so a workflow_dispatch run on the same SHA with another profile can't be taken as "the source suite passed". Add a unit test if the query building is testable.
4. Minor: confirm tools/ci_changes.py is stdlib-only and runs on the runner's system python3 (the `changes` job has no setup-python). State the minimum Python version it needs.
OWNED FILES (nothing else): .github/workflows/compatibility_job.yaml, .github/workflows/pull_request.yaml, tools/ci_changes.py, tests/behaviors/ci_tooling/test_ci_changes.py, .orchestrator/workers/ci-docs-skip.md (append an "r1" section).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/ci_tooling/test_ci_changes.py -q` passes, except the release.yml caller check if release.yml isn't fixed yet (name it); system python3 yaml.safe_load OK for compatibility_job.yaml and pull_request.yaml.
EXCLUSIONS: no git add/commit/stash/reset/clean/push, no workflow_dispatch, no live runs; focused tests only (the exact selector above). Say READ vs EXECUTED. Update the report file, then worker_done.
