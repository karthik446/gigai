TARGET (s2b-setup-interview-ui): S23 stage 2, packet B: the setup interview in the Scout UI + the "Discover companies" button. Operator decisions: all 11 S23 questions (docs/development/v0.1.9/spikes/S23-scout-setup-interview-exa-agent.md §2), asked ONCE in the Scout UI (`gigai scout run` opens it; if no discovery prefs exist, the UI shows the interview first), pre-filled from the current find-jobs.json where possible; answers write BOTH find-jobs.json (roles, merged_queries, location, remote, countries, visa_sponsorship_required: read FindJobsConfig in src/gigai/scout/find_jobs/contracts.py; keep other fields) AND the discovery prefs. A "Discover companies" panel shows "last run N days ago", cost, new boards with sponsorship evidence (+ a verified/unverified mark), and a button to start a session (weekly cadence is shown, not scheduled: gigai has no scheduler).
INTERFACE CONTRACT between packets S2-A (engine) and S2-B (UI/API); both must match it exactly:
- Module src/gigai/scout/find_jobs/discovery/__init__.py exports:
  run_discovery(*, home_root: Path, target: Path, prefs: DiscoveryPrefs, runs: int = 3, on_progress: Callable[[dict], None] | None = None) -> DiscoveryResult   (synchronous; may take ~5-30 min; never raises for provider errors, records them)
  latest_discovery(*, home_root: Path, target: Path) -> DiscoveryResult | None
  load_prefs(*, home_root: Path, target: Path) -> DiscoveryPrefs | None ; save_prefs(*, home_root: Path, target: Path, prefs: DiscoveryPrefs) -> None
- DiscoveryPrefs (frozen dataclass, to_json/from_json): the 11 S23 interview answers: roles (tuple[str]), titles_to_avoid, countries (ISO alpha-2), work_mode ("remote"|"hybrid"|"onsite"|"any"), city (str|None), visa_sponsorship_required (bool), exclude_companies, watch_companies, company_stage_size (str|None), industries_include, industries_exclude, must_have_stack, dealbreaker_stack, cadence_days (int, default 7), budget_usd_per_session (float, default 0.50).
- DiscoveryResult (to_json): discovery_id, status ("running"|"succeeded"|"failed"|"partial"), started_at, finished_at, cost_usd, sources: [{name: "openai_web_search"|"h1b", runs, cost_usd, error|null}], new_boards: [{company, provider, board_token, careers_url, sponsorship: "yes"|"no"|"unknown", sponsorship_evidence, evidence_source_url, evidence_verified: bool, found_by: [..], matching_us_postings: int}], skipped: {reason: count}.
- Stored under the gigai home (per project), not in the target: <home>/scout/<project_id>/discovery/prefs.json and discovery/runs/<discovery_id>.json.
CHANGE:
1. present_api.py: GET /api/setup (prefs or 404 prefs_missing + a pre-fill derived from find-jobs.json), PUT /api/setup (validate → save_prefs + update find-jobs.json atomically; field errors as 400 with per-field messages), POST /api/discover (starts run_discovery in a background THREAD in the API process; one at a time → 409 if running; 202 {discovery_id}), GET /api/discover/latest (latest_discovery + days_ago + running flag; progress via on_progress kept in memory). Loopback only; the existing error conventions.
2. UI (src/gigai/scout/ui/src): an interview form (11 questions; free-text list inputs as tags; yes/no toggles; country picker from ISO codes), shown when /api/setup says prefs_missing, editable later from a "Preferences" link; a Discover panel (button, running state with progress, results list of new boards with evidence + source link + verified mark, cost, last-run age). Rebuild ui/dist (the CI freshness job).
3. Until S2-A lands, code against the contract with a test stub (monkeypatch the discovery module in tests). Import it as `from gigai.scout.find_jobs import discovery` lazily inside the handlers so the API still starts if the module is absent (return 503 discovery_unavailable).
OWNED FILES (nothing else): src/gigai/scout/find_jobs/present_api.py (setup/discover routes only), src/gigai/scout/ui/src/**, src/gigai/scout/ui/dist/**, tests/behaviors/scout_find_jobs/test_present_setup_discover.py (new), .orchestrator/workers/s2b-setup-interview-ui.md. S2-A owns the discovery package, scout_cli.py and watchlist.py.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_present_setup_discover.py tests/behaviors/scout_find_jobs/test_present_ui.py tests/behaviors/scout_find_jobs/test_present_api_static.py -q` passes (prefs_missing → pre-fill; PUT validates + writes both files; POST discover 202/409/503; latest with days_ago; stubbed discovery). `make unit-tests` passes; yarn build deterministic. Describe the UI flow in the report. TEST BUDGET: exact files only.
EXCLUSIONS: no live provider calls; temp homes only; no git add/commit/stash/reset/clean. Say READ vs EXECUTED. Write .orchestrator/workers/s2b-setup-interview-ui.md, then worker_done.
