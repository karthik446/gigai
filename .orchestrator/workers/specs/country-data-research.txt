TARGET (country-data-research): RESEARCH ONLY, no code changes. The operator objects to hand-made lookup tables in src/gigai/scout/find_jobs/filters.py (shipped in 0.1.8.1 as a patch): `_COUNTRY_ALIASES` (:43), `_COUNTRY_ALIASES_EXTRA` (:136), `_CITY_COUNTRIES` (:108), plus the special cases (bare "ca"/"in"/"de" excluded because they collide with US state codes). Operator: "figure out if there are already libraries that define this or a json; we shouldn't make up shit."
INVESTIGATE, with evidence (versions, licenses, sizes, last release, offline or not):
1. What filters.py actually needs: read the whole file and its callers/tests (tests/behaviors/scout_find_jobs/test_filters.py, the acquire/assess code that calls location_countries/country_match/exclusion_reason). List each function's job: country names/codes → ISO alpha-2, city → country, US-state disambiguation, "Remote - US" style strings.
2. Structured data first: do the ATS APIs Scout already calls (Greenhouse, Lever, Ashby: see src/gigai/scout/find_jobs/*client*.py and the saved raw payloads/fixtures in tests) return a structured country/location field (e.g. Ashby address.postalAddress.addressCountry, Lever categories.location / country, Greenhouse offices[].location)? Does Exa? If a structured field exists, string-parsing may be unnecessary for those sources. Cite the code or fixture lines.
3. Libraries/data for country names and codes: pycountry (ISO 3166 from Debian iso-codes; license, and does it have fuzzy search?), iso3166, country_converter (license!), babel territory names, python-geonames/geonamescache (cities, license, size), others you find. For each: what it covers (alpha-2/3, common names, other languages, cities, US states), license compatibility with gigai's license (read LICENSE / pyproject), install size, pure-Python or not, offline.
4. US-state disambiguation: a library or dataset for US state names and codes (e.g. `us` package), so "CA"/"IN"/"DE" handling isn't hand-made.
5. Recommend a minimum set (label each option required / optional / alternative) and sketch how filters.py would use it (pseudocode, not a patch), including what hand-made entries would remain and why. Say whether a new dependency is needed and how big it is. Note the wheel-size and offline (Debian offline container) constraints.
OUTPUT (owned files, nothing else): .orchestrator/research/country-data.md, .orchestrator/workers/country-data-research.md.
EXCLUSIONS: no code, schema, test, pyproject or lock edits; no git add/commit/stash/reset/clean; no pip/uv install into the project env (read PyPI pages / docs, or install into a throwaway venv under your scratchpad only if you must inspect a package, and say so); no live Exa/ATS calls. Say READ vs EXECUTED for every claim. Then worker_done.
