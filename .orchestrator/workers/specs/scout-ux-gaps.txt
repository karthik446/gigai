TARGET (scout-ux-gaps): two usability gaps found by the installed-wheel acceptance (.orchestrator/workers/scout-run-acceptance.md "Findings" 1). Make the one-command flow actually one-command: setup → init → secrets add → resume add → run, from inside the target folder.
1. `gigai scout resume add <file>` must install/approve/activate Scout itself when it isn't yet (call the existing idempotent install_scout from src/gigai/scout/template.py, exactly as `gigai scout run` does in src/gigai/scout/run_supervisor.py / scout_cli.py), and say so in its output. Rerunning stays idempotent.
2. Inside a bound NON-GIT target folder (created by `gigai init --target <dir>`), commands without --target fail with GitTargetError even when the cwd IS the target. Read how the bound project is resolved when no --target is given (src/gigai/workpad.py resolve_bound_project and whatever it calls for the cwd; likely a git-root discovery). Make cwd resolution also find a bound non-git target: if the cwd (or an ancestor) is a registered non-git target in the registry, use it; git targets behave exactly as today. This is generic core behavior (not Scout-specific). If it needs a registry query that doesn't exist, add a small read-only helper; no schema or storage change (if one is needed, STOP and ask).
OWNED FILES (nothing else): src/gigai/scout/scout_cli.py, src/gigai/workpad.py (target resolution only), src/gigai/registry.py (a read-only lookup helper only, if needed), tests/behaviors/scout_find_jobs/test_scout_setup_cli.py, and the existing workpad/target-resolution test file(s) that cover resolve_bound_project (grep; name the exact files), .orchestrator/workers/scout-ux-gaps.md.
ACCEPTANCE: tests: resume add on a fresh bound target installs Scout and wraps the resume; from cwd = a bound non-git target (and a subfolder of it), `gigai scout install`, `resume add`, `run --no-browser`/`status`/`stop` work without --target; git-target resolution is unchanged (the existing tests pass); an unbound non-git cwd still gives the existing error. Run exact test files only (list them) + `make unit-tests`. Then EXECUTE the brief's flow as written from inside a temp non-git target (temp GIGAI_HOME, fake Exa key, no --target anywhere): setup → init --target . → secrets add exa --stdin → scout resume add r.md → scout run --no-browser → status → stop; paste the trimmed transcript.
EXCLUSIONS: no schema or storage migration; no live calls; temp homes only; no git add/commit/stash/reset/clean; never a whole test directory. Say READ vs EXECUTED. Write .orchestrator/workers/scout-ux-gaps.md, then worker_done.
