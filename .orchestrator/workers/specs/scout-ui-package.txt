TARGET (scout-ui-package): packet A of 4 for `gigai scout run` (U13). Ship the built Scout UI in the wheel and serve it from the API process.
CONTEXT: operator-approved brief .orchestrator/workers/specs/scout-run-command.brief.txt (read it) and the UAT log /Users/kar/orca/workspaces/gigai/v0.1.8-uat.md rows U6, U13, U14, U15, U23 (read-only). Goal of the whole feature: from a clean GIGAI_HOME + `gigai setup` + `gigai init --target <dir>`, `gigai scout run` reaches a UI that loads config and resume and can start a run, from the INSTALLED package: no Python helper, no manual TOML edit, no `record create`, no source checkout. It is a thin first slice of v0.2.0 roadmap Workstream 3 (D-C, `gigai <gig> run`). Operator decisions (2026-09-24): commit ui/dist + a PR CI freshness check; background run with stop/status (+ --foreground); a starter find-jobs.json + UI guidance on first run (no UI resume upload yet). Rules: uv only in docs and output (never pip); don't add new core→scout imports where a registration seam works, and if you must, list each as debt (the existing `cli.add_command(<scout group>)` block in cli.py is the accepted pattern for now).
FACTS (coordinator, READ): the UI is a Vite+React app at src/gigai/scout/ui (package.json "build": "vite build"); its .gitignore ignores dist/ and node_modules/. vite.config.js proxies /api to 127.0.0.1:8765 in dev; src/api.js calls relative /api/... paths, so same-origin serving works. The API is stdlib ThreadingHTTPServer in src/gigai/scout/find_jobs/present_api.py (do_GET ~:289; server built ~:448-461; loopback only). pyproject.toml:47 package-data for gigai.scout lists data/ui/*.html etc. but not the Vite dist. No workflow sets up Node today. Local: node v26, yarn 1.22.
CHANGE:
1. Build the UI (`yarn --frozen-lockfile install && yarn build` in src/gigai/scout/ui) and commit the output: un-ignore dist/ in src/gigai/scout/ui/.gitignore (keep node_modules ignored). Check the bundle is small and has no absolute dev URLs.
2. Package data: include the dist files in the wheel and sdist (pyproject.toml package-data for gigai.scout, e.g. "ui/dist/*", "ui/dist/assets/*"). Don't package ui/src, node_modules, package.json.
3. present_api.py: serve the packaged dist at "/" (index.html) and "/assets/*", with the right Content-Type, read via importlib.resources (works from a wheel); never serve outside dist (reject path traversal); unknown non-/api paths → index.html (SPA) or 404, your choice, say which. /api/* behavior unchanged. If dist is missing (source checkout without a build), "/" returns a clear 503 message naming `yarn build`, and the API still works.
4. PR CI freshness check in .github/workflows/pull_request.yaml: a small job (runs only when code_changed, same as the others) that sets up Node (actions/setup-node, the version from package.json engines or node 22 LTS), runs the same install+build, and fails if `git diff --exit-code src/gigai/scout/ui/dist` shows a difference. Keep job ids stable; name it e.g. "Scout UI bundle is up to date". Vite output must be deterministic for this; if hashed filenames change run to run, fix that or say why the check can't be exact.
5. Wheel resources: make the existing wheel checks see the UI (tools/release_check.py verify-artifacts or the installed verifiers; read how they check package data and add the dist files the same way).
OWNED FILES (nothing else): src/gigai/scout/ui/.gitignore, src/gigai/scout/ui/dist/** (generated), src/gigai/scout/find_jobs/present_api.py (static serving only), pyproject.toml ([tool.setuptools.package-data] only), .github/workflows/pull_request.yaml (the new job only), tools/release_check.py or the relevant tools/verify_installed_*.py (package-data checks only), tests/behaviors/scout_find_jobs/test_present_api_static.py (new), .orchestrator/workers/scout-ui-package.md.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_present_api_static.py -q` passes (index served, an asset served with the right type, traversal rejected, missing dist → 503, /api unaffected), plus the existing present_api test file(s) you touch (list them); `uv build` then list the wheel contents and paste the ui/dist entries; run the freshness check locally twice (build, git diff --exit-code) and paste the result; `make unit-tests` passes.
EXCLUSIONS: no git add/commit/stash/reset/clean/push; no live Exa/ATS/provider calls; no changes to /api behavior; focused tests only (never make test/test-wheel). Network only for yarn install and uv build dependency resolution. Say READ vs EXECUTED. Write .orchestrator/workers/scout-ui-package.md, then worker_done.
