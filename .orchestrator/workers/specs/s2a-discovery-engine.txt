TARGET (s2a-discovery-engine): S23 stage 2, packet A: the weekly "Discover companies" ENGINE (backend only). Operator decisions: OpenAI web_search + the H-1B (DOL LCA) data together (S24 recommendation); 3 OpenAI runs per session merged; interview answers drive the query, filters and exclusions; new boards merge into the Scout watchlist with sponsorship evidence + source; record cost per session. READ: docs/development/v0.1.9/spikes/S23-scout-setup-interview-exa-agent.md (§2 questions, §3 query template, §4 schema, Recommendation incl. "input.exclusion objects", "verify every source URL") and S24-company-discovery-bakeoff.md (OpenAI setup, model gpt-6-luna, TPM backoff, H-1B pipeline + the startup/mid-size re-rank suggestion), and research/discovery_bakeoff/{run_openai_search.py,h1b_baseline.py,probe_h1b_boards.py,check_boards.py} + research/exa_agent_spike/check_boards.py. Productionise these into src/; don't import from research/.
INTERFACE CONTRACT between packets S2-A (engine) and S2-B (UI/API); both must match it exactly:
- Module src/gigai/scout/find_jobs/discovery/__init__.py exports:
  run_discovery(*, home_root: Path, target: Path, prefs: DiscoveryPrefs, runs: int = 3, on_progress: Callable[[dict], None] | None = None) -> DiscoveryResult   (synchronous; may take ~5-30 min; never raises for provider errors, records them)
  latest_discovery(*, home_root: Path, target: Path) -> DiscoveryResult | None
  load_prefs(*, home_root: Path, target: Path) -> DiscoveryPrefs | None ; save_prefs(*, home_root: Path, target: Path, prefs: DiscoveryPrefs) -> None
- DiscoveryPrefs (frozen dataclass, to_json/from_json): the 11 S23 interview answers: roles (tuple[str]), titles_to_avoid, countries (ISO alpha-2), work_mode ("remote"|"hybrid"|"onsite"|"any"), city (str|None), visa_sponsorship_required (bool), exclude_companies, watch_companies, company_stage_size (str|None), industries_include, industries_exclude, must_have_stack, dealbreaker_stack, cadence_days (int, default 7), budget_usd_per_session (float, default 0.50).
- DiscoveryResult (to_json): discovery_id, status ("running"|"succeeded"|"failed"|"partial"), started_at, finished_at, cost_usd, sources: [{name: "openai_web_search"|"h1b", runs, cost_usd, error|null}], new_boards: [{company, provider, board_token, careers_url, sponsorship: "yes"|"no"|"unknown", sponsorship_evidence, evidence_source_url, evidence_verified: bool, found_by: [..], matching_us_postings: int}], skipped: {reason: count}.
- Stored under the gigai home (per project), not in the target: <home>/scout/<project_id>/discovery/prefs.json and discovery/runs/<discovery_id>.json.
CHANGE (all under src/gigai/scout/find_jobs/discovery/, a Scout-only package; core never imports it):
1. prefs.py: DiscoveryPrefs + load/save (atomic write, 0600 not needed; not secret).
2. openai_source.py: the OpenAI Responses API web_search with strict structured output (the S23/S24 schema minus board_token; board_token is derived from careers_url with the existing board-URL parser), the query built from prefs (S23 §3 template), exclusions = the Scout watchlist + prefs.exclude_companies + companies found in earlier discovery runs. Key via gigai.secrets_store / the secrets catalog ("openai"); a missing key → the source is recorded as skipped with the fix ("gigai secrets add openai"), never raises. Retry with backoff on 429/TPM (bounded), per-call timeout, cost read from the usage block (price table in one place, documented). The model is configurable (default from S24) with an env/pref override. httpx only (as exa_client.py); no OpenAI SDK unless justified.
3. h1b_source.py: download the latest DOL OFLC LCA disclosure file once into <home>/cache/scout/h1b/ (show size before downloading; refresh only when a newer fiscal quarter exists; never into the target or the repo), derive a compact sponsor index (employer, case count, SOC/job titles relevant to prefs.roles, worksite states, NAICS if present) cached as a small JSON/SQLite, rank toward likely tech startups/mid-size (filter staffing/outsourcing-firm patterns + NAICS, per S24), and probe Greenhouse/Ashby/Lever slugs for the top N (default 200, bounded concurrency, polite rate) with evidence = the DOL case count + the source file URL.
4. merge.py: union both sources, dedupe by (provider, board_token) and normalized company, drop exclusions, board-check each (the existing ats_board_clients: ≥1 posting matching prefs.roles in prefs.countries), verify every sponsorship evidence URL resolves (HEAD/GET, timeout) → evidence_verified, then add usable boards to the watchlist (watchlist.add_to_watchlist with the evidence + source recorded; read watchlist.py for its entry shape; additive fields only, and if WatchlistEntry needs a new field that is a contract change, STOP and ask).
5. run_discovery / latest_discovery per the contract, with a budget guard: never start a session whose worst case exceeds prefs.budget_usd_per_session; record the real cost.
6. CLI: `gigai scout discover [--runs N] [--json]` in src/gigai/scout/scout_cli.py (it runs in the foreground and prints a summary: new boards, cost, where stored) and `gigai scout discover --status` (the last run, "N days ago").
OWNED FILES (nothing else): src/gigai/scout/find_jobs/discovery/** (new), src/gigai/scout/find_jobs/watchlist.py (only if an additive helper is needed), src/gigai/scout/scout_cli.py (the discover command only), src/gigai/secrets_catalog.py (only if the openai entry needs adjusting), tests/behaviors/scout_find_jobs/test_discovery_*.py (new), tests/behaviors/scout_find_jobs/fixtures/discovery/** (copy the needed REDACTED S23/S24 fixtures + a tiny synthetic LCA sample; never the 240MB file), .orchestrator/workers/s2a-discovery-engine.md. Another worker (S2-B) owns present_api.py and ui/ and codes against the contract above.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_discovery_prefs.py tests/behaviors/scout_find_jobs/test_discovery_openai.py tests/behaviors/scout_find_jobs/test_discovery_h1b.py tests/behaviors/scout_find_jobs/test_discovery_merge.py -q` passes with NO live calls (MockTransport + fixtures): the query built from prefs, exclusions as objects, schema parsing, 429 backoff, a missing key → skipped, H-1B derive/rank/probe on the synthetic sample, merge/dedupe/verify, the watchlist gets exactly the usable boards with evidence, the budget guard refuses, latest_discovery round trip. `make unit-tests` passes. Then ONE live smoke run is authorized: `gigai scout discover --runs 1` in a TEMP GIGAI_HOME/target (copy nothing from the operator's home; the keys come from the operator's secrets via secrets_store, so run with GIGAI_HOME pointing at the temp home but pass the keys through the ENVIRONMENT from secrets_store.get in a small wrapper, never printed) with a hard cap of $0.50, and the H-1B probe limited to 50 employers; paste the summary. TEST BUDGET: exact files only.
EXCLUSIONS: no schema/storage migration of existing records; never print keys; no commits (the coordinator commits); temp homes only; the LCA raw file never enters git. Say READ vs EXECUTED. Write .orchestrator/workers/s2a-discovery-engine.md, then worker_done.
