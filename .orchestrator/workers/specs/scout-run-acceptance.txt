TARGET (scout-run-acceptance): packet D part 2: prove the `gigai scout run` feature from an INSTALLED WHEEL (not the source tree), and finish the README config example. This is the acceptance the operator-approved brief set (.orchestrator/workers/specs/scout-run-command.brief.txt, ACCEPTANCE): "from a clean ~/.gigai + gigai setup + gigai init --target <dir>, gigai scout run reaches a UI that loads config and resume and can start a run, with no Python helper, no manual TOML edit, no record create, no source checkout. Proven on an installed wheel."
PART 1, the installed-wheel proof (EXECUTE; a visible tab for anything over ~30s: `.claude/skills/gigai-orchestrator/run_visible.sh "TEST scout wheel" <cmd>`):
- `uv build` → a wheel in a temp dir. Install it as a tool into an ISOLATED location (UV_TOOL_DIR and UV_TOOL_BIN_DIR pointing at temp dirs; `uv tool install <wheel>`), and run every command via that installed `gigai` with the working directory set to a temp dir OUTSIDE the repo, so nothing imports from the source checkout. Confirm `python -c "import gigai; print(gigai.__file__)"` via the tool env points into the tool dir.
- With a temp GIGAI_HOME and a temp non-git target: `gigai setup --non-interactive ...` (as release.yml's smoke step) → `gigai init --target` → `gigai secrets add exa --stdin` (a FAKE value like "test-not-a-real-key") → `gigai scout resume add <small .md>` → `gigai scout run --no-browser --port <free port>` → curl: GET / (UI html + an asset from /assets/ with the right content type), GET /api/config (the starter config), GET /api/runs/does-not-exist (the not-found shape), GET /api/runs/{fake}/progress (the not-found shape) → `gigai scout status` → `gigai scout stop` → the pid is gone and the port is free.
- "Can start a run", WITHOUT live network: edit the temp find-jobs.json to disable every live source (read the config schema / FindJobsConfig for the right fields) and POST the run the way the UI does (read src/gigai/scout/ui/src/api.js for the request shape, incl. the consent fields). Expect the run to be accepted and to finish with a clean, recorded outcome (e.g. the B5 all-sources-failed/no-sources result) visible via /api/runs/{id} and /progress. If the API refuses a run with no sources, report exactly what it says; don't add product code to force it. NO live Exa/ATS/provider calls at any point (the Exa key is fake on purpose).
- Kill everything you started; paste a trimmed transcript.
PART 2, README (README.md only): the "Scout: install and run" example find-jobs.json omits the country and visa-sponsorship fields that acquire now filters on (commits 80a2e98, 8856b1d). Add them with the exact field names from FindJobsConfig (src/gigai/scout/find_jobs/contracts.py), one line each on what they do: countries = ISO codes; region-only locations like AMER/EMEA never match; visa sponsorship required → "not offered" postings are excluded. Also mention per-company diversity (at most 2 per company assessed) in one sentence, and that results appear as cards while the run progresses. Keep it short. uv only.
OWNED FILES (nothing else): README.md, .orchestrator/workers/scout-run-acceptance.md. No product code, no tests.
ACCEPTANCE: the transcript shows every step passing from the installed wheel (import path in the tool dir), no orphan process, and the README example validates against the config schema (EXECUTE: load it through FindJobsConfig's own validator from the installed package and paste the result).
EXCLUSIONS: temp homes and temp tool dirs only (never the operator's ~/.gigai or their global uv tools); no live network calls except uv resolving build dependencies; no git add/commit/stash/reset/clean. If something fails, report it with the exact error; don't patch product code. Say READ vs EXECUTED. Write .orchestrator/workers/scout-run-acceptance.md, then worker_done.
