TARGET (acquire-country-filter-r1): coordinator review of your packet. Accepted: structured Lever/Ashby countries via pycountry, the additive PostingRow.countries (omitted at None, so digests are unchanged), DropCount on AcquireOutput, the B3 root cause (html_to_text double-escaped Greenhouse content) + phrase, the B5 invariant, the pycountry tables with the 3 labelled policy lists, pycountry>=24.
ONE FIX (the operator's explicit B1 rule, brief scout-uat-0181-bugs.brief.txt: "Region tokens (AMER, EMEA, APAC, LATAM, 'Remote - Americas') must not count as US"): today a region-only location returns country_match → None (ambiguous), and exclusion_reason never excludes ambiguous, so "AMER" rows still pass a US-only filter, which is the exact UAT complaint. Coordinator probe (EXECUTED): location_countries('AMER') == [] and country_match('AMER', ('US',)) is None; same for 'Remote - Americas' and 'EMEA'.
CHANGE: when config.countries is set and a posting's location resolves to ONLY region tokens (no country, no US state, no known city, and no structured countries), treat it as a definite non-match: exclusion_reason returns the location-mismatch reason and acquire drops it, counted under its own DropCount key (e.g. "region_only") so it's auditable. A location that has a region token AND a matching country (e.g. "AMER; Denver, CO" or "Remote - US") still matches. A bare unrecognized string with no region token (e.g. an unknown city) stays ambiguous → kept, as today. Update the docstrings, add tests for each case, and extend the replay test to assert no AMER/EMEA/APAC-only rows survive; paste the list of surviving replay rows (company | location | countries).
OWNED FILES: same as your packet (filters.py, market_acquisition.py, contracts.py only if a DropCount key needs it, the 4 test files, fixtures), plus your worker file (append r1).
ACCEPTANCE: the same 4 focused test files + test_m1_end_to_end.py pass; make unit-tests passes. Test budget reminder: exact files only, never the whole scout_find_jobs directory.
EXCLUSIONS: as before (no live calls, no git add/commit/stash/reset/clean). Say READ vs EXECUTED. Then worker_done.
