TARGET (readme-uv-only): operator rule: "never pip, uv only". Remove every pip install instruction from USER-FACING docs and replace it with the uv equivalent. Relayed by the v0.1.8-worktree session (msg_abd3c5724176).
KNOWN HITS (coordinator, EXECUTED grep): README.md:36 `pip install gigai        # or: uv tool install gigai`; README.md:203 "Works from an installed package (`pip install gigai` / `uv tool install ...`".
CHANGE:
1. README.md: make uv the only install path (e.g. `uv tool install gigai`; upgrading: `uv tool upgrade gigai`; a pinned version: `uv tool install gigai==X.Y.Z`). Check each command against `uv tool --help` / `uv tool install --help`; don't guess flags. Keep the surrounding text accurate.
2. Grep the other user-facing surfaces for pip instructions and fix the doc ones: docs/development/v0.1.8/runbooks/*.md and any other runbook, CHANGELOG.md's Unreleased section only (never rewrite released sections), and the Scout UI README if one exists. Report, but do NOT edit: historical docs (docs/research/, docs/adr/, docs/architecture/, docs/development/**/spikes|evidence|roadmaps), and any user-visible string in src/ that says pip (list file:line; the coordinator routes those separately, since src/gigai/cli.py is owned by another worker right now).
OWNED FILES (nothing else): README.md, docs/development/v0.1.8/runbooks/*.md, CHANGELOG.md (Unreleased section only), src/gigai/scout/ui/README.md if it exists, .orchestrator/workers/readme-uv-only.md.
ACCEPTANCE: `grep -rniE '\bpip3? install' README.md docs/development/v0.1.8/runbooks` returns nothing; paste the before/after of each changed line; list every reported-not-edited hit with file:line.
EXCLUSIONS: docs only; no code; no git add/commit/stash/reset/clean; no installs. Say READ vs EXECUTED. Write .orchestrator/workers/readme-uv-only.md, then worker_done.
