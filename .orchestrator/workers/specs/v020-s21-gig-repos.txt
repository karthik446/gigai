ORIGIN: handed off by the v0.1.8 coordinator (run_b12de8fdda28) on 2026-09-23 at the operator's request ("it needs to be stored in 1.9 worktree"). The evidence line numbers came from gigai-v0.1.8 @ de9b0d6. The cited files are byte-identical in gigai-v0.1.9 @ aa9d06a (EXECUTED cmp; only pyproject.toml differs, in testpaths, and the :30-31/:46 citations still hold). Dispatch in THIS worktree under run_7cba834cb817; suggested route Sonnet 5 (doc/spike writing).

TARGET (v020-s21-gig-repos): write the v0.2.0 spike S21 "Gigs in their own repos; gigai as a facilitation library" — a DISCUSSION ticket that records the operator's decisions below, lays out what they imply for today's code (with file:line evidence), and collects the decisions still open. Not a plan, not authorized implementation. Not a v0.1.8/0.1.8.1 blocker.

OPERATOR DECISIONS (2026-09-23, from chat; record verbatim-in-intent, do NOT re-open them):
1. Scout moves to its own repo; any gig can live in its own repo. "each gig can be on it's own repo.. gigai facilitates all the tracking, db tracking, run tracking on local.. we could also strip out pii by default later on"
2. Gig domain data lives in the gig's own store, not gigai's: "they should live in scouts own.. scout runs it's own gigai just facilitates.. like a cli.. again scout is our gig"
3. Python only; no polyglot protocol/SDKs: "we can stick to python tbh it's fine.. not any stack"
4. PII stripping by default is LATER (not 0.2.0 scope unless the operator says otherwise).
Consequence to state: gigai = a Python library + `gigai` CLI that a gig depends on (e.g. `gigai>=0.2,<0.3`); core never imports a gig (existing rule, docs/development/v0.2.0/README.md "The gig architecture rule"). This keeps S20's library framing and adds: separate distributions/repos, and gig-owned data.

EVIDENCE ALREADY GATHERED by the coordinator (re-verify each before citing; these are leads, marked EXECUTED = command run, READ = file read):
F1 Scout's domain data is currently AUTHORITATIVE IN GIGAI'S WORKPAD JOURNAL, so decision 2 reverses authority, not just location.
  - src/gigai/scout/projection.py:1-8 docstring: "The private Git journal and committed record artifacts are authority; state.sqlite is only a disposable cache".
  - Core module src/gigai/private_records.py writes Scout records/imports/operations via the journal (_publish :246, import_reference :307, import_run_input :339, create_record :431) and rebuilds Scout projection tables scout_records/scout_operations/scout_meta in the workpad's state.sqlite (rebuild_scout_projection :524, CREATE TABLE at :554). It validates scout-operation-receipt.schema.json (~:540). I.e. a core module that is Scout-specific in practice.
  - state.sqlite is SHARED between Scout projections and core's G22 append-only interview trace: src/gigai/lifecycle.py:3003-3011 (_persist_interview_trace), src/gigai/index.py:213-216 docstring, src/gigai/proposal_interview.py:578 (CREATE TABLE interview_events). Splitting stores must not lose that trace.
  - scout/projection.py:333-340 in-memory tables: opportunities, proposals, questions, documents, evidence, applications, runs, scout_cursor.
F2 Core HOSTS Scout's execution today (so "Scout runs itself" is a real change):
  - src/gigai/run.py:913 launch_find_jobs_run ("Seal and launch one local find-jobs Run from the API boundary"), invocation_argv ("gigai","find-jobs","run") at ~:964; find-jobs specifics also at run.py:156, :337, :371, :493, :2580-2656, :4069.
  - run.py:53 imports graph_node_registry.lookup; Scout registers nodes in src/gigai/scout/find_jobs/bindings.py:441-450; bindings.py:1-6 docstring: launch_run starts the scheduler in a spawned child and a worker wrapper re-registers nodes there.
  - Scout's UI API calls back into core run: src/gigai/scout/find_jobs/present_api.py:24,154,169,205.
F3 The workpad is already per (project, gig): src/gigai/workpad.py:90-95 ResolvedWorkpad(project_id, gig_id, path, ...), provision_workpad :114. But its layout v2 roots are core-defined (private_records.py:136-139: records, references, run-inputs, indexes, ...) and src/gigai/package_privacy.py:27 hard-codes "reports/scout/", :30 "state.sqlite".
F4 Redaction today is explicit value-list redaction of model-provider input, not default PII detection: src/gigai/model_execution.py:46 DEFAULT_REDACTION_POLICY, :78 redaction_values, :233-236 redact_text + required_sensitive_values fail-closed. package_privacy.py:1-6 says it is "a narrow provenance gate, not a content classifier". So PII-by-default is new work; its natural chokepoint is gigai-owned records (runs, events, model-call logs, exports), and Scout's own store is Scout's responsibility.
F5 Packaging is one distribution: pyproject.toml:30-31 single script `gigai = "gigai.cli:cli"`; :46 package-data for gigai.scout (data md, goalgraphs, ui html/css, tools json). src/gigai/schemas has 82 *.json (EXECUTED ls|wc); a NAME grep matched 13 scout-*.schema.json + application-event.schema.json — name-grep only, schemas Scout uses under generic names NOT traced (trace by field name before claiming). Scout UI is a Vite app in src/gigai/scout/ui (package.json, yarn.lock, node_modules, dist).
F6 Coupling: EXECUTED grep for `from .scout|from gigai.scout` in src/gigai/*.py → 36 lines across 10 core files: run.py, portability.py, native_records.py, capability_successor.py, private_transfer.py, cli.py, default_init.py, capability_review.py, external_recording.py, application_events.py. S16 (docs/development/v0.1.9/spikes/S16-core-gig-decoupling.md) counted 45 strict-pass sites by AST. Reconcile the two counts (method difference) — do not just pick one. external_recording.py:1 docstring is "Journal-backed recording of externally performed Scout work" (imports scout.inputs :37, scout.tailoring :43).
F7 Size: `wc -l src/gigai/*.py` total 46,264 (top-level core only); src/gigai/scout/**/*.py total 20,384. Tests: 159 test_*.py files; 54 test files reference gigai.scout (EXECUTED grep -rl). run.py 5,067 lines, cli.py 4,054.

WHAT S21 MUST COVER (Investigate sections, each with evidence):
1. The line between gigai and a gig under these decisions: what gigai provides (run tracking incl. stale/crashed-run reconciliation since the gig now owns its process; journal as a library mechanism; model calls with recording + redaction; credentials/config; per-gig directory provisioning; diagnostics; cross-gig CLI like runs/doctor/export) vs what a gig owns (graph + nodes, domain schemas, domain store, prompts, adapters, UI, its own CLI). Label each item required / optional / alternative.
2. Data ownership migration: the F1 authority reversal; what moves out of core (private_records.py's Scout parts, application_events.py, external_recording.py, Scout projections), splitting state.sqlite without losing the G22 interview trace, correlation IDs (gig records store the gigai run_id), and what happens to operators' EXISTING workpads (migrate vs fresh start — open question).
3. Execution ownership (F2): alternatives, NOT decided: (A) Scout's CLI calls gigai's generic graph runner as a library (run.py loses all find-jobs specifics); (B) Scout owns execution and only reports run lifecycle/events to gigai. Give the coordinator's lean (A: sealing/worker/effects/budget are the bulk of "run tracking" and every gig would otherwise rewrite it) as a lean, with its cost.
4. Packaging and repos: separate distributions; entry-point group (e.g. `gigai.gigs`) for discovery; `scout` as its own console script vs `gigai scout …` plugin dispatch (lean: own script); schemas and UI move with Scout; core validators must accept gig-supplied schemas (see S16 validators.py:69,101-116 references); a tiny example gig kept IN the gigai repo so core CI tests the gig contract without Scout; Scout CI pins a gigai range and optionally tests against gigai main.
5. Sequencing options (for discussion): (i) decouple in-repo first (S16) then extract; (ii) interim monorepo with two distributions (e.g. gigs/scout/ with its own pyproject) then extract; (iii) extract now. Give tradeoffs; note memory-level constraint that v0.1.8.x = Scout working ASAP, so nothing here disrupts 0.1.8.x.
6. PII-by-default (later): what the chokepoint is under decision 2 and what 0.2.0 must preserve so it stays possible (all model calls go through gigai — convention in a library model, not enforceable; say so).
7. Relationship to S16/S17/S20: what S21 keeps, amends, or supersedes (e.g. S16's GigManifest/registry still applies to node registration under option A; S20's public-API + semver work becomes the actual contract Scout pins against). Don't edit those docs; state the relationship in S21.

OPEN QUESTIONS section (one place, numbered): at minimum — execution A vs B; Scout's store = plain SQLite as authority vs gigai's journal library in a Scout-owned dir; where Scout's data lives physically (gigai-provisioned per-gig dir vs anywhere); CLI shape; UI (moves wholly to Scout? generic gigai run viewer later?); existing-workpad migration; must gigs route model calls through gigai; sequencing (i/ii/iii); repo name and distribution name for Scout.

OWNED FILES (nothing else): docs/development/v0.2.0/spikes/S21-gig-repos-and-data-ownership.md (new, ~250–400 lines); docs/development/v0.2.0/spikes/README.md (add one S21 index bullet in the existing style); .orchestrator/workers/v020-s21-gig-repos.md (worker file).

FORMAT: per docs/development/v0.2.0/spikes/README.md "Ticket format": Status / Problem / Operator decisions (recorded) / Intended change (proposed, not decided) / Tasks / Acceptance / Investigate / Open questions / Non-claims / Change log. Read S20 first and match its tone and evidence style. READ vs EXECUTED stated for every claim; every claim cites file:line or a command actually run. Evidence discipline: before any "unused"/"only X"/"all necessary"/count claim, trace by field name and recount from rows; label candidate changes required/optional/alternative and give the minimum set.

ACCEPTANCE: (a) the S21 file exists with all sections above; (b) every file:line cited resolves (spot-check with sed -n); (c) counts reconciled (F6 36 vs 45 explained); (d) no text phrases an open question as decided, and the 4 operator decisions are not re-opened; (e) `git status --short` shows only the three owned files changed/added.

EXCLUSIONS: docs only — no code, schema, test, or storage edits; no git add/commit/stash/reset/clean; no live/provider runs; no tests needed (grep/wc/AST scripts are fine). Write .orchestrator/workers/v020-s21-gig-repos.md (state, files, what you READ vs EXECUTED, open questions) on finish or on any question, then send worker_done.
