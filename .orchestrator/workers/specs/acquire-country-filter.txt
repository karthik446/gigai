TARGET (acquire-country-filter): fix the 0.1.8.1 live-UAT tickets B1 (filter at acquire), B3 (sponsorship 69/69 unknown) and B5 part (a run where every source fails shows "succeeded"), together with the operator-approved country fix: use the structured country fields Lever and Ashby already return, and replace the hand-typed country/US-state alias tables with pycountry. Keep the 45-city list (operator: keep for now, relabelled as a deliberate short list).
READ FIRST: .orchestrator/workers/specs/scout-uat-0181-bugs.brief.txt (B1, B3, B5) and .orchestrator/research/country-data.md (§2 structured fields with doc quotes; §5 recommendation + sketch). Evidence run (read-only, the operator's own data): ~/.gigai/workpads/projects/project_e12ab112-*/gigs/gig_015b1fc0-*/runs/run_d73cb030-62a3-4d5e-9629-a33977d90194 (outputs/acquire.json, raw/{exa,greenhouse,ashby}). You MAY read these files for real payload shapes and posting text; copy only small, necessary snippets into test fixtures, and never copy anything from the operator's resume or credentials.
CHANGE:
1. Structured country at fetch (ats_board_clients.py): Lever `country` (ISO alpha-2, may be null) + `categories.allLocations`; Ashby `address.postalAddress.addressCountry` + `secondaryLocations[].address.postalAddress.addressCountry` (+ `isRemote`/`workplaceType` if useful). Normalize to ISO alpha-2 with pycountry (Ashby may send "USA"/"United States"; confirm against the raw ashby payload in the evidence run and say what format it actually uses). Greenhouse: free text only (location.name).
2. PostingRow (contracts.py): an ADDITIVE optional field, e.g. `countries: tuple[str, ...] | None = None` (ISO alpha-2, from structured data when present). Existing serialized rows without it must still read. If a JSON schema file validates posting rows or acquire outputs, add the optional property there too; if that requires a schema version bump or migration, STOP and ask.
3. filters.py: replace `_COUNTRY_ALIASES`/`_COUNTRY_ALIASES_EXTRA` and the US-state tables with pycountry data (countries: alpha-2/alpha-3/name/common_name/official_name; US states: pycountry.subdivisions for US). Keep, as small explicitly labelled policy lists: the UK constituent-country fold, the CA/IN/DE-as-US-state disambiguation, and the 45 cities. Region tokens (AMER, AMERICAS, EMEA, APAC, LATAM, "Remote - Americas", worldwide/global) must NOT count as matching a single country like US; decide and document the multi-location policy (brief: "Mountain View, USA; Seattle, USA" matches US; "Seoul, South Korea; z-Test & Templates Only" doesn't). country_match(): structured `countries` first; fall back to parsing only when absent.
4. B1, filter at ACQUIRE: in market_acquisition.py, apply the country/location/remote (and existing role) filters right after fetch; drop non-matching rows from outputs/acquire.json results (raw/ keeps everything for audit/replay; record per-reason drop counts in the acquire output or receipt, additively). Read how market_acquisition.py:~472 uses exclusion_reason today and how proposal_execution.py:~141-178 relies on it; keep assess-time exclusion working for old runs.
5. B3, sponsorship: find why 69/69 are unknown (is sponsorship_from_text called on the text actually present? do the phrases match real postings?). Test against real posting texts from the evidence run. Cover at least "will not sponsor", "unable to sponsor", "must be authorized to work ... without (visa) sponsorship", "sponsorship (is) (not) available", "visa sponsorship available", "we sponsor". Check that visa_sponsorship_required=true actually excludes "not offered".
6. B5 part: in acquire_node, if EVERY source failed (0 postings and ≥1 source failure), the node must not report success; use the existing failure/receipt mechanism (read how failures are recorded; no new status values without asking).
7. Dependency: `uv add "pycountry>=24"` (pyproject.toml + uv.lock).
OWNED FILES (nothing else): src/gigai/scout/find_jobs/filters.py, ats_board_clients.py, contracts.py (PostingRow only), market_acquisition.py; src/gigai/schemas/<posting/acquire schema> only if step 2 requires it; pyproject.toml (dependencies only) + uv.lock; tests/behaviors/scout_find_jobs/test_filters.py, test_ats_board_clients.py, test_acquire_network.py, test_contracts.py, fixtures/ (new small fixtures); .orchestrator/workers/acquire-country-filter.md. Other workers own exa_client.py, proposal_execution.py, cli.py, workpad.py, scout_cli.py, present_api.py and ui/; don't touch them. Keep sponsorship_from_text's signature (exa_client.py imports it).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_filters.py tests/behaviors/scout_find_jobs/test_ats_board_clients.py tests/behaviors/scout_find_jobs/test_acquire_network.py tests/behaviors/scout_find_jobs/test_contracts.py -q` passes, plus the tests that use exclusion_reason/proposal_execution selection (grep; list the exact files) and tests/behaviors/scout_find_jobs/test_m1_end_to_end.py (offline). Include a replay test: feed the evidence run's raw payloads (as small fixtures) through acquire with countries=["US"] → no Bengaluru/Hyderabad/Seoul/Singapore/Australia/Canada/AMER-only rows; paste before/after counts. `make unit-tests` passes. Report the wheel-size delta from pycountry.
EXCLUSIONS: no storage migration; no live Exa/ATS/provider calls (MockTransport + fixtures only); never read ~/.gigai/.env or the resume; no git add/commit/stash/reset/clean/push; focused tests only (the selectors above; never make test). Say READ vs EXECUTED. Write .orchestrator/workers/acquire-country-filter.md, then worker_done.
