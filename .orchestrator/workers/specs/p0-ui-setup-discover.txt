TARGET (p0-ui-setup-discover): PR #37 review P0-2 (CONFIRMED by trace) and P0-5 (PLAUSIBLE).
P0-2: src/gigai/scout/ui/src/App.jsx handleSaveSetup (~:140-147) reloads only setupState after PUT /api/setup, but PUT rewrites find-jobs.json, so the UI keeps the OLD config_digest (App.jsx ~:271 sends configResponse.config_digest) and the next Run gets 409 config_digest_mismatch (present_api.py ~:1200). Fix: after a successful setup save, reload the config too (and anything derived from it), so Run uses the new digest. Also make a 409 config_digest_mismatch in the UI reload the config and tell the user to confirm again, instead of a dead end.
P0-5: present_api.py ~:752 _capture_progress sets self._discovery_progress = the raw progress EVENT, overwriting the DiscoveryResult-shaped snapshot, so GET /api/discover/latest mid-run lacks cost_usd/new_boards/sources and the panel shows blank or "Cost $undefined". Reproduce with a test (a stub discovery that emits progress events), then keep the latest full snapshot shape (status "running", discovery_id, started_at, cost_usd so far or null, sources/new_boards so far) with the progress event in a nested field; make DiscoverPanel robust to null/missing fields (never render "undefined").
Review file: .orchestrator/reviews/pr37-review-findings.md. REPRODUCE FIRST with a failing test, then fix, then show it passing. Ask questions ONLY via `orca orchestration ask`, never a local prompt. TEST BUDGET: exact test files only (list them), plus `make unit-tests`; never a whole directory. No git add/commit/stash/reset/clean. Say READ vs EXECUTED.
OWNED FILES: src/gigai/scout/ui/src/** (App.jsx, api.js, DiscoverPanel.jsx and related), src/gigai/scout/ui/dist/** (rebuild; deterministic), src/gigai/scout/find_jobs/present_api.py (the discover snapshot only), tests/behaviors/scout_find_jobs/test_present_setup_discover.py, .orchestrator/workers/p0-ui-setup-discover.md.
ACCEPTANCE: tests: setup PUT then GET /api/config returns the new digest and a run POST with it succeeds (202); mid-run /api/discover/latest has the contract fields; the UI logic reloads config after save (unit-test the helper, or describe the manual check). yarn build twice → git diff --exit-code on dist.
