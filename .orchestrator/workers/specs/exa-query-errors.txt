TARGET (exa-query-errors): 0.1.8.1 live-UAT tickets B1 (Exa part) and B5 (Exa part). Read .orchestrator/workers/specs/scout-uat-0181-bugs.brief.txt and the addendum at the end of .orchestrator/workers/specs/scout-run-command.brief.txt (hidden Exa 402 / missing key).
CHANGE (src/gigai/scout/find_jobs/exa_client.py only):
1. Query shaping: pass the run config's location/country and a published-after date to Exa so fewer irrelevant boards come back. Use Exa's documented search parameters (read the Exa search API reference: e.g. startPublishedDate, and whatever location/user-location or query-text approach the docs support; don't invent params). Map from the sealed FindJobsConfig fields (read contracts.py; don't change it). Say which config field feeds which param and what happens when a field is absent.
2. Errors: an Exa HTTP error must surface its status and a short, redacted reason in ExaClientError (e.g. exa_http_error with status 402 → "Exa returned 402 (payment required / out of credits)"); keep exa_missing_key's message (it names `gigai secrets add exa`). Never include the API key or the request headers in any message or log.
OWNED FILES (nothing else): src/gigai/scout/find_jobs/exa_client.py, tests/behaviors/scout_find_jobs/test_exa_client.py, .orchestrator/workers/exa-query-errors.md. Another worker is changing filters.py, ats_board_clients.py, contracts.py and market_acquisition.py right now: import from them read-only and keep calls to sponsorship_from_text as they are.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_exa_client.py -q` passes with new MockTransport cases: the request body carries the date/location params built from config; absent config fields omit them; 402/429/500 → exa_http_error with the status in the message and no key; the existing tests still pass. `make unit-tests` passes. Paste the doc URL(s) and the exact parameter names you used.
EXCLUSIONS: no live Exa calls (docs pages only); never read ~/.gigai/.env; no git add/commit/stash/reset/clean; focused tests only. Say READ vs EXECUTED. Write .orchestrator/workers/exa-query-errors.md, then worker_done.
