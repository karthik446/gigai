TARGET (selection-diversity-u2): 0.1.8.1 live-UAT ticket B2 (selection quality) and the still-open UAT U2 (find-jobs resolves the literal model-target name "codex_cli"). Both live in src/gigai/scout/proposal_execution.py. Read .orchestrator/workers/specs/scout-uat-0181-bugs.brief.txt (B2) and the addendum in .orchestrator/workers/specs/scout-run-command.brief.txt (U2: setup names targets codex-default/claude-default, find-jobs resolves "codex_cli", proposal_execution.py:117-125; workaround `gigai setup --non-interactive --model-target "codex_cli=codex:default"`).
CHANGE:
1. B2: in the assess candidate selection (proposal_execution.py ~:141-178; not_assessed reasons by priority exclusion_reason → over_cap → ...): (a) dedupe same company + normalized title (case/whitespace/punctuation-insensitive; same-country locations) before capping, keeping the newest; (b) cap per company (default 2, from config if a field exists; otherwise a module constant, don't add a config field without asking); (c) fill the run cap round-robin across companies so one board can't take every slot. Dropped rows get a recorded not_assessed reason (reuse existing reasons, or add one additively, e.g. duplicate / company_cap, if the reason enum allows; if it's a sealed contract/schema change, ask). The UAT evidence: 5/5 picks were ClickHouse, 3 with the same title.
2. U2: the sealed enum value (ollama_local | codex_cli | openrouter_api) must resolve to the target that `gigai setup` actually creates (e.g. codex-default for codex_cli) without the operator renaming targets. Read resolve_model_adapter and setup's default target names; map through the existing config (the configured target of that adapter kind), not a new hard-coded string list, and fail with a message naming the fix if nothing matches. No silent fallback to another provider (existing rule).
OWNED FILES (nothing else): src/gigai/scout/proposal_execution.py, tests/behaviors/scout_find_jobs/test_assess_contract.py, test_assess_model_policy.py, test_run_seam.py (only if needed), a new tests/behaviors/scout_find_jobs/test_selection_diversity.py, .orchestrator/workers/selection-diversity-u2.md. Other workers own filters.py, ats_board_clients.py, contracts.py, market_acquisition.py, exa_client.py, cli.py, workpad.py, scout_cli.py, present_api.py, ui/; read them only.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_selection_diversity.py tests/behaviors/scout_find_jobs/test_assess_contract.py tests/behaviors/scout_find_jobs/test_assess_model_policy.py -q` passes: the UAT shape (36 ClickHouse + 31 Coupang + 2 gen-digital, cap 5) → ≤2 per company, no duplicate titles; a setup-default config (codex-default) resolves codex_cli; an unmatched target → a clear error. Also run test_m1_end_to_end.py (offline) and `make unit-tests`.
EXCLUSIONS: no schema or storage migration without asking; no live provider calls; no git add/commit/stash/reset/clean; focused tests only. Say READ vs EXECUTED. Write .orchestrator/workers/selection-diversity-u2.md, then worker_done.
