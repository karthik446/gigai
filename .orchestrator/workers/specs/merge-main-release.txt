TARGET (merge-main-release): finish the in-progress merge of origin/main (GigAI 0.1.8.1, #38, 7adf3bb) into karthik446/gigai-v0.1.9. The coordinator ran `git merge --no-commit --no-ff origin/main`; one conflict is left: .github/workflows/release.yml. decisions.log is already resolved and staged. Do NOT commit, abort or reset the merge; the coordinator commits.
BACKGROUND: both sides fixed the v0.1.8 release race and the release notes independently.
- main (operator-approved, shipped in 0.1.8.1; its release run 35948069799 is running now): verify-testpypi runs post-publish with continue-on-error and no longer gates publish-pypi; `until uv tool install --refresh …` retry with a 600s deadline, timeout 15; github-release needs [preflight, build, publish-pypi] with a sparse checkout of CHANGELOG.md and INLINE python that extracts the "### X.Y.Z" section, FALLING BACK to --generate-notes if it's missing.
- ours (wave 1, commit c4cc1b3): verify-testpypi still gates publish-pypi; retry 20x30s, timeout 20; tools/release_notes.py (extraction + a minimal workflow parser) called from github-release, plus a preflight step that FAILS if the section is missing; and `permissions: {contents: read, actions: read}` on the `ci` and `post-release-compatibility` jobs.
RESOLUTION (coordinator decision):
1. Take MAIN's release.yml as the base: its job graph, post-publish verify jobs, continue-on-error, retry loop, timeouts and fallback-notes semantics win.
2. Add ours on top: `permissions: {contents: read, actions: read}` on BOTH jobs that `uses: ./.github/workflows/pull_request.yaml` (`ci` and `post-release-compatibility`). This is REQUIRED: this branch's pull_request.yaml `changes` job requests actions: read, and GitHub refuses to start a called workflow that asks for more than its caller grants.
3. Drop our preflight CHANGELOG hard-fail step (main chose fallback). The sparse checkout must still work for any step you keep.
4. Notes extraction: replace main's inline python with `python tools/release_notes.py --version "$VERSION"`, keeping main's fallback (if the tool exits non-zero because the section is missing or empty, write an empty release-notes.md so the existing `-s` check falls back). If the sparse checkout then needs `tools/release_notes.py`, add it to the sparse list. If that gets awkward, keep main's inline python and say why; don't invent a third design.
5. Reconcile tests: tests/behaviors/installed_release/test_release_check.py (auto-merged; main added test_four_part_hotfix_version_is_a_valid_release_tag, keep it) and tests/behaviors/installed_release/test_release_notes.py must match the resolved graph: github-release needs exactly {preflight, build, publish-pypi}; verify-pypi needs github-release; verify-testpypi needs publish-testpypi and nothing needs verify-testpypi; ci and post-release-compatibility grant actions: read. The hand-rolled parser in tools/release_notes.py must cope with main's `continue-on-error` keys (and any other new keys); extend it minimally if needed.
6. Also run the ci-docs-skip caller test, which parses release.yml: tests/behaviors/ci_tooling/test_ci_changes.py.
OWNED FILES (nothing else): .github/workflows/release.yml, tools/release_notes.py, tests/behaviors/installed_release/test_release_check.py, tests/behaviors/installed_release/test_release_notes.py, .orchestrator/workers/merge-main-release.md. Mark release.yml resolved with `git add .github/workflows/release.yml` ONLY (that one git add is allowed; nothing else).
ACCEPTANCE: `git diff --name-only --diff-filter=U` is empty; no conflict markers anywhere (`git grep -n '^<<<<<<<\|^>>>>>>>'` empty); `uv run --locked --extra test pytest tests/behaviors/installed_release/test_release_check.py tests/behaviors/installed_release/test_release_notes.py tests/behaviors/ci_tooling/test_ci_changes.py -q` passes; system `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` OK; paste the resolved job graph (needs + permissions per job) parsed with system yaml; `make unit-tests` passes (~7s).
EXCLUSIONS: no git commit/merge --abort/reset/stash/clean/checkout of other files; no push; no tags; no workflow_dispatch; no live runs; focused tests only (the selectors above plus make unit-tests). Say READ vs EXECUTED. Write .orchestrator/workers/merge-main-release.md, then worker_done.
