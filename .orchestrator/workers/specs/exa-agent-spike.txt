TARGET (exa-agent-spike): STAGE 1 of the operator-approved brief .orchestrator/workers/specs/scout-exa-agent-discovery.brief.txt (read it fully: decisions, pricing facts, metrics, deliverables). Learn the Scout setup-interview questions, the answers→query template, the output schema and the default effort/cap for a weekly Exa Agent company-discovery run. SPIKE ONLY: docs + scripts; no product code under src/.
LIVE SPEND IS AUTHORIZED by the operator: at most $2 per agent run and $30 TOTAL (Exa charges). Enforce it in code, before each call:
- a spend tally file research/exa_agent_spike/spend.jsonl (one line per call: timestamp, run label, effort, estimated or reported cost, cumulative); refuse to start a call whose worst-case cost would push the total over $30 or that has a cap above $2 (for auto/max, set the API's own cost cap to ≤ $2 per the docs);
- start cheap (low effort, one phrasing) and scale up only while the results teach something; you do NOT have to spend the budget. Stop and report once the questions/schema/effort are clear.
KEY: read EXA_API_KEY via gigai's secrets lookup (os.environ first, then ~/.gigai/.env: `from gigai import secrets_store; secrets_store.get("EXA_API_KEY")`). NEVER print, log, echo, save or commit the key or request headers; redact them from every saved response. Don't open ~/.gigai/.env yourself. The key was rotated today after a paste leak, so treat it carefully.
DO:
1. Verify the Agent API from Exa's docs (endpoint path, request body incl. input.data for the exclusion list, output_schema, effort levels, cost cap, async polling/SSE, output.grounding). Cite URLs. Use REST via httpx (as src/gigai/scout/find_jobs/exa_client.py does); no exa_py SDK unless you justify it.
2. Draft the setup-interview question list from the brief (roles/level + titles to avoid; countries + remote/hybrid/onsite + city; needs visa sponsorship yes/no; companies to exclude; companies to always watch; stage/size + industries in/out; must-have / deal-breaker stack; cadence + budget) with the operator's UAT answers as defaults (staff/senior backend; Denver, CO or remote; US; sponsorship yes; exclude coupang, clickhouse, gen-digital).
3. Vary 3-4 query phrasings built from those answers × 2 schema shapes (strict {company, careers_url, ats_provider, board_token, sponsorship: yes|no|unknown, sponsorship_evidence, source} vs looser) × effort (low, medium, auto capped ≤ $2). Not every cell is required; explain which you skipped and why.
4. For every returned company, check usability WITHOUT extra Exa spend: resolve the careers/board URL and poll it with Scout's existing ATS clients (import from gigai.scout.find_jobs.ats_board_clients; public ATS APIs, free) to see whether it's a Greenhouse/Ashby/Lever board with ≥1 posting matching the roles in the US.
5. Metrics per run: cost, latency, companies returned, % new vs the exclusion list, % US, % sponsorship claims with a real grounding source (spot-check a sample of sources by fetching them), % boards that resolve and poll, and the PRIMARY metric: cost per NEW USABLE BOARD.
6. Save every raw agent response as a redacted fixture under research/exa_agent_spike/fixtures/ (named by run label) for stage-2 tests.
DELIVERABLES: docs/development/v0.1.9/spikes/S23-scout-setup-interview-exa-agent.md in the spike ticket format (read docs/development/v0.1.9/spikes/README.md "Ticket format"; Status / Problem / Operator decisions / Tasks / Acceptance / Investigate / Results (metrics table) / Recommendation / Open questions / Non-claims / Change log): the final question wording, the answers→query template, the output schema, the default effort + cap, the metrics table, total spend, and a short recommendation for stage 2. Add one S23 bullet to docs/development/v0.1.9/spikes/README.md.
OWNED FILES (nothing else): research/exa_agent_spike/** (new: scripts, fixtures, spend.jsonl, a README), docs/development/v0.1.9/spikes/S23-scout-setup-interview-exa-agent.md (new), docs/development/v0.1.9/spikes/README.md (one bullet), .orchestrator/workers/exa-agent-spike.md. Don't touch src/, tests/, pyproject.toml or uv.lock (use the existing env; if you truly need a new package, ask).
ACCEPTANCE: the S23 doc exists with all sections and a metrics table whose totals match spend.jsonl (recount); every fixture is redacted (grep the fixtures for "x-api-key", "Authorization" and the first 6 chars of nothing: just grep for header names and "api_key"); total spend ≤ $30 and no run over $2 (show the tally); every doc claim cites a URL or a command run.
EXCLUSIONS: no product code; no Exa Monitors (webhooks need a public endpoint); no Exa search/contents spend beyond what a spike run needs; no git add/commit/stash/reset/clean; never print the key. Say READ vs EXECUTED. Write .orchestrator/workers/exa-agent-spike.md (include the running spend) on finish or on a question, then worker_done.
