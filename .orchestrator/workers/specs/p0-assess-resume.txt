TARGET (p0-assess-resume): PR #37 review P0-3 and P0-4 (both PLAUSIBLE; reproduce first).
P0-3: src/gigai/scout/proposal_execution.py _assess_progress_writer (~:87-103) prefers Path(target) over context.workpad_path. If target is ever the repo/target root rather than the workpad root, progress files land in <target>/runs/... (a stray dir in the operator's repo) and the UI cards stay "waiting" because /progress reads the workpad run dir. Trace what assess_node's target argument actually is at every call site (the graph worker path in find_jobs/bindings.py and any tests), write a test that proves where progress lands in the real run path, and make the writer ALWAYS use the workpad run dir (the same root acquire's writer and present_api's /progress use). Never write under the target.
P0-4: src/gigai/scout/scout_cli.py (~:204) : re-adding an EDITED resume with the same file name conflicts (private_operation_conflict). Reproduce, then key the operation by content (e.g. name + sha256 of the bytes) so the same bytes stay idempotent and edited bytes create a new resume revision; check the record-wrapper step's key (~:215) stays consistent. The newest revision must be what find-jobs uses (run.resolve_newest_resume).
Review file: .orchestrator/reviews/pr37-review-findings.md. REPRODUCE FIRST with a failing test, then fix, then show it passing. Ask questions ONLY via `orca orchestration ask`, never a local prompt. TEST BUDGET: exact test files only (list them), plus `make unit-tests`; never a whole directory. No git add/commit/stash/reset/clean. Say READ vs EXECUTED.
OWNED FILES: src/gigai/scout/proposal_execution.py (the progress-writer root only), src/gigai/scout/scout_cli.py (resume add only), tests/behaviors/scout_find_jobs/test_progress.py, tests/behaviors/scout_find_jobs/test_scout_setup_cli.py, .orchestrator/workers/p0-assess-resume.md.
ACCEPTANCE: the reproduction tests fail before and pass after; the existing test_progress.py, test_scout_setup_cli.py and test_m1_end_to_end.py pass.
