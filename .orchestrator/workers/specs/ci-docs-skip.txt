TARGET (ci-docs-skip): v0.1.9 first-things item 2, plus clearer CI job names. A docs-only push to a PR that already contains code must not rerun the ~17-min source suite, and must never let untested code through.
Today (.github/workflows/pull_request.yaml, job `changes`, step `detect`): for pull_request events it diffs the WHOLE PR (`git diff BASE...SHA`), so once a PR has any code, every later push reruns everything. Only `*.md`/`*.markdown` count as non-code. The workflow uses `cancel-in-progress` for PRs, and it is also called via workflow_call by release.yml (profile release/full) and compatibility_job.yaml (full); those profiles must keep running everything.

CHANGE:
1. Move the classification out of inline shell into a small tested script (e.g. tools/ci_changes.py) that the `detect` step calls.
2. Non-code paths: `*.md`, `*.markdown`, `*.txt`, `docs/**`, `.orchestrator/**`, `.claude/**/*.md`. Everything else is code (including `.claude/**/*.py|*.sh`, workflows, pyproject, uv.lock, src, tests, tools).
3. On `pull_request` `synchronize`: skip the heavy jobs ONLY IF the incremental diff (`github.event.before`..SHA) is all non-code AND the source suite already passed for `before` (look up the conclusion of this workflow's run(s) for head SHA `before` with `gh api` using GITHUB_TOKEN; add the minimal `actions: read` permission to that job). Otherwise diff the whole PR as today. This matters because cancel-in-progress can cancel a code push's run: a following docs-only push must not skip then. Also fall back to the whole-PR diff if `before` is missing, all zeros, or not an ancestor of SHA (force-push). Opened/reopened/ready_for_review: whole-PR diff as today. Non-`pr` profiles: unchanged (always run).
4. Rename displayed job names only (keep job ids, `needs:` and outputs unchanged): "Source suite (Python X on OS)" → "All tests (source) (Python X on OS)"; "Deterministic G28 behavior evaluation" → "Eval scoring check (frozen cases)"; "Built-wheel resources" → "Wheel install + packaging checks". The repo has no branch protection or required checks (checked: `gh api .../branches/main/protection` → 404, the only ruleset is disabled), so renames are safe. Grep the repo for the old names (docs, tools, tests) and report every hit; fix only the ones in your owned files, list the rest.
5. Log clearly in the step output why it ran or skipped (diff base used, the non-code/code verdict, the `before` run conclusion).

OWNED FILES (nothing else): .github/workflows/pull_request.yaml, tools/ci_changes.py (new), tests/behaviors/ci_tooling/test_ci_changes.py (new), .orchestrator/workers/ci-docs-skip.md. Don't touch release.yml (another worker owns it), compatibility_job.yaml, pyproject.toml or Makefile.

ACCEPTANCE:
- `uv run --locked --extra test pytest tests/behaviors/ci_tooling/test_ci_changes.py -q` passes, with cases for: docs-only incremental push + before passed → skip; docs-only incremental + before cancelled/failed/unknown → run whole-PR check; code in the incremental diff → run; force-push/non-ancestor/zero `before` → whole-PR diff; `.claude/skills/x/SKILL.md` non-code but `.claude/skills/x/run.sh` code; non-pr profile → run.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/pull_request.yaml'))"` OK; `actionlint` on it if installed (say if not).
- Paste the final `detect` step and the list of old-name grep hits.

EXCLUSIONS: no schema or storage migration; no git add/commit/stash/reset/clean/push (the coordinator commits, and pushing to the draft PR is how the coordinator will prove this live); no workflow_dispatch runs; no live/provider runs; focused tests only (the exact selector above; never whole directories, never -n, never make test). Write .orchestrator/workers/ci-docs-skip.md on finish or on a question (state, files changed, evidence, open questions). Say what you READ vs EXECUTED. Then worker_done.
