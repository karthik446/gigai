TARGET (p1-stale-pid): src/gigai/scout/run_supervisor.py stop/status trust the pid in the state file. If the Scout server died and the OS reused the pid for another process, `gigai scout stop` would SIGTERM/SIGKILL an unrelated process, and status would report "running".
Context: PR #37 review P1s the operator wants BEFORE UAT (.orchestrator/reviews/pr37-review-findings.md). REPRODUCE FIRST with a failing test, then fix, then show it passing. Ask questions ONLY via `orca orchestration ask`. TEST BUDGET: exact test files only (list them) + `make unit-tests`; never a whole directory. No git add/commit/stash/reset/clean/push. Say READ vs EXECUTED.
CHANGE: before signalling or reporting "running", confirm the pid is OUR server: it answers the health check on the recorded port with the recorded identity (add a small identity field to the health response if needed, e.g. the state file's run token, since present_api's health/config route is owned by another worker right now; if you need a change in present_api.py, ASK first), and/or its command line matches the gigai scout server (psutil isn't a dependency: use a portable approach, e.g. /proc on Linux and `ps -o command= -p PID` on macOS). If it's not ours: treat the state as stale, clean it up, report "not running (stale state cleaned)", and NEVER signal that pid.
OWNED FILES: src/gigai/scout/run_supervisor.py, tests/behaviors/scout_find_jobs/test_scout_run_supervisor.py, .orchestrator/workers/p1-stale-pid.md.
ACCEPTANCE: tests: a state file pointing at a live unrelated process (spawn a harmless `sleep` child) → stop does NOT kill it and cleans the state, status says not running; a real server → stop works as before; a dead pid → stale cleanup. Kill every process you start.
