TARGET (wire-selection): finish UAT 0.1.8.1 ticket B2 by wiring the committed pure helper src/gigai/scout/find_jobs/selection.py (`select_for_assessment(rows, *, cap, per_company=2) -> SelectionResult(selected, dropped)`, commit 0bf97c0; read .orchestrator/workers/selection-diversity-u2.md "B2" + its recommended call shape) into acquire's selection loop, replacing the first-come-first-served walk.
FACTS (READ): acquire's selection loop is in src/gigai/scout/find_jobs/market_acquisition.py around :453-490 (after the B1 drop-at-acquire change in 80a2e98; re-locate it); selected rows become the sealed AssessInput.selected_postings (unique, role_match=True, len ≤ selection_cap: contracts._validate_assess_input). Assess labels unselected candidates in src/gigai/scout/proposal_execution.py ~:141-178 by priority exclusion_reason → over_cap → ...
CHANGE:
1. market_acquisition.py: candidates = the new/edited, role-matched rows not excluded by exclusion_reason (as today); then `select_for_assessment(candidates, cap=selection_cap)` decides selected_postings. Keep the sealed contract valid (unique, ≤ cap). Deterministic (same input → same selection).
2. Labels (coordinator decision): rows dropped as "duplicate" → the existing NotAssessedReason.DUPLICATE; "company_cap" and "over_cap" → NotAssessedReason.OVER_CAP. Make assess's not-assessed labeling (proposal_execution.py) produce those labels for the right rows (recompute with the same pure helper from the same inputs, or read what acquire recorded; pick the one that can't disagree and say why). No new enum values.
3. If acquire records per-reason drop/selection counts (DropCount from 80a2e98), add the selection outcome counts there additively if it fits; otherwise skip and say so.
OWNED FILES (nothing else): src/gigai/scout/find_jobs/market_acquisition.py (selection loop only), src/gigai/scout/proposal_execution.py (not-assessed labeling only), src/gigai/scout/find_jobs/selection.py (only if a small fix is needed), tests/behaviors/scout_find_jobs/test_acquire_network.py, tests/behaviors/scout_find_jobs/test_selection_diversity.py, tests/behaviors/scout_find_jobs/test_assess_contract.py, .orchestrator/workers/wire-selection.md.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_acquire_network.py tests/behaviors/scout_find_jobs/test_selection_diversity.py tests/behaviors/scout_find_jobs/test_assess_contract.py tests/behaviors/scout_find_jobs/test_m1_end_to_end.py -q` passes, with a new acquire-level test using the UAT shape (36 ClickHouse + 31 Coupang + 2 gen-digital, cap 5): selected ≤ 2 per company, no duplicate titles, and the unselected rows labelled DUPLICATE / OVER_CAP in assess output. `make unit-tests` passes. TEST BUDGET: those exact files only; never the whole scout_find_jobs directory.
EXCLUSIONS: no schema or storage migration; no live calls; no git add/commit/stash/reset/clean. Say READ vs EXECUTED. Write .orchestrator/workers/wire-selection.md, then worker_done.
