TARGET (release-pipeline): v0.1.9 first-things items 0 and 3. Fix .github/workflows/release.yml so post-publish install checks can't block the release, and publish the CHANGELOG section as the release notes.
Background: in release run 35909539154 (v0.1.8), `verify-pypi` ran `uv tool install gigai==0.1.8` <1s after upload, before the PyPI index had the version ("no version of gigai==0.1.8"). `github-release` needs `verify-pypi` (release.yml:239), so the GitHub Release and the post-release matrix were skipped. Also `gh release create ... --generate-notes` (release.yml:~303) yields one line per squash-merged PR.

CHANGE:
1. `github-release` must need [preflight, build, publish-pypi], NOT verify-pypi. Create the Release right after publish.
2. `verify-pypi` becomes a post-release check: needs github-release (and preflight). A failure there reports but can't block the Release. `post-release-compatibility` must not depend on verify-pypi.
3. `verify-pypi` and `verify-testpypi`: before installing, poll for the version with a bounded retry (up to ~10 min total, e.g. every 20-30s), then install without a stale index cache (`uv tool install --refresh …`, or equivalent; read uv's docs/--help to pick the right flag, don't guess). Keep the setup/doctor smoke steps as they are. `verify-testpypi` still gates `publish-pypi` (it's the pre-publish staging check); only add the retry there.
4. Release notes: replace `--generate-notes` with `--notes-file` built from the CHANGELOG.md section for the version (`### X.Y.Z` under "## Released versions"; read CHANGELOG.md and tools/release_check.py first and reuse any existing parser). Put the extraction in a small tested tool (tools/release_notes.py, or a function in tools/release_check.py if it fits there better). Run the extraction in `preflight` too, so a missing or empty section fails BEFORE anything is built or published.

OWNED FILES (nothing else): .github/workflows/release.yml, tools/release_notes.py (new) or tools/release_check.py, tests/behaviors/installed_release/test_release_check.py and/or a new tests/behaviors/installed_release/test_release_notes.py, .orchestrator/workers/release-pipeline.md. Don't touch .github/workflows/pull_request.yaml (another worker owns it), pyproject.toml, Makefile or CHANGELOG.md.

ACCEPTANCE:
- `uv run --locked --extra test pytest tests/behaviors/installed_release/test_release_check.py tests/behaviors/installed_release/test_release_notes.py -q` passes (drop the second path if you put the parser in release_check.py). The tests cover: 0.1.8 section extracted; missing version → error; empty section → error; the next "### " heading ends the section.
- Add a test asserting the job graph: github-release does not need verify-pypi; verify-pypi needs github-release; publish-pypi still needs verify-testpypi. Parse the YAML; don't grep strings.
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release.yml'))"` OK; also run `actionlint .github/workflows/release.yml` if actionlint is installed (say if it isn't).
- Paste the extracted 0.1.8 notes and diff them against /Users/kar/orca/workspaces/gigai/v0.1.8-release-notes.md (read-only; just report how they differ).
- Paste the retry shell snippet and explain its time bound.

EXCLUSIONS: no schema or storage migration; no git add/commit/stash/reset/clean/push (the coordinator commits); no tags, no workflow_dispatch runs, no gh release edits; no live/provider runs; focused tests only (the exact selectors above; never whole directories, never -n, never make test). Write .orchestrator/workers/release-pipeline.md on finish or on a question (state, files changed, evidence, open questions). Say what you READ vs EXECUTED. Then worker_done.
