TARGET (p0-country-words): PR #37 review P0-1 (CONFIRMED by the coordinator): src/gigai/scout/find_jobs/filters.py (~:96, the pycountry-built alias table) matches ISO alpha-2/alpha-3 codes as ordinary words, so real US postings are dropped at acquire. Coordinator probe (EXECUTED): location_countries('New York City and Remote') == ['AD'] ("and" → Andorra), 'Hybrid (3 days per week)' → ['PE'] ("per" → Peru), 'Remote - EST timezone' → ['EE'] ("EST" → Estonia); all give country_match(..., ('US',)) == False. Also 'Remote (US)' → [] (None, should be US) and "New York City"/"NYC" aren't recognised.
Review file: .orchestrator/reviews/pr37-review-findings.md. REPRODUCE FIRST with a failing test, then fix, then show it passing. Ask questions ONLY via `orca orchestration ask`, never a local prompt. TEST BUDGET: exact test files only (list them), plus `make unit-tests`; never a whole directory. No git add/commit/stash/reset/clean. Say READ vs EXECUTED.
CHANGE (filters.py only): codes (alpha-2 and alpha-3) match only as UPPERCASE tokens in the original string (case-sensitive), delimited by punctuation/whitespace/parentheses/start/end; never lowercase words. Full country names and common names stay case-insensitive. Keep the three labelled policy lists (UK fold; CA/IN/DE as US states; the city list) and the region-only rule. Add common US forms: "Remote (US)", "US-based", "USA only", "United States (Remote)", "NYC", "New York City". Guard against timezone tokens (EST/PST/CST/MST/ET/PT/CT/GMT/UTC/CET...) being read as countries.
REGRESSION CORPUS (required): collect every distinct location string from the operator's UAT evidence run raw payloads (read-only: ~/.gigai/workpads/projects/project_e12ab112-*/gigs/gig_015b1fc0-*/runs/run_d73cb030-62a3-4d5e-9629-a33977d90194/raw/{greenhouse,ashby}, plus the committed tests/behaviors/scout_find_jobs/fixtures/fixture-uat-0181-replay-postings.json and research/exa_agent_spike + research/discovery_bakeoff board-check fixtures). Save the de-duplicated location strings (no other posting data) as a small fixture, and add a test with the expected country set for each (hand-labelled). Paste a before/after table of every string whose classification changed.
OWNED FILES: src/gigai/scout/find_jobs/filters.py, tests/behaviors/scout_find_jobs/test_filters.py, tests/behaviors/scout_find_jobs/fixtures/location-corpus.json (new), .orchestrator/workers/p0-country-words.md.
ACCEPTANCE: the three reported strings + 'Remote (US)' → US match; the region-only and existing filter tests still pass (test_filters.py, test_acquire_network.py incl. the UAT replay); the corpus test passes.
