TARGET (b4-progressive-cards): UAT 0.1.8.1 ticket B4 (+ U18). Read .orchestrator/workers/specs/scout-uat-0181-bugs.brief.txt (B4) and the UAT log row U18 (/Users/kar/orca/workspaces/gigai/v0.1.8-uat.md, read-only). Operator: "load ui sooner, one search → assess load, as a card … this list isn't going to work … we load as we get".
WHAT THE OPERATOR WANTS:
1. Replace the long results table with CARDS, one per posting, each carrying its assessment details (requirement × resume matrix, suggestions, questions; there is already src/gigai/scout/ui/src/components/AssessmentCard.jsx, MatrixBadge.jsx, SponsorshipBadge.jsx; reuse them).
2. PROGRESSIVE: a card appears as soon as its posting is acquired (title, company, location, countries, sponsorship, source) and fills in when its assessment finishes. Don't wait for the whole run.
3. LIVE STEP STATUS (U18): acquire/assess/present show "running" while they run, not "waiting" until a receipt appears at step end.
4. Show the run's assess cap (e.g. "assessing 5 of 42 matches, cap 5") and why the others weren't assessed (DUPLICATE / OVER_CAP / exclusion reasons already exist).
DESIGN CONSTRAINTS (read the code first: the run executes in a spawned child process (src/gigai/run.py launch_run; scout/find_jobs/bindings.py); outputs/*.json + receipts are the sealed authority, written at step end; present_api.py serves /api/runs/{id} (status + node_receipts) and /api/runs/{id}/results):
- Add NON-AUTHORITATIVE progress files next to the sealed outputs, written as work happens, e.g. <run>/progress/acquire.jsonl (one line per posting kept after the B1 filter, as each source/board completes) and <run>/progress/assess.jsonl (one line per finished assessment, plus a started line), plus a small progress/steps.json (step → running/done/failed with timestamps). Append-only, atomic line writes, fsync not required. The sealed outputs/receipts stay exactly as they are and remain the authority; the final results view must still come from them.
- A new GET /api/runs/{id}/progress returns {steps, postings[], assessments[], cap, not_assessed_counts} built from those files (and from the sealed outputs once they exist). Loopback only, same error conventions as the other routes; unknown run → the existing not-found shape.
- UI polls /progress every ~1-2s while the run is active (stop when done/failed), renders cards incrementally, keeps a compact filter/sort (company, sponsorship, assessed/not); no layout thrash. Rebuild ui/dist (the PR CI freshness job compares it).
- Don't change the sealed contracts, the run seam, or the scheduler. If live step status needs a hook in run.py or bindings.py, STOP and ask first.
OWNED FILES (nothing else): src/gigai/scout/find_jobs/market_acquisition.py (progress writes only), src/gigai/scout/proposal_execution.py (progress writes only), src/gigai/scout/find_jobs/progress.py (new: the file format + reader/writer), src/gigai/scout/find_jobs/present_api.py (the /progress route only), src/gigai/scout/ui/src/**, src/gigai/scout/ui/dist/**, tests/behaviors/scout_find_jobs/test_progress.py (new), tests/behaviors/scout_find_jobs/test_present_ui.py (progress cases), .orchestrator/workers/b4-progressive-cards.md.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_progress.py tests/behaviors/scout_find_jobs/test_present_ui.py tests/behaviors/scout_find_jobs/test_present_api_static.py tests/behaviors/scout_find_jobs/test_m1_end_to_end.py -q` passes. test_progress covers: the writer/reader round trip; partial files mid-run; /progress before acquire, mid-acquire, mid-assess and after completion; the sealed results unchanged. The offline M1 end-to-end also asserts that progress files exist and agree with the sealed outputs at the end. `make unit-tests` passes. yarn build is deterministic (run twice, git diff --exit-code on dist). Describe the card UI in the report (with a screenshot only if you can make one without a live run; otherwise describe it). TEST BUDGET: exact files only.
EXCLUSIONS: no schema or storage migration; no sealed-contract changes; no live Exa/ATS/provider runs (use the existing MockTransport + stand-in model fixtures); temp homes only; no git add/commit/stash/reset/clean. Say READ vs EXECUTED. Write .orchestrator/workers/b4-progressive-cards.md, then worker_done.
