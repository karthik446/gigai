TARGET (readme-uv-only-r1): your pip→uv edit (accepted) exposed a broken instruction. README.md:206 says `python -m gigai.scout.find_jobs.present_api --home ~/.gigai --target /path/to/target/repo` under "Works from an installed package (`uv tool install gigai`)". With `uv tool install`, gigai lives in an isolated tool environment, so a plain `python` can't import it; that line fails for a uv-only user.
CHANGE: replace it with a uv command that runs the module from the installed package, e.g. `uv tool run --from gigai python -m gigai.scout.find_jobs.present_api …` (read `uv tool run --help`; pick the correct form, and pin with `--from gigai==<version>` only if the README pins elsewhere). VERIFY it for real: `uv tool run --from gigai==0.1.8.1 python -c "import gigai.scout.find_jobs.present_api; print('ok')"` (this downloads the published package into uv's cache; that's allowed, nothing else). Keep the rest of the paragraph accurate.
OWNED FILES: README.md (that paragraph only), .orchestrator/workers/readme-uv-only.md (append r1).
ACCEPTANCE: paste the new line and the verification command's output.
EXCLUSIONS: no other edits; no git add/commit/stash/reset/clean; no network beyond that one uv tool run. Say READ vs EXECUTED. Then worker_done.
