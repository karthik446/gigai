TARGET (v020-s21-gig-repos-r3): one precision fix in docs/development/v0.2.0/spikes/S21-gig-repos-and-data-ownership.md. The coordinator's r2 spec over-attributed, and this is the coordinator's error, not yours: D-B says only "monorepo for now; split into repos at 4-5 gigs with clear separation". It does NOT say gigs are separately built packages/distributions inside the monorepo. That in-repo package layout (gigs/<name>/, each publishable) is the PROPOSED Workstream 7 of docs/development/v0.2.0/roadmaps/v0.2.0-gig-workbench-roadmap.md, not an operator decision.
CHANGE: wherever S21 says D-B makes option (ii) "the monorepo-period target/direction" or that D-B "settles that gigs live ... as separately built packages" (§5 intro, option 2's last sentence, the "Open (not decided)" paragraph, Q9, Q10, and the D-B note in the decisions section or Status if it says the same), restate it as: D-B rules out (iii) extract-now and keeps gigs in this repo; whether they become separately built in-repo packages (ii), and when relative to decoupling (i), is open; the roadmap's Workstream 7 proposes (ii) (proposed, not accepted). Keep (iii) struck through. Q10: the in-repo distribution name is only needed if (ii) is adopted; say so. Add an r3 Change log line naming the correction.
OWNED FILES (nothing else): the S21 file, .orchestrator/workers/v020-s21-gig-repos.md (append r3). Don't touch the roadmap (another worker is revising it).
ACCEPTANCE: grep -n "monorepo-period target\|separately built" shows no text attributing (ii) to D-B; paste the changed paragraphs.
EXCLUSIONS: docs only; no git add/commit/stash/reset/clean. Say READ vs EXECUTED. Then worker_done.
