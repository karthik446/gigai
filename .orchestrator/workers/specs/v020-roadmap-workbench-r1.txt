TARGET (v020-roadmap-workbench-r1): rework the roadmap per the coordinator's review .orchestrator/reviews/roadmap-workbench-r0.md. Fix items 1-5 in docs/development/v0.2.0/roadmaps/v0.2.0-gig-workbench-roadmap.md. Re-run the count yourself (`grep -c 'from \.scout\|from gigai\.scout' src/gigai/*.py`, sum it, and list the files) and cite S21 §7 "Relationship to S16, S17, S20" for the 36 + 9 = 45 reconciliation. For item 2, read .orchestrator/status.md "Goal" and .orchestrator/decisions.log (the 15:20 wave 1 line and the v0.1.8 "10:40" deferral line) and cite them. Add an r1 Change log entry. Keep everything the review says holds up.
OWNED FILES (nothing else): the roadmap file, .orchestrator/workers/v020-roadmap-workbench.md (append an r1 section). Don't touch docs/development/v0.2.0/spikes/ (the S21 worker is still editing).
ACCEPTANCE: no "only 9" or "34 distinct" count claims remain (grep); every "Pull into 0.1.9?" line states the real v0.1.9 scope; the diagram and table agree; the W0 test selectors are exact files; the new citations resolve (sed -n).
EXCLUSIONS: docs only; no git add/commit/stash/reset/clean; no live runs; never read secret values. Say READ vs EXECUTED. Update the worker file, then worker_done.
