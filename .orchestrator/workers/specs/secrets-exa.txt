TARGET (secrets-exa): Workstream 0 P3 (pulled into 0.1.9). Exa reads its API key through gigai's secrets lookup (environment first, then <GIGAI_HOME or ~/.gigai>/.env), so the operator no longer needs `export EXA_API_KEY`.
FACTS (READ): src/gigai/scout/find_jobs/exa_client.py:83-90 `_require_api_key()` reads os.environ[EXA_API_KEY] directly (EXA_API_KEY_ENV_VAR at :51) and raises ExaClientError("exa_missing_key", ...). The committed secrets packet (a915c24) added src/gigai/secrets_store.py (get(name) → None for missing/empty; reads with interpolation off) and src/gigai/secrets_catalog.py (exa → EXA_API_KEY). Scout (a gig) importing gigai core is the allowed direction.
CHANGE:
1. `_require_api_key()`: os.environ first (a non-empty value), then secrets_store.get(EXA_API_KEY_ENV_VAR). Keep the ExaClientError code "exa_missing_key"; change the message to name the fix: "EXA_API_KEY is not set; run `gigai secrets add exa` (or export EXA_API_KEY)". Never include the value in any message or log.
2. docs/development/v0.1.8/runbooks/M1-find-jobs.md :151 and :162 tell the operator to `export EXA_API_KEY` by hand: make `gigai secrets add exa` the primary instruction and keep export as the alternative. README: if it mentions exporting EXA_API_KEY, do the same (grep). uv only, never pip.
OWNED FILES (nothing else): src/gigai/scout/find_jobs/exa_client.py (_require_api_key only), tests/behaviors/scout_find_jobs/test_exa_client.py, docs/development/v0.1.8/runbooks/M1-find-jobs.md, README.md (EXA_API_KEY lines only), .orchestrator/workers/secrets-exa.md. Other workers are editing present_api.py, pyproject.toml, cli.py and src/gigai/scout/scout_cli.py right now; don't touch them.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_exa_client.py -q` passes with new cases (temp GIGAI_HOME via monkeypatch; env unset): key only in .env → used; env set and .env set → env wins; neither → exa_missing_key with the new message and no value leaked; empty env value → falls back to .env. `make unit-tests` passes.
EXCLUSIONS: never read or print the operator's real ~/.gigai/.env; no live Exa calls (MockTransport only); no git add/commit/stash/reset/clean; focused tests only. Say READ vs EXECUTED. Write .orchestrator/workers/secrets-exa.md, then worker_done.
