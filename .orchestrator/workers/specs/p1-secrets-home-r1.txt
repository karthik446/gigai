TARGET (p1-secrets-home-r1): coordinator review of p1-secrets-home. Accepted: home_root threaded through credentials.resolve_reference_value, the Exa client, discovery's _api_key and interview prep, plus the Protocol line and the test fakes.
ONE GAP: the MODEL adapters still resolve keys without the home. adapters/http.py uses `credential_resolver or resolve_reference_value` and calls `self._credential_resolver(self._credential)` (~:32, :43); adapters/openrouter_api.py and openai_api.py pass credential_resolver through. Wherever these adapters are constructed (the adapter factory / resolve_model_adapter path that assess and prep use; trace it), no home-aware resolver is passed, so with `--home X` an OpenRouter/OpenAI model key stored in X is not found when assess runs on openrouter_api (or prep uses an openai target).
CHANGE: at the adapter-construction site(s), pass `credential_resolver=functools.partial(resolve_reference_value, home_root=<the run's home>)` when a home is known (thread the home into the factory with an optional keyword; default unchanged). Don't change the adapters' own defaults. Add a test: a key stored under a temp home via secrets_store.set(..., home_root=X) and GIGAI_HOME pointing elsewhere → the factory-built adapter (with a MockTransport client) sends that key.
OWNED FILES: src/gigai/adapters/http.py (only if needed), the adapter factory module(s) you trace (name them), the call site(s) in scout (proposal_execution.py / interview_prep) that build adapters (home plumbing only), tests/behaviors/secrets/test_secrets_home.py, your worker file (append r1).
ACCEPTANCE: the new test fails before and passes after; test_secrets_home.py, test_secrets_resolution_order.py and test_assess_model_policy.py pass; make unit-tests passes. Ask only via orca orchestration ask; exact test files only; no git add/commit/stash/reset/clean. Say READ vs EXECUTED. Then worker_done.
