CONTEXT: GigAI 0.1.8.1 hotfix from the operator UAT of published 0.1.8 (log: /Users/kar/orca/workspaces/gigai/v0.1.8-uat.md; read the findings named below). Worktree: /Users/kar/orca/workspaces/gigai/gigai-v0.1.8.1. C0 has already extended src/gigai/scout/find_jobs/contracts.py (FindJobsConfig.countries + visa_sponsorship_required; PostingRow.text + sponsorship; NotAssessedReason location_mismatch/sponsorship_excluded/model_output_invalid; AssessmentResult.sponsorship): import those, never edit contracts.py (ask via orca orchestration ask if you need a change). Real live data to learn from (read-only): the operator run outputs under ~/.gigai-scout/workpads/projects/*/gigs/*/runs/run_*/outputs/acquire.json. Other workers are editing other files in this worktree right now: touch only your owned files.
TARGET (P1 acquire): fix U25 (keep the posting text), U26 (store raw responses), U20 (prefer job-board rows), U19 (country filter), U12 (visa).
1. ATS clients (scout/find_jobs/ats_board_clients.py): keep each posting's description as plain text in PostingRow.text (Greenhouse `content` is HTML-escaped HTML, Lever `descriptionPlain` + lists, Ashby `descriptionPlain`; convert HTML to readable text with the stdlib html.parser; no new deps); keep content_sha256 as the hash of that text.
2. Exa client (scout/find_jobs/exa_client.py): request page text with the search (Exa's `contents: {"text": {"maxCharacters": N}}` option; bounded N) so Exa-only rows have PostingRow.text too; record the assumed API shape in the worker file.
3. market_acquisition.py: (a) dedupe Exa vs ATS rows for the same job (same normalized URL, or the same board_token + job id parsed from the URL): the ATS row wins (full title, location, text), the Exa row only as a fallback; (b) country filter: when config.countries is non-empty, keep a row only if its location matches one of those countries: parse multi-location strings ("Remote, Canada; Remote, United States", "Bengaluru, India", "Denver, CO", "New York, NY", "Atlanta, Georgia", "Remote - US"); US state names/abbreviations mean US; ambiguous/empty locations are KEPT and flagged, not dropped; filtered-out rows stay visible in outputs with reason location_mismatch and are never selected; (c) visa: derive PostingRow.sponsorship from the text (not_offered when it says e.g. "unable to sponsor", "no visa sponsorship", "must be authorized to work … without sponsorship", "not able to provide sponsorship"; offered when it says "visa sponsorship available", "will sponsor"; else unknown); when config.visa_sponsorship_required, rows with sponsorship=not_offered are excluded from selection with reason sponsorship_excluded (still visible); (d) selection = new/edited + role match + country ok + not visa-excluded, capped; (e) U26: store the raw responses per run: every Exa response body and every ATS board response body (redacted: no request headers or API keys; response JSON only), gzip-compressed, under the run dir `runs/<run_id>/raw/<source>/<n>.json.gz` (get the run dir from NodeContext.workpad_path + run_id), plus an index.json listing source, url (keys stripped), status, bytes and sha256. Cap the total size (e.g. 20 MB per run) and note the cap in the index.
OWNED: src/gigai/scout/find_jobs/ats_board_clients.py, exa_client.py, market_acquisition.py, watchlist.py (only if needed); tests/behaviors/scout_find_jobs/test_ats_board_clients.py, test_exa_client.py, test_acquire_network.py; .orchestrator/workers/p1-acquire.md
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_ats_board_clients.py tests/behaviors/scout_find_jobs/test_exa_client.py tests/behaviors/scout_find_jobs/test_acquire_network.py -q` passes, with NEW tests: text kept (HTML → text) per provider; Exa contents requested and text mapped; Exa/ATS dedupe (ATS wins); the country filter over the real location strings listed above (incl. multi-location and US states; ambiguous kept); visa phrases → not_offered/offered/unknown, with exclusion only when required; raw payloads written gzip + index, with no key material (assert the API key string is absent from every stored byte).
EXCLUSIONS: owned files only; no git add/commit (the coordinator commits after verifying); no live network in tests (httpx.MockTransport only); the test budget is ONLY the listed test files (never directories, -n, or make test). Say READ vs EXECUTED. Write .orchestrator/workers/p1-acquire.md, then worker_done. Questions go through orca orchestration ask.
