CONTEXT: GigAI 0.1.8.1 hotfix from the operator UAT of published 0.1.8 (log: /Users/kar/orca/workspaces/gigai/v0.1.8-uat.md; read the findings named below). Worktree: /Users/kar/orca/workspaces/gigai/gigai-v0.1.8.1. C0 has already extended src/gigai/scout/find_jobs/contracts.py (FindJobsConfig.countries + visa_sponsorship_required; PostingRow.text + sponsorship; NotAssessedReason location_mismatch/sponsorship_excluded/model_output_invalid; AssessmentResult.sponsorship): import those, never edit contracts.py (ask via orca orchestration ask if you need a change). Real live data to learn from (read-only): the operator run outputs under ~/.gigai-scout/workpads/projects/*/gigs/*/runs/run_*/outputs/acquire.json. Other workers are editing other files in this worktree right now: touch only your owned files.
TARGET (P3 UI): the operator asked to "give users the ability to search", plus UAT U17 (show the resume label), U24 (new first), and the new fields.
In src/gigai/scout/ui/src: (1) a search box over the postings table (matching title, company, location, text snippet; case-insensitive, instant); (2) filter chips: outcome (new / edited / unchanged / all, default new+edited), country ok vs location_mismatch, sponsorship (offered / unknown / not_offered), assessed vs not assessed; (3) sort new/edited first, then by published_at desc; (4) a sponsorship badge per posting and in the assessment card; (5) show company display names nicely (if company looks like a slug, e.g. `customerio`, title-case it for display; a display-only helper); (6) the Resume line shows the resume label or filename if the API provides it, else the ids (and note in the worker file which API field would be needed; C2's API is owned by nobody in this hotfix, so don't edit Python); (7) a not-assessed reason label for location_mismatch, sponsorship_excluded and model_output_invalid; (8) show the run's failure cause from the node receipt's failure message when a step fails (it now carries the real error from P2).
OWNED: src/gigai/scout/ui/src/**; .orchestrator/workers/p3-ui.md (no Python, no package.json dependency changes).
ACCEPTANCE: `cd src/gigai/scout/ui && yarn build` succeeds; describe the UX in the worker file (no screenshots required; the coordinator checks visually).
EXCLUSIONS: owned files only; no git add/commit (the coordinator commits after verifying); no live network in tests (httpx.MockTransport only); the test budget is ONLY the listed test files (never directories, -n, or make test). Say READ vs EXECUTED. Write .orchestrator/workers/p3-ui.md, then worker_done. Questions go through orca orchestration ask.
