CONTEXT: GigAI 0.1.8.1 hotfix release. Worktree: /Users/kar/orca/workspaces/gigai/gigai-v0.1.8.1. UAT log: /Users/kar/orca/workspaces/gigai/v0.1.8-uat.md. Release lessons: /Users/kar/orca/workspaces/gigai/v0.1.9-first-things.txt items 0 and 3.
TARGET (R): make the 0.1.8.1 release publish cleanly, and fix the docs the UAT proved wrong.
1. release.yml: the v0.1.8 release published to PyPI, but "PyPI clean install" (verify-pypi) ran < 1 s after upload, raced the index ("no version of gigai==0.1.8") and skipped "create GitHub Release" + the post-release matrix. Fix: create the GitHub Release right after publish-pypi (no dependency on verify-pypi); move verify-pypi and verify-testpypi (and their dependents) AFTER it, as non-blocking post-publish checks; add a bounded poll (up to ~10 min) for the version to appear, and `uv tool install --refresh` (no stale cache). Also use --notes-file with the version's CHANGELOG section instead of --generate-notes (extract it in the workflow; fall back to --generate-notes only if missing). Validate all workflow YAML.
2. Version: bump to 0.1.8.1 everywhere it must match (pyproject version, uv.lock own-package version via `uv lock`, catalog.py CATALOG_REVISION if it tracks the version, any version-pinning tests); check tools/release_check.py accepts a 4-part version and tag v0.1.8.1 (fix it if not).
3. CHANGELOG: add a 0.1.8.1 section (the coordinator will give the final list; start from: the assessment gets the real posting text + a real prompt; tolerant model-output parsing and per-posting failure isolation, with recorded failure causes; raw Exa/ATS responses stored per run; country filter and preferring job-board rows over Exa rows; visa-sponsorship filter; UI search and filters; release pipeline fixes). Mark items "pending" if their packets haven't landed; the coordinator finalizes.
4. README corrections from UAT (don't invent; verify each against the installed CLI with `uv run --locked gigai … --help`): U7 (binding does NOT auto-create Scout); U10 (resume must be .md/.txt ≤ 1 MB; show the pdftotext/textutil conversion); the working Scout install path until 0.1.9 fixes it (U6: the documented Python helper `initialize_defaults(inventory=scout_candidate_inventory())` + `gigai approve <proposal> --gig <gig>`); setting the active gig (U14: `active_gig_id` in `<target>/.gigai/project.toml`); the resume record wrapper (U15: `gigai record create --kind imported_reference --content-family g45_reference --content-id <ref> --operation-key … --gig …`); the setup model-target naming workaround for Codex (U2: `--model-target "codex_cli=codex:default"` and output limits, U4); how to start the API with the installed package; the UI only from a checkout (U13). Mark all of these as "0.1.8.x workarounds; 0.1.9 replaces them with `gigai scout setup/run`". Keep it brief.
OWNED: .github/workflows/release.yml; pyproject.toml (version only); uv.lock (lock refresh only); src/gigai/catalog.py (version constant only); tools/release_check.py (only if 4-part versions need support; add a test in tests/behaviors/installed_release/test_release_check.py if you change it); CHANGELOG.md; README.md; .orchestrator/workers/r-release.md
ACCEPTANCE: YAML parses (uvx --from pyyaml …); `uv run --locked python tools/release_check.py verify-tag --tag v0.1.8.1` (or its local equivalent) passes; `uv run --locked --extra test pytest tests/behaviors/installed_release/test_release_check.py -q` passes if touched; `uv build` then `uvx twine check dist/*` passes on the README. No git add/commit. Say READ vs EXECUTED. Write .orchestrator/workers/r-release.md, then worker_done.
