TARGET (I-3, the final integration seam): bind the real Scout node functions into the graph and wire the API to real runs, so a Run workflow click works end to end. Read first: .orchestrator/decisions.log (all frozen conventions), .orchestrator/status.md "Next up → I-3 must", and the worker reports .orchestrator/workers/{i2-runner-seam,c2-present-api,c2b-async,a6-acquire-node,b2-assess-node,c1-present-node}.md.

OWNED FILES (nothing else): src/gigai/scout_find_jobs_bindings.py (new: the registration + dependency wiring); src/gigai/scout_materialization.py (ONLY if the bindings must hook in there); src/gigai/scout_present_api.py (ONLY the default backend/`__main__` wiring; don't change the handler/route behavior); src/gigai/scout_find_jobs_contracts.py (ONLY: add 504 to POST /api/run's ROUTES status codes, and remove the NotAssessedReason compatibility aliases NOT_NEW/CAP_REACHED/ASSESSMENT_UNAVAILABLE/ASSESSMENT_FAILED after grepping that nothing uses them); tests/behaviors/scout_find_jobs/test_m1_end_to_end.py (new); docs/development/v0.1.8/runbooks/M1-find-jobs.md (new); .orchestrator/workers/i3-bind.md.

CHANGE:
1. scout_find_jobs_bindings.py: `register_find_jobs_nodes(*, home_root, target)` registers acquire/assess/present in graph_node_registry under graph_id "find-jobs-functional", version 1, with the contracts' capabilities and declared effects. Bind deps with functools.partial: acquire → A-6 acquire_node with a real httpx.Client (timeouts set), the A-1 Exa client, A-3 ATSBoardClients and the A-5 watchlist; assess → B-2 assess_node with the GigAI config loaded once; present → C-1 present_node. EXA_API_KEY is read by the Exa client at call time, never here. Registration MUST also happen inside the run's child process: launch_run(wait=False) executes in a separate process (run.py ~840) and the registry is per-process. Find the right hook (a module the child imports before executing goals, or an explicit registration step I-2 exposes) and prove it in the test. If this needs a run.py change, don't edit run.py (I-2 owns it and is done): use `orca orchestration ask` with the exact change.
2. The API's default backend (`python -m gigai.scout_present_api --target PATH`): read_config ⇐ <target_root>/find-jobs.json (canonical bytes); resume_preview ⇐ run.resolve_newest_resume (None if absent); start_run ⇐ run_id = run.launch_find_jobs_run(..., ui_loopback_verified=True), then on_run_allocated(run_id); map a typed consent/loopback RunError to 403 and a digest mismatch to 409 (per the C-2 reports); run_status/run_results ⇐ C-1 build_present_payload + the receipts.
3. test_m1_end_to_end.py: the whole M1 path offline, with NO stubs of our own modules: a temp home + a fresh project (gigai init + install the Scout graph set the normal way; find the helper sibling tests use), a committed resume via the reference-add code path, a find-jobs.json, the real API server on an ephemeral port, the real run launch in its child process, the Exa + ATS HTTP served by httpx.MockTransport (inject through the bindings so the child process uses it; if cross-process injection is hard, expose a test-only env or hook and document it), and the deterministic/fake model adapter returning valid matrix JSON. Assert: POST /api/run ⇒ 202 fast; polling GET /api/runs/{id} reaches succeeded; GET results has rows, a non-empty matrix, suggestions, questions, not_assessed reasons, three node receipts and the pinned resume; and a second run dedups (unchanged rows ⇒ not assessed: unchanged).
4. docs/development/v0.1.8/runbooks/M1-find-jobs.md: the operator's exact steps for M1: create/bind a fresh project dir, install Scout, `gigai reference add --kind resume --file ... --target ...`, write find-jobs.json (give a filled example), export EXA_API_KEY, start `ollama serve` + pick the model, start the API (`uv run --locked python -m gigai.scout_present_api --target DIR`), start the UI (`cd ui && yarn dev`), click Run workflow; plus how to read failures. Say which steps you verified by running them and which are untested.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs -q` passes (the whole directory; paste the count), AND the import smoke `uv run --locked python -c "import gigai.scout_find_jobs_bindings, gigai.scout_present_api"` passes.
EXCLUSIONS: owned files only; no live network/provider/model calls; no git stash/reset/clean/add/commit; don't leave servers running. Say READ vs EXECUTED. Write the worker file, then worker_done.
