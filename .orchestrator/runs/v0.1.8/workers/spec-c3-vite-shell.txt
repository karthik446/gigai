TARGET (C-3): create the Vite + React shell for the Scout find-jobs UI. Only the shell: the real screens are C-4's job later.

OWNED FILES (nothing else): ui/package.json; ui/index.html; ui/vite.config.js; ui/yarn.lock; ui/.gitignore; ui/src/main.jsx; ui/src/App.jsx (a placeholder only; C-4 will replace it); .orchestrator/workers/c3-vite-shell.md

CHANGE:
- ui/package.json: private, `"type": "module"`, scripts `dev`, `build`, `preview`. Dependencies react + react-dom, devDependencies vite + @vitejs/plugin-react, each pinned to exact current stable versions (no ^ or ~). Nothing else: no UI kits, no router, no state library, no CSS framework.
- ui/vite.config.js: react plugin; the dev server binds host 127.0.0.1; `server.proxy` sends `/api` to `http://127.0.0.1:8765` (the frozen API bind from src/gigai/scout_find_jobs_contracts.py API_BIND; read it, don't edit it).
- ui/index.html + ui/src/main.jsx mount App; App.jsx renders a placeholder heading "Scout · find jobs" and fetches nothing.
- ui/.gitignore: node_modules/ and dist/.
ACCEPTANCE (run these, and paste the output tails into the worker file):
- `cd ui && yarn install --frozen-lockfile=false` (creates yarn.lock; a registry download is allowed for this step only) then `cd ui && yarn build` succeeds.
- `grep -n '8765' ui/vite.config.js` shows the proxy.
- `grep -c '\^\|~' ui/package.json` prints 0.
EXCLUSIONS: owned files only; no Python edits; no other network access; no git stash/reset/clean/add/commit. Don't leave `yarn dev` running. Say READ vs EXECUTED. On finish, write the worker file, then worker_done. Questions go through `orca orchestration ask`.
