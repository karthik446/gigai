TARGET (I-0b, small addition to your contracts): add a `FindJobsRunInput` frozen DTO to src/gigai/scout_find_jobs_contracts.py, the sealed run input that I-2 writes to runs/{run_id}/run-input.json and C-1 reads. Fields: schema_version "scout-find-jobs-run-input:1", config: FindJobsConfig, config_digest (sha256 digest; must equal config.digest(), validated), selection_cap (1..50), selection_rule: SelectionRule, model_target: ModelTarget, pinned_resume: PinnedResume (required). Same closed from_json/to_json/digest pattern as the rest. Add fixture tests/behaviors/scout_find_jobs/fixtures/fixture-run-input-v1.json and include the new DTO in the table-driven negative tests (unknown/missing/wrong-type/bad-enum + the digest mismatch).
OWNED: the same I-0 files (contracts module, test_contracts.py, fixtures/). Other workers are running in parallel: change nothing else in the module, especially no renames and no changes to existing DTOs.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_contracts.py -q` passes (paste the count); ruff passes on the module.
EXCLUSIONS: as before. Write .orchestrator/workers/i0b-run-input.md, then worker_done.
