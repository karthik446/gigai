TARGET (fix pack 1, the last fixes before M1). Read .orchestrator/reviews/terra-w1b-triage.md and .orchestrator/reviews/terra-w1b.md (W1BT-2).
OWNED FILES (nothing else): src/gigai/scout_market_acquisition.py; tests/behaviors/scout_find_jobs/test_acquire_network.py; tests/behaviors/scout_find_jobs/test_contracts.py (ONLY the POST /api/run status assertion near :477); src/gigai/scout_present_api.py (ONLY the `__main__`/CLI startup); tests/behaviors/scout_find_jobs/test_present_ui.py; .orchestrator/workers/fp1.md
CHANGES:
1. W1BT-2: in acquire_node, after source collection: if at least one source was enabled and EVERY enabled source failed, raise a redacted node error (FindJobsContractError-style code `acquire_all_sources_failed`, with the per-source failure codes in the message but no URLs or keys) BEFORE writing any acquisition batch. Partial success keeps today's FailureRow path. Tests: all-Exa-fail with only Exa enabled ⇒ raises; Exa fails + ATS succeeds ⇒ returns rows plus a FailureRow; missing EXA_API_KEY with only Exa enabled ⇒ raises, and the message doesn't contain the key.
2. test_contracts.py: update the exact POST /api/run status tuple to include 504 (it now matches contracts ROUTES).
3. scout_present_api `__main__`: if GIGAI_SCOUT_FIND_JOBS_TEST_HTTP or GIGAI_SCOUT_FIND_JOBS_TEST_MODEL is set in the environment, refuse to start (exit 2, clear stderr message naming the variable) unless `--allow-test-seams` is passed; with the flag, print a loud "TEST SEAMS ACTIVE: results are fixture data" warning at startup. Test both paths by calling the CLI entry function directly.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs -q` passes in FULL (the whole directory, 0 failures); paste the count. Import smoke: `uv run --locked python -c "import gigai.scout_market_acquisition, gigai.scout_present_api"`.
EXCLUSIONS: owned files/regions only; no network; no git stash/reset/clean/add/commit. If a permission prompt blocks you, say so via `orca orchestration ask`. Say READ vs EXECUTED. Write the worker file, then worker_done.
