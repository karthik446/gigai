TARGET (v019-spikes): create docs/development/v0.1.9/ and record THREE research spikes for the next version (S16, S17, S18). The v0.1.9 roadmap is NOT decided: these are research/spike docs that inform it, not plans or decisions. Documentation only; no code changes.
ARCHITECTURE RULE (the operator's, non-negotiable, state it verbatim in both spikes): GigAI is a platform; a Gig is a self-contained package built ON the platform. Scout is a Gig (later: trader, shopper, not now). **A gig depends on and imports gigai core; core never imports any gig.** Core provides registration and discovery points; gigs plug in.
CONVENTIONS: follow docs/development/v0.1.8/spikes/README.md ("Ticket format for new work": a short Jira-style ticket first, with problem, intended system-behavior change, tasks, acceptance criteria and attached evidence; granular detail below) and the shape of docs/development/v0.1.8/spikes/S11-behavior-based-test-organization.md (Status line, Question, Investigate, Deliverable, Change log). Spike ids continue the sequence: v0.1.8 ends at S15, so these are **S16** and **S17**. Read the memory-style discipline: every code claim cites file:line you actually read; counts are recounted from rows; label proposals as proposals, not decisions.
FILES (owned, nothing else): docs/development/v0.1.9/README.md (backlog index: purpose, the architecture rule, links to the spikes, and the carried-over 0.2.0/v0.1.9 ledger items from docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md "0.2.0 generalization ledger"); docs/development/v0.1.9/spikes/README.md; docs/development/v0.1.9/spikes/S16-core-gig-decoupling.md; docs/development/v0.1.9/spikes/S17-gig-module-structure-classes.md; docs/development/v0.1.9/spikes/S18-scout-interview-prep-graphs.md; .orchestrator/workers/v019-spikes.md.
S16, core never imports a gig: INVENTORY every import of gigai.scout (and any gig-specific names or strings) from core modules (everything in src/gigai outside src/gigai/scout/). Use AST parsing, not just grep, including lazy in-function imports, string-based module references (importlib, entry-point strings, multiprocessing targets, monkeypatch targets in tests), and data/resource paths. One row per site: file:line, what core uses it for, a category (CLI registration, graph node binding/registry, records/journal kinds, schema/resource loading, run sealing, reporting/projection, defaults/init), and the proposed seam. Then propose the registration mechanism(s): e.g. a Gig protocol/manifest that registers CLI groups, node callables (generalize graph_node_registry.py), record kinds, schemas/resources and UI; discovery via Python entry points (`gigai.gigs` group) and/or an explicit bundled-gig list; import-time vs lazy loading; the effect on run.py's child process (registration must happen in the child; see the I-3/fp2 lessons in .orchestrator/runs/v0.1.8/workers/fp2.md). Include an ENFORCEMENT test proposal: an import-linter-style check in tests that fails if any module outside src/gigai/scout imports gigai.scout (and in general any core→gigs import), plus how to phase the removal (order by dependency; which sites move to Scout and which become core seams). Estimate the size per category and a migration sequence with small, parallelizable packets. Risks: pinned/sealed digests or resource paths that embed module paths; installed verifiers; pickle/multiprocessing names; the existing v0.1.7 Scout install path.
S17, gig module structure (classes instead of loose functions): INVENTORY the repeated module families in src/gigai/scout (e.g. documents / documents_cli / document_records, interview / interview_cli / interview_records, proposals / proposal_cli / proposal_records / proposal_execution, report / report_cli / report_readers, research / research_inputs / research_v3, tailoring / tailor_cli / tailor_execution / tailor_selection, …). For each family: files, public function count, shared shapes (records read/write, validation, CLI wiring, execution against models). Identify the duplicated pattern concretely (e.g. N copies of "validate → build record → publish to journal → read back"), with file:line examples. Propose a class-based structure: e.g. per-capability classes (Records repository, Service/Execution, CLI command group) with dedicated methods, or a small set of core base classes/protocols that gigs subclass (which belong in core, since trader/shopper will reuse them, vs in Scout). Show a before/after sketch for ONE family (pseudocode, not implementation). Say explicitly where classes DON'T help (pure functions that should stay functions) and how this interacts with S16 (the core base classes are the registration seams). Include the test impact (the tests/behaviors packages) and a migration path that keeps behavior identical.
S18, Scout interview-prep goal graphs, used by agents as a tool (the operator's idea, captured as a spike, not a decision): "extend Scout with a prep-for-interview workflow: research the role, research the company's interview process and so on, and figure out what they could ask, with probabilities like JEV. The agents (Claude, Codex or whatever) can use gigai Scout to do the research and prepare the person for the interview. That's one set of goal graphs." INVESTIGATE: (1) the goal-graph family: candidate graphs/nodes (e.g. research-role → research-company-interview-process → predict-questions → prep-plan / mock-drill) as Scout graphs under the gig architecture rule, reusing v0.1.8 pieces (pinned resume, posting snapshots, requirements matrix, research/research_inputs, proposals, interview modules already in src/gigai/scout; inventory what exists, with file:line); (2) how agents use Scout as a tool: the options (CLI, the localhost API from v0.1.8 find_jobs.present_api, an MCP server, a Claude Code skill/plugin) with trade-offs, consent and privacy boundaries (the resume/private evidence never leaves without explicit consent; hosted-model network needs the effect + consent like v0.1.8 D5/D11); (3) question prediction with typed probabilities per the JEV research at docs/development/v0.1.8/spikes/evidence/jev-typesafe-ai-structured-decisions-research.md (read it fully): map noul (yes/no probability), choice (per-option probabilities) and score (a rubric) onto interview prediction, and adopt its confidence/abstention contract and frozen-cases + grader approach; propose recording actual interview outcomes to MEASURE calibration, since model probabilities are not calibrated by default; whether to use a local model, a hosted one, or Jev itself (it's a hosted service; flag the privacy/cost implications); (4) sources for company interview-process research: public, terms-respecting sources only (posting text, company engineering blogs/career pages, the operator's own notes); flag scraping/ToS risks for sites like Glassdoor/Blind/LeetCode discuss, and prefer operator-supplied material; (5) the minimal first slice (one graph, one agent path) and its acceptance evidence. All three spikes end with: open questions for the operator, non-claims, and a change log entry dated 2026-09-23.
NOTE: src/gigai/scout/ is being created by a re-org worker that has just finished. If src/gigai/scout/ doesn't exist yet, or old src/gigai/scout_*.py files still exist, STOP and use `orca orchestration ask`; don't inventory a half-moved tree.
ACCEPTANCE: the four docs exist; every table row cites file:line; the S16 inventory includes an AST-based count (paste the script and output into the worker file) and the row count matches; S17 covers every module family found (list any module not assigned to a family). No code edits; no git add/commit. Say READ vs EXECUTED. Write the worker file, then worker_done.
