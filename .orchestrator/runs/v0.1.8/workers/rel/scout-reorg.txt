TARGET (scout-reorg): operator decision 2026-09-23: "scout is its own gig; gigai core should not influence it; scout and its UI belong inside src/gigai/scout; each future gig is contained to its own package." Scope for THIS PR: a physical move plus import updates. Core may keep importing gigai.scout.* for now; removing core→scout imports is 0.2.0 item #1 (don't do it now). No compatibility shims (there are no users).
MOVE (use plain mv; no git add/mv/commit):
- src/gigai/scout_<name>.py → src/gigai/scout/<name>.py (drop the prefix), EXCEPT the find-jobs modules, which go to a subpackage src/gigai/scout/find_jobs/: scout_find_jobs_contracts→contracts, scout_find_jobs_bindings→bindings, scout_exa_client→exa_client, scout_ats_board_clients→ats_board_clients, scout_market_acquisition→market_acquisition, scout_watchlist→watchlist, scout_present_api→present_api. If a new name would collide or confuse (e.g. scout.py, a stdlib-shadowing name, or one needing a better name), pick a clear one and list it.
- The Vite app ui/ → src/gigai/scout/ui/ (move everything except node_modules/ and dist/; don't commit those; delete the old root ui/ after moving). Check the existing src/gigai/data/scout/ui/ (old static v0.1.7 assets): keep it separate from the new Vite app and say what it's used for.
- src/gigai/data/scout/ → src/gigai/scout/data/ ONLY IF resource paths (e.g. "data/scout/tools/cap_…/discovery.py" in scout_discovery.py:22,108, scout_posting_inputs.py:40-41, scout_research_inputs.py, scout_research_v3.py) are NOT baked into sealed/pinned digests, golden hashes, schema-validated manifests or installed-verifier expectations beyond a plain path string. Check first. If moving data would change any pinned digest or golden value other than a path string, DON'T move data: leave src/gigai/data/scout and ask via `orca orchestration ask` with the evidence.
- graph_node_registry.py stays in core (it's generic).
UPDATE: every import in src/, tests/, tools/ (49 test/tool files import gigai.scout_*; 10 core modules import scout: run.py, cli.py, external_recording.py, application_events.py, capability_review.py, capability_successor.py, default_init.py, native_records.py, portability.py, private_transfer.py); lazy imports inside functions; strings that name modules (monkeypatch targets, `python -m gigai.scout_present_api`, multiprocessing entry names, registry/binding strings, env-hook docs); pyproject.toml (packages.find must include gigai.scout and gigai.scout.find_jobs; package-data for moved data; the wheel must NOT include scout/ui/node_modules or dist; keep pytest markers working); Makefile/CONTRIBUTING if they reference paths; the runbook docs/development/v0.1.8/runbooks/M1-find-jobs.md (API command becomes `python -m gigai.scout.find_jobs.present_api`; UI dir becomes src/gigai/scout/ui); the orchestrator commit plan's file lists will be regenerated by the coordinator, so leave it.
Historical planning docs (roadmap/amendment/spike evidence) cite old paths with line numbers: DON'T rewrite them. Add ONE mapping table (old path → new path) to docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md under a "2026-09-23 package re-org" change-log entry, and add "decouple core from gigai.scout (registry/plugin discovery for CLI, graph nodes, records)" as the FIRST 0.2.0 ledger row.
ACCEPTANCE (paste outputs into the worker file):
1. `grep -rn -E "gigai\.scout_|from \.scout_|import scout_|scout_[a-z_]+\.py" src tests tools Makefile pyproject.toml CONTRIBUTING.md docs/development/v0.1.8/runbooks` has only intentional hits (list each; pytest marker names like scout_acquisition are fine).
2. `uv run --locked python -c "import gigai.cli, gigai.run, gigai.scout, gigai.scout.find_jobs.present_api, gigai.scout.find_jobs.bindings"` works, plus an import smoke of EVERY module under src/gigai/scout.
3. `cd src/gigai/scout/ui && yarn install && yarn build` succeeds.
4. `uv run --locked --extra test pytest tests/behaviors -q -n 8` passes (0 failed; if the known g04 two-process-init flake fails, re-run that one test and report both).
5. `uv build` and list the wheel contents: gigai/scout/** present, no node_modules/dist, data present where the code loads it.
Long runs go in a visible tab: `.claude/skills/gigai-orchestrator/run_visible.sh "TEST <name>" <cmd>`.
OWNED: everything the move touches (src/gigai/**, tests/**, tools/**, ui/ → new location, pyproject.toml, Makefile, CONTRIBUTING.md, the runbook, the roadmap change-log/ledger rows). Nothing else is running now: you're the only editor.
EXCLUSIONS: no behavior changes beyond paths/names; no git add/mv/commit/stash/reset/clean; no network except yarn/uv package installs. Say READ vs EXECUTED. Write .orchestrator/workers/scout-reorg.md (full old→new mapping, every non-mechanical decision, all acceptance outputs), then worker_done.
