TARGET (ci-audit): make the release CI green. The v0.1.7 release run failed: https://github.com/karthik446/gigai/actions/runs/35660274375 (use `gh run view 35660274375 --repo karthik446/gigai --log-failed`). The only failed job is "Exact-tag CI / Built-wheel resources", at step `.wheel-venv/bin/python tools/verify_installed_g03.py`, with `installed first setup failed: ` and an EMPTY message; every publish job after it was skipped. A local patch in tools/verify_installed_g03.py already adds stdout/stderr diagnostics.
DO: (1) Reproduce locally the way CI does (read .github/workflows/*.y*ml for the exact Built-wheel resources steps): uv build → a fresh repo-local .wheel-venv (python 3.11; uv-managed 3.11.11 is installed) → install the wheel → run tools/verify_installed_schemas.py, _canonical.py, _cli.py, _g03.py and onward in CI order. `make test-wheel` is now SAFE (tools/run_ci_tests.py was fixed to never follow the .wheel-venv symlink; see tests/behaviors/ci_tooling). Run long commands in a visible Orca tab: `.claude/skills/gigai-orchestrator/run_visible.sh "TEST <name>" <cmd>`. (2) Find the ROOT CAUSE of the g03 first-setup failure (likely `gigai setup` in a clean HOME on the installed wheel: a missing package resource, questionary/TTY, or a path assumption) and fix it at the source. (3) Continue down the whole CI verifier list until every Built-wheel resources step passes locally, then run `make test-installed`. (4) Audit the other workflow jobs for anything that would fail for v0.1.8 (Python 3.11/3.12/3.13 matrix, the Debian 12 offline container, the new files: ui/ must not be packaged into the wheel or break sdist; the Scout find-jobs modules must import on 3.11). Fix the workflows only if needed.
OWNED FILES: tools/verify_installed_*.py, tools/run_ci_tests.py, .github/workflows/*, pyproject.toml (packaging only), and the minimal src/gigai/* change the root cause requires (say which in your report; don't touch src/gigai/scout_present_api.py or ui/, which another worker owns now), tests/behaviors/ci_tooling/*, .orchestrator/workers/ci-audit.md.
ACCEPTANCE: paste the output of every CI Built-wheel resources verifier run locally (all pass), `make test-installed` exit 0, and the root cause with file:line. No network except the package-index downloads uv needs; no pushing; no git add/commit/stash/reset/clean.
Say READ vs EXECUTED. Write .orchestrator/workers/ci-audit.md, then worker_done.
