TARGET (commit-prep): get the branch karthik446/gigai-v0.1.8 ready for commits and a PR against main, WITHOUT committing yet (the coordinator commits after the operator confirms). The operator wants the orchestrator workpad PUBLISHED in the repo, including logs (useful for the 1.9 worktree).
DO:
1. .gitignore: stop ignoring `.orchestrator/` (it's currently ignored). Keep ignoring ui/node_modules, ui/dist, .wheel-venv and any secrets. Also check the skill at .claude/skills/gigai-orchestrator/SKILL.md, which says the workpad is gitignored and never committed: update those lines to say it's committed as the project's coordination record.
2. SECRET SCAN everything that would be committed (`git status --porcelain` including untracked files, and all of .orchestrator/**): Orca dispatch capabilities (`dcap_`), API keys (EXA, OpenRouter, OpenAI, Anthropic, `sk-`, `ghp_`, `gho_`), bearer tokens, private keys, the operator's email or home-dir personal data in logs, and resume content. Redact in place (replace with `[redacted]`) in .orchestrator files only, and list every hit with file:line in your report. If a hit is in a source file, don't edit it: report it.
3. Size check: flag any file over 1 MB and any binary. Trim huge test logs to head plus tail if needed (keep the EXIT lines and summaries), and say so.
4. Write a COMMIT PLAN at .orchestrator/release/commit-plan.md: an ordered list of logical commits (e.g. S11 test reorganization + runner; the v0.1.8 planning docs/roadmap/amendment; find-jobs contracts; runner seam + node registry; acquire; assess; present + API; UI; bindings + M1 e2e + runbook; CI/tooling fixes; the orchestrator skill + workpad), each with its exact file list (every changed/untracked/deleted path assigned exactly once; verify with a script and paste the check) and a conventional commit message. The deleted tests/test_*.py files belong with the S11 behavior-test migration commit.
5. Write the PR body at .orchestrator/release/pr-body.md: summary, what works (with evidence paths), what's unproven (the live M1 click, installed/publish), the known 0.2.0 ledger, and the test counts. End with the line: 🤖 Generated with [Claude Code](https://claude.com/claude-code)
OWNED FILES: .gitignore, .claude/skills/gigai-orchestrator/SKILL.md (only the workpad-publishing lines), .orchestrator/** (redactions + the new release/ files), .orchestrator/workers/commit-prep.md. Two other workers (ci-audit, polish) are editing tools/, .github/, src/gigai/scout_present_api.py and ui/src now: don't touch those; in the plan, put their files in the commits they belong to, since they'll land before committing.
ACCEPTANCE: the secret scan command(s) and results pasted into your report; the plan's file-coverage check shows 0 unassigned and 0 duplicates. No git add/commit/push/stash/reset/clean. Say READ vs EXECUTED. Write the worker file, then worker_done.
