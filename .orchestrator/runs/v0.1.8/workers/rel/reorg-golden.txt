TARGET (reorg follow-up, coordinator decision on your flagged failure): convert the git-history test into a pinned golden-digest test; don't retire it. Rationale: its green run BEFORE the move already proved that the 6 pre-existing Scout graphs' compiled artifacts were byte-identical to pre-I-1 HEAD, so their current canonical digests are the verified baseline. Pinning them keeps the protection permanently, with no git or path dependency.
CHANGE (only that test file, plus a fixture if you prefer a JSON file under tests/behaviors/scout_find_jobs/fixtures/): compute the canonical digest (the repo's canonical helpers) of each of the 6 existing compiled graph artifacts (plus any sibling compiled artifacts the old test compared) from the current materialization, hard-code them (or load them from a fixture) with a comment citing the prior proof (I-1 report .orchestrator/runs/v0.1.8/workers/ and your scout-reorg.md) and the date, and assert the current output matches exactly. Remove the git-show/HEAD mechanism. Keep the test's name meaningful (rename if needed).
ACCEPTANCE: that test passes; `uv run --locked --extra test pytest tests/behaviors -q -n 8` = 0 failed (paste the count). Update your worker report, then worker_done.
