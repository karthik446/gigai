TARGET (polish, from the coordinator's UI dry run). P1: in src/gigai/scout_present_api.py, the GET handlers (config, run status, run results) let an unexpected backend exception escape, so the connection drops ("socket hang up" in the Vite proxy) and the UI hangs on "pending". Catch unexpected exceptions in the GET handlers and return a JSON 500 `{"error": {"code": "internal_error", "message": <redacted, no traceback or paths>}}`, logging the traceback to stderr only. Keep the POST behavior as documented. P2: in ui/src, while the API reports status "running" and there are no node receipts yet, the UI shows "Run status: pending". Show the API's aggregate status as-is, and show nodes as "waiting" until their receipts arrive. Also, when a status poll returns an error, show it and stop polling instead of silently continuing.
OWNED FILES: src/gigai/scout_present_api.py, tests/behaviors/scout_find_jobs/test_present_ui.py, ui/src/*, .orchestrator/workers/polish.md.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_present_ui.py -q` passes, with new tests for the 500 path on each GET route (a fake backend raising RuntimeError, asserting a JSON 500 with no traceback text in the body), and `cd ui && yarn build` succeeds. No git add/commit. Say READ vs EXECUTED. Write the worker file, then worker_done.
