TARGET (C-3 + C-4 combined): build the Scout find-jobs UI: the Vite + React shell plus the real screens. Read the frozen API in src/gigai/scout_find_jobs_contracts.py (ROUTES, API_BIND, RunRequest, UIConsentEnvelope, RunStatusResponse, RunResultsResponse, PresentPayload and nested DTOs) and the API samples in tests/behaviors/scout_find_jobs/fixtures/fixture-api-*-v1.json + fixture-present-payload-v1.json. Don't edit Python.
OWNED FILES (nothing else): ui/package.json, ui/yarn.lock, ui/index.html, ui/vite.config.js, ui/.gitignore, ui/src/main.jsx, ui/src/App.jsx, ui/src/api.js, ui/src/styles.css, ui/src/components/*.jsx, .orchestrator/workers/c34-ui.md
SHELL: package.json is private with "type": "module" and scripts dev/build/preview; react + react-dom, and devDeps vite + @vitejs/plugin-react, all pinned to EXACT versions (no ^ or ~); no other dependencies (no UI kit, router, state or CSS library). vite.config.js: react plugin, server.host 127.0.0.1, server.proxy {'/api': 'http://127.0.0.1:8765'}. .gitignore: node_modules/ and dist/.
SCREENS (one page, plain CSS, readable, works at a narrow width):
1. Header "Scout · find jobs". On load, GET /api/config: show roles, queries, location/remote, published_after, sources, default cap and model target, plus the resume preview (label/record id, or a clear "No resume saved: run `gigai reference add --kind resume ...`" message that disables Run).
2. "Run workflow" button opens a confirm dialog showing EXACTLY what will happen: sources, queries, role filter, cap (a number input 1–50, default from config), model target (a select: ollama_local / codex_cli / openrouter_api, default from config), and a note that acquisition uses the network and hosted targets send posting + resume text to that provider. Confirm ⇒ POST /api/run with the RunRequest (the consent envelope has the fixed values from contracts; invocation_id/occurrence_id from crypto.randomUUID(); config_digest from the config response) ⇒ 202 {run_id}. Map 403/409/422/504 to clear messages (409 = config changed since loaded ⇒ offer a reload).
3. While running, poll GET /api/runs/{id} every 2s: show the three node statuses (acquire/assess/present) and the aggregate status; stop at a terminal status.
4. Results (GET /api/runs/{id}/results): the pinned resume used; a postings table (title, company, location, source, outcome); for each assessed posting an expandable card with the requirements matrix (requirement | resume evidence | met/partial/gap as colored badges), suggestions and questions; a "Not assessed" list with human-readable reasons; failures (redacted) in their own section.
Keep all API calls in src/api.js.
ACCEPTANCE (run and paste the output tails): `cd ui && yarn install` (registry download allowed for this step only), `cd ui && yarn build` succeeds, `grep -n 8765 ui/vite.config.js`, `grep -c '[\^~]' ui/package.json` prints 0. Don't leave dev servers running. If a permission prompt blocks you, stop and say so in your worker file and via `orca orchestration ask`; the operator approves prompts, not you. Say READ vs EXECUTED. Write the worker file, then worker_done.
