TARGET (ci-profiles): replace the boolean `full_matrix` input with explicit CI PROFILES, per the operator: "everyone uses macOS, so that's the one to test at release"; the broad matrix can run nightly and/or after release.
FINDING TO FIX: .github/workflows/compatibility_job.yaml (nightly cron + dispatch) calls pull_request.yaml with `full_matrix: true`, and the previous ci-speed change narrowed full_matrix macOS to 3.12 only, so the nightly job ALSO lost macOS 3.11/3.13. The ci-speed report claimed the nightly still ran the full matrix; that was wrong.
DESIGN (implement): an input `profile` (choice/string: `pr` | `release` | `full`, default `pr`) on pull_request.yaml for workflow_call and workflow_dispatch; drop or alias `full_matrix` (keep the workflow_dispatch UI simple).
- pr (pull_request events): ubuntu-24.04 + Python 3.11 source suite, behavior, wheel (today's PR behavior).
- release (release.yml `ci:` job; BLOCKS publishing): source suite on **macos-latest Python 3.12** AND ubuntu-24.04 Python 3.12, plus behavior and wheel. No Debian offline and no other versions.
- full (compatibility_job.yaml nightly + dispatch, AND a NEW post-release job): macOS + ubuntu × Python 3.11/3.12/3.13, plus the Debian 12 offline container.
- Post-release: add a job to release.yml after `github-release` (non-blocking for publishing, since it runs after) that calls pull_request.yaml with profile: full against the tag's ref, so every release gets the full matrix after it ships; a failure there shows on the release run and means a patch release.
Keep: timeouts, caching, ubuntu pin, concurrency, and the Debian xdist fix from ci-speed. Update the `changes` job logic where it reads full_matrix.
OWNED: .github/workflows/*.y*ml, .orchestrator/workers/ci-profiles.md. Other files are read-only.
ACCEPTANCE: YAML parses (`uvx --from pyyaml python -c ...`; `uvx actionlint` if it runs); a table profile → jobs → runner/python; confirm by reading each file that compatibility_job.yaml → full, release.yml ci → release, and the post-release → full with the tag ref; the PR event → pr. No git add/commit/push (the coordinator commits AFTER the in-flight timing run 35899145345 finishes). No test runs. Say READ vs EXECUTED. Write the worker file, then worker_done.
