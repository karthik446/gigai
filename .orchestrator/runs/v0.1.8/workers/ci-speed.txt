TARGET (ci-speed): cut the tag → published release time (the v0.1.7 release's exact-tag CI took ~1h20m before publishing even started) and keep the PR checks fast, WITHOUT losing coverage that caught real bugs. Target: release CI ≤ 30 min to the publish jobs; PR CI ≤ 15 min.
MEASURE FIRST (evidence table in your report): per-job durations from `gh run view 35660274375 --repo karthik446/gigai --json jobs` (the v0.1.7 release: Debian 12 offline 1h19m; macOS readiness tiers ~1h22m each for 3.11/3.12/3.13; Ubuntu tiers 35–40m; built-wheel resources 17s before failing) and the current PR run `gh run list --repo karthik446/gigai --branch karthik446/gigai-v0.1.8` (built-wheel now passes in 1m57s). NOTE: the workflows have since been restructured by S11 (pull_request.yaml: source-tests matrix via `make test-source`, behavior, wheel, debian-offline when full_matrix; release.yml `ci:` calls pull_request.yaml with full_matrix: true). Read both workflows, containers/debian-offline/Dockerfile (and whatever it runs), Makefile and tools/run_ci_tests.py.
LEVERS (evaluate each and implement the ones that are safe; justify any you reject): (1) matrix: e.g. full Python 3.11/3.12/3.13 on ubuntu, and macOS on ONE version (3.12 or 3.11) since macOS runners are slow and costly; keep the behaviors that are OS-specific covered; (2) xdist on CI: make sure `make test-source` uses the bounded xdist runner with the right cap for GitHub runners (ubuntu 4 vCPU, macOS 3); check it actually parallelizes in CI; (3) caching: astral-sh/setup-uv `enable-cache: true` (cache-dependency-glob uv.lock), and yarn if any job builds the UI (none should need it); (4) the Debian offline container: find out what it runs and why it takes 1h19m (e.g. the full suite serially), then make it run the xdist runner or narrow it to what only an offline read-only container can prove (installed verifiers + an offline smoke), with docker layer caching (buildx gha cache) if useful; (5) `concurrency:` with cancel-in-progress for PR runs; (6) timeouts (`timeout-minutes`) on every job so a hang can't burn an hour; (7) release: make sure the build/smoke/TestPyPI/PyPI jobs are as parallel as their dependencies allow; (8) add a `workflow_dispatch` trigger (with a full_matrix boolean input) to pull_request.yaml so the coordinator can run the release-equivalent full matrix on this branch WITHOUT tagging, to measure; (9) pin runner images to explicit versions if the ubuntu-latest → Ubuntu 26 migration (Oct 19, 2026) warning suggests risk (e.g. ubuntu-24.04), and note it.
OWNED: .github/workflows/*.y*ml, containers/debian-offline/*, the Makefile (CI-facing targets only; don't change what `make test` covers locally), tools/run_ci_tests.py (xdist cap/flags only), .orchestrator/workers/ci-speed.md.
ACCEPTANCE: all workflow YAML parses (`uvx --from pyyaml python -c ...`, or `uvx actionlint` if available); `make -n test-source test-behavior test-wheel` still resolves; a before → projected-after table per job, with the reasoning; the list of anything whose coverage changed (e.g. macOS 3.13 no longer run) with the risk. Test budget: NO full local suites. You may run `uv run --locked --extra test pytest tests/behaviors/ci_tooling -q` if you touch run_ci_tests.py. No git add/commit/push (the coordinator commits, pushes and triggers the full-matrix dispatch to measure). Say READ vs EXECUTED. Write the worker file, then worker_done.
