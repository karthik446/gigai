TARGET (A-1 follow-up, your files only: src/gigai/scout_exa_client.py, tests/behaviors/scout_find_jobs/test_exa_client.py): fix the empty-company issue you flagged. Every Exa row you emit has a board_token (non-ATS URLs are dropped), so set company from the board token, the same way A-3 does in scout_ats_board_clients.py `_company_from_token` (copy that tiny logic; don't import a private helper across modules). Add a test proving every emitted row round-trips via PostingRow.from_json(row.to_json()). ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_exa_client.py -q` passes. Update .orchestrator/workers/a1-exa-client.md, then worker_done.
