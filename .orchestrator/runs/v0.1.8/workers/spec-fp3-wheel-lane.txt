TARGET (fix pack 3): a destructive bug in the wheel test lane. `make test` (test-wheel) corrupted the operator's uv-managed CPython 3.11.14 install. Cause: tools/run_ci_tests.py `_resolve_wheel_python` (~:247-250) calls Path.resolve(), which follows `.wheel-venv/bin/python` (a symlink to ~/.local/share/uv/python/cpython-3.11.14-.../bin/python3.11) into the managed interpreter. `_run_wheel` then sets `virtualenv = wheel_python.parent.parent` (~:290), which is the MANAGED INTERPRETER DIR, and runs `uv venv --allow-existing` there (~:325), overwriting bin/python3.11 with a self-referential symlink loop. It triggers on the second run, once .wheel-venv exists. Log: .orchestrator/logs/074338-test-make-test-pre-m1-2.log (tail).
OWNED FILES: tools/run_ci_tests.py; tests/behaviors/ci_tooling/test_run_ci_tests_wheel_python.py (new; create the dir and an __init__.py if sibling behavior dirs have one); .orchestrator/workers/fp3.md
CHANGE: (1) Never follow symlinks when deriving the venv: make the path absolute relative to the repo root WITHOUT resolving symlinks (os.path.abspath / Path.absolute()), and derive the virtualenv from that unresolved path. (2) Add a hard guard before any `uv venv`/`uv pip install`: the virtualenv dir must be inside the repository root (or an explicitly allowed temp dir), must not be inside ~/.local/share/uv, a pyenv root, /usr or /opt, and if it already exists it must contain a pyvenv.cfg (it must be a venv, not an interpreter install). Otherwise exit non-zero with a clear message, and touch nothing. (3) The same unresolved-path rule for `_run_installed`.
ACCEPTANCE: tests in the new file (pure unit tests on temp dirs; don't run uv or create real venvs) prove: a symlinked .wheel-venv/bin/python pointing outside the repo derives the venv as <repo>/.wheel-venv, not the symlink target; the guard refuses a managed-interpreter-looking dir (e.g. a temp dir shaped like .../uv/python/cpython-x/bin/python3.11 with no pyvenv.cfg); the guard allows a normal repo-local .wheel-venv. Run `uv run --locked --extra test pytest tests/behaviors/ci_tooling -q` and paste it. Do NOT run `make test`, `make test-wheel` or any uv venv command.
EXCLUSIONS: owned files only; no git stash/reset/clean/add/commit; don't touch ~/.local/share/uv (the operator repairs it). Say READ vs EXECUTED. Write the worker file, then worker_done.
