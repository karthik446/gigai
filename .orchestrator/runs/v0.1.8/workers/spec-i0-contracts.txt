TARGET (I-0, wave 1a): write the shared contracts for the Scout find-jobs graph AS CODE. This code is the normative freeze every other packet builds against (roadmap T2 decision). Read first:
- docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3: D1-D11, node receipt/precedence, real node execution, watchlist storage, API server + routes)
- docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md (DAG rows I-0..C-4, fixtures table, M1)
- For house style: src/gigai/canonical.py (EntityPrefix, validate_entity_id, canonical JSON helpers — reuse them, don't reinvent), src/gigai/scout_proposals.py:51-85,366-485 (closed-key validation style), src/gigai/scout_acquisition_records.py:56-99, src/gigai/adapters/factory.py:61-124 (adapter ids).

OWNED FILES (nothing else):
- src/gigai/scout_find_jobs_contracts.py (new)
- tests/behaviors/scout_find_jobs/__init__.py and conftest.py only if the suite layout needs them (check tests/behaviors/* siblings)
- tests/behaviors/scout_find_jobs/test_contracts.py (new)
- tests/behaviors/scout_find_jobs/fixtures/*.json (new)
- .orchestrator/workers/i0-contracts.md

CHANGE: stdlib only at runtime (dataclasses, enum, typing; pydantic is test-extra only, so don't import it). Frozen dataclasses, each with `from_json(obj)` that rejects unknown/missing keys, wrong types and bad enums with a single `FindJobsContractError(code, message)`, and `to_json()`. One canonical serialization + `digest()` (sha256 of canonical bytes), reusing canonical.py helpers if they fit. Define, with every field name/type/optionality explicit:
1. `FindJobsConfig` (D9): roles, merged queries, location/remote, published_after window, source toggles (exa, ats; hiringcafe present but default off / after-M1), default assess cap (int 1..50, default 10), default model target (enum `ollama_local|codex_cli|openrouter_api`, matching factory.py adapter ids).
2. Posting/row types: normalized public posting row (url, normalized_url, provider, board_token|null, company, title, location, published_at|null, content_sha256|null, source_kind `exa|ats|hiringcafe`, query_key), row outcome enum (`new|edited|unchanged|duplicate|failed`), failure row (redacted).
3. `WatchlistEntry` exactly as the amendment's JSON shape (`scout-watchlist:1`).
4. `NodeContext` (run/project/gig ids, graph_id/version, goal slug, manifest digest, operation key, target observation digest, workpad path as str, redeemed-consent ref, model target) — no live objects, so it serializes.
5. `AcquireInput/AcquireOutput`, `AssessInput/AssessOutput` (selected posting identities per D6; pinned resume triple per D7; matrix rows {requirement, resume_evidence[], status met|partial|gap}; suggestions; questions; not-assessed rows with reason enum), `PresentInput/PresentOutput`.
6. Node receipt: status enum `pending|running|complete|failed|blocked|cancelled|interrupted`, `producer {callable, version, actor, model_target, adapter}`, usage block (optional for M1 per the cut list), timestamps, failure. Plus `aggregate_status(statuses) -> str` implementing the precedence `interrupted > failed > blocked > cancelled > running > pending > succeeded` (only all-complete ⇒ succeeded). This pure function is what I-2 and C-1 will call.
7. Node callable protocol: `NodeCallable = Callable[[NodeContext, <Input>], <Output>]` typed per node, and the three capability ids `scout.find_jobs.acquire|assess|present` as constants, with each node's declared effect set (acquire: network_read, credential_use, write_workpad; assess: the same superset declared, narrowed per run to write_workpad for a local target; present: write_workpad) using the existing effect names from src/gigai/schemas/common.schema.json:148-157.
8. Client protocols (typing.Protocol) for A-1 Exa search, A-3 ATS board listing + `parse_board_url(url) -> (provider, board_token) | None` for the four domains (boards.greenhouse.io, job-boards.greenhouse.io, jobs.lever.co, jobs.ashbyhq.com), A-4 normalize_url / diff_url_sets / content_hash, and A-5 add-to-watchlist. Signatures only; take an `httpx.Client` param where network is used, so tests inject MockTransport.
9. Present API: `API_BIND = ("127.0.0.1", 8765)` and a ROUTES table (method, path, request type, response type, status codes) for at least GET /api/config, POST /api/run, GET /api/runs/{run_id}, GET /api/runs/{run_id}/results, with request/response dataclasses, including the UI consent envelope `{schema_version:"1.0", kind:"operator_run_consent", action:"run", actor:{kind:"operator",id:"local-user"}, source:"direct_local_ui_confirm", invocation_id, occurrence_id}` (amendment D5; match run.py:2187-2213's closed field set).
Fixtures (one JSON per name in the roadmap fixtures table: find-jobs-config, acquire-input, watchlist, acquire-batch, assessment, ui-consent, node-receipts, present-payload; `-v1` suffix), each loadable via its dataclass.

ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_contracts.py -q` passes, and the tests prove: every fixture round-trips (from_json → to_json equals canonical input); unknown key / missing key / wrong type / bad enum are each rejected with a stable error code; digest is stable across key order; aggregate_status covers every precedence case, including "complete + running ⇒ running"; parse_board_url handles all 4 domains plus a negative; ROUTES matches the frozen list exactly. Also run `uv run --locked ruff check src/gigai/scout_find_jobs_contracts.py tests/behaviors/scout_find_jobs` if ruff is configured in pyproject (check first). Paste both outputs into the worker file.

EXCLUSIONS: only the owned files; no edits to run.py or any other module; no schema-file changes; no git stash/reset/clean/add/commit; no network/provider/model calls; focused tests only (never make test). Say READ vs EXECUTED. If a contract choice is genuinely ambiguous in the docs, pick the simplest option that serves M1, record it under "Choices made" in the worker file, and continue; use `orca orchestration ask` only for a real blocker.

ON FINISH: write .orchestrator/workers/i0-contracts.md (state, files, test + lint output, choices made, READ vs EXECUTED), then worker_done.
