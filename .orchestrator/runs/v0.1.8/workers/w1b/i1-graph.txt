CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (I-1): author the sealed Scout graph `find-jobs:functional:1` and widen its graph-set ceiling so the existing validators accept it.
OWNED: src/gigai/scout_materialization.py; tests/behaviors/scout_find_jobs/test_integration_graph.py (new)
CHANGE: In scout_materialization.py (read its current graph/graph-set/shared_policy construction around :260-400 first), add the find-jobs graph: three goals (acquire, assess, present), executors `{"kind":"local_capability","capability": <ACQUIRE|ASSESS|PRESENT>_CAPABILITY from the contracts module, ...}`, effects from ACQUIRE_DECLARED_EFFECTS / ASSESS_DECLARED_EFFECTS / PRESENT_DECLARED_EFFECTS (sorted lists), two automatic dependency edges acquire→assess→present on COMPLETE, no branches or recovery, max_parallel_goals 1, failure_policy fail_gig (these are what run.py:3438-3460 requires). Widen ONLY this graph's descriptor ceiling: effect_policy ⊇ those effects, capability_requirements ⊇ the three capabilities, and provider eligibility to include ollama_local, codex_cli and openrouter_api (today it's deterministic only, ~:395). The existing Scout graphs must stay byte-identical: prove it with a digest comparison in the test.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_integration_graph.py -q` passes and proves: the new graph validates with validate_goal_graph and the graph-set validator (src/gigai/graph_set.py:238-262), the edges/effects/capabilities are exact, and existing Scout graph output is unchanged. Also run any existing scout_materialization tests you find via `grep -rl scout_materialization tests/behaviors` (focused) and paste the results.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/i1-graph.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
