CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (C-1): the present node, the payload builder, and the aggregate-status bug fix in report readers.
OWNED: src/gigai/scout_projection.py; src/gigai/scout_report_readers.py; src/gigai/scout_report.py; tests/behaviors/scout_tracking_reporting/test_scout_r3_report.py; tests/behaviors/scout_tracking_reporting/test_scout09_application_events.py
CHANGE: (1) implement `present_node(context: NodeContext, input: PresentInput, *, home_root, target) -> PresentOutput` and `build_present_payload(*, home_root, target, run_id) -> PresentPayload` (rows, failures, assessments, not_assessed, node receipts, pinned resume, status), snapshot-derived and rebuildable, with NO application/tracking mutation. (2) Fix `_run_rows` (scout_report_readers.py:315-337) to use the contracts' aggregate_status precedence instead of "any complete ⇒ succeeded". Add tests to the two owned existing test files for the precedence fix and for present_node/build_present_payload against the fixtures (fixture-present-payload-v1, fixture-node-receipts-v1).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_tracking_reporting/test_scout_r3_report.py tests/behaviors/scout_tracking_reporting/test_scout09_application_events.py -q` passes, including the new cases: complete+failed ⇒ failed, complete+running ⇒ running, all complete ⇒ succeeded, and a payload round-trip via the contracts' PresentPayload.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/c1-present-node.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
