CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (A-1): the Exa discovery client.
OWNED: src/gigai/scout_exa_client.py (new); tests/behaviors/scout_find_jobs/test_exa_client.py (new)
CHANGE: implement the contracts' ExaSearchClient protocol (read its exact signature). POST https://api.exa.ai/search with header `x-api-key` from the environment variable EXA_API_KEY, read at call time (missing ⇒ raise a clear error whose message never includes any key material; never log or store the key). Body: one request per merged query, with numResults (a module constant, 25), includeDomains = the four ATS domains (boards.greenhouse.io, job-boards.greenhouse.io, jobs.lever.co, jobs.ashbyhq.com), startPublishedDate = config.published_after when set. Map results[] {url,title,publishedDate} → PostingRow (source_kind exa, provider/board_token via contracts.parse_board_url, normalized_url via contracts.normalize_url; drop rows whose URL isn't on an ATS domain). HTTP/JSON errors ⇒ redacted FailureRow-compatible errors (no headers or keys in messages). Record in your worker file the Exa API shape you assumed (you cannot verify it live).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_exa_client.py -q` passes, using httpx.MockTransport to assert the request URL/header/body, the mapping, the domain filtering, the missing-key error (with the message checked for absence of the key), and 4xx/5xx/bad-JSON handling.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/a1-exa-client.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
