CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (I-2): make the G14 scheduler run REAL node callables for the sealed find-jobs graph only, add UI consent, newest-resume resolution, run-input sealing, and the corrected aggregate status. You are the sole owner of run.py.
OWNED: src/gigai/run.py; src/gigai/graph_node_registry.py (new); src/gigai/schemas/run-details.schema.json (ONLY if an additive change is truly needed; NodeReceipt.to_goal_details() already targets the existing goal_details shape, so prefer no change); tests/behaviors/scout_find_jobs/test_run_seam.py (new)
CHANGE:
1. graph_node_registry.py (generic, not Scout-named): register(graph_id, graph_version, goal_slug, capability, declared_effects, callable); lookup fails closed. Registration happens at import/bind time (I-3 will register the real Scout callables; your tests register stubs).
2. _validate_scheduler_policy (run.py:3438-3460): keep today's rule for every graph, EXCEPT admit a `local_capability` executor whose (graph_id, version, slug, capability) is registered and whose goal effects ⊆ the registered declared effects. Refuse unregistered capabilities and undeclared effects.
3. _execute_goal (run.py:3546-3561): for a registered goal, build NodeContext and call the callable with the goal's input, persist the output as evidence and a NodeReceipt (use NodeReceipt.to_goal_details() for run-details), keep the target-change interrupt check, and map exceptions to failed with a redacted error. Placeholder goals behave exactly as today. Per-run effect narrowing: for assess with a local model target, the run admits only ASSESS_LOCAL_EFFECTS.
4. Consent (run.py:2187-2213, 280-303): accept source `direct_local_ui_confirm` ONLY when the caller passes an explicit `ui_loopback_verified=True` flag (the API sets it after its peer check); the CLI path is unchanged. Redeemed before allocation as today.
5. `resolve_newest_resume(home_root, target) -> PinnedResume`: newest committed kind=resume via private_records list_imports/list_revisions/read_record + scout_inputs._record_revision (the amendment's resume trace section cites the lines). Raise a clear error when there is none.
6. Public entry `launch_find_jobs_run(*, home_root, target, run_request: RunRequest, config_bytes: bytes, ui_loopback_verified: bool) -> str` that validates that config_bytes' digest == run_request.config_digest, seals the config snapshot + digest, selection cap + rule, model target and pinned resume into the run input (the existing sealing at run.py:218-237 and 2313-2400), and launches the find-jobs graph. C-2's API will call this (wired in I-3).
7. _terminal_status (run.py:3491-3496) uses the contracts' aggregate_status precedence (interrupted is run-level).
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_run_seam.py tests/behaviors/runtime_run_authority/test_g14_scheduler.py -q` passes. test_run_seam proves: a registered stub callable is actually called (its output lands as evidence, not `gigai-offline-ok`); an unregistered capability and an undeclared effect are each refused; a local assess target gets narrowed effects; UI consent is refused without ui_loopback_verified; a config digest mismatch is refused; no resume ⇒ a clear error; aggregate status "complete + running ⇒ running". Also run `grep -rl "from gigai.run import\|gigai.run" tests/behaviors | head -20` and run the 3-5 most relevant existing run tests focused; paste the results.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/i2-runner-seam.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
