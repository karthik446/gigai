CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (A-5): the auto-watchlist record, the only new persisted record type.
OWNED: src/gigai/scout_watchlist.py (new); tests/behaviors/scout_find_jobs/test_watchlist.py (new)
CHANGE: implement the contracts' WatchlistClient protocol plus `list_active(home_root, target) -> tuple[WatchlistEntry, ...]`. Persist WatchlistEntry at `records/scout-watchlist/{watchlist_id}.json` through the SAME journal publish mechanism the existing records use. Read src/gigai/private_records.py:306-326 (`_publish`) and src/gigai/scout_acquisition_records.py (how it writes records/scout-acquisition/...) and reuse the public journal API they use; don't call a private `_publish` across modules if a public helper exists (if none does, ask via `orca orchestration ask` before copying). Operation key `scout_watchlist:{provider}:{board_token}` makes re-adding idempotent (same entry back, no duplicate). No API key, posting body, cursor or assessment state is stored.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_watchlist.py -q` passes, proving: add → list returns it; re-add is idempotent; the entry round-trips via the contracts' WatchlistEntry; a record written by the journal is readable after a fresh process-level reload (use the repo's existing temp-workpad test helpers; find them in sibling tests).
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/a5-watchlist.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
