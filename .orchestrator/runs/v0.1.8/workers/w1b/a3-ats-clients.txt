CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (A-3): public ATS board clients for Greenhouse, Lever and Ashby.
OWNED: src/gigai/scout_ats_board_clients.py (new); tests/behaviors/scout_find_jobs/test_ats_board_clients.py (new)
CHANGE: implement the contracts' ATSBoardClient protocol (read its exact signature). Public endpoints (no auth): Greenhouse GET https://boards-api.greenhouse.io/v1/boards/{token}/jobs?content=true → jobs[] {id,title,absolute_url,location.name,updated_at,content}; Lever GET https://api.lever.co/v0/postings/{token}?mode=json → [] {id,text,hostedUrl,categories.location,createdAt(ms),descriptionPlain}; Ashby GET https://api.ashbyhq.com/posting-api/job-board/{token} → jobs[] {id,title,location,jobUrl,publishedAt,descriptionPlain}. Map each to PostingRow (source_kind ats, normalized_url via contracts.normalize_url, content_sha256 = contracts.content_hash of the posting's text bytes (UTF-8)). Local role filter: a pure function `matches_roles(title, roles) -> bool` (case-insensitive token containment, documented). Use contracts.parse_board_url for URL→(provider, token); don't reimplement it. Redacted errors. Record the API shapes you assumed.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_ats_board_clients.py -q` passes, with MockTransport tests per provider (URL, mapping, content hash), role filter cases, and 404/5xx/bad-JSON handling.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/a3-ats-clients.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
