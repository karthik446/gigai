CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (C-2): the localhost API server the UI talks to.
OWNED: src/gigai/scout_present_api.py (new); tests/behaviors/scout_find_jobs/test_present_ui.py (new)
CHANGE: a stdlib server (http.server.ThreadingHTTPServer; no new dependencies) run with `python -m gigai.scout_present_api [--target PATH]`, binding contracts.API_BIND (127.0.0.1:8765) by default; `serve(bind=...)` accepts another bind for tests. Implement exactly the contracts' ROUTES (method, path, request/response DTOs, status codes). Every request checks that the peer address is loopback, else 403. Dependencies are injected through a `Backend` Protocol defined in this module: `read_config() -> (FindJobsConfig, bytes)`, `resume_preview() -> PinnedResume | None`, `start_run(run_request: RunRequest, config_bytes: bytes) -> str` (I-3 will wire it to run.launch_find_jobs_run with ui_loopback_verified=True), `run_status(run_id) -> RunStatusResponse`, `run_results(run_id) -> RunResultsResponse`. POST /api/run: validate the RunRequest, reject when its config_digest ≠ the current config's digest (409), run start_run in a background thread, return 202 with the run id. JSON errors use the contracts' error codes. A default `__main__` backend may raise "not wired yet" until I-3.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_present_ui.py -q` passes, with the server on an ephemeral port and a fake Backend: each route's happy path against the fixtures (fixture-api-*-v1.json), 404/409/422, the non-loopback peer refused (simulate via the handler's client_address), and malformed JSON.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/c2-present-api.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
