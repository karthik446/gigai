CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (B-1): the assessment proposal shape (requirements matrix, suggestions, questions) and its saved revision.
OWNED: src/gigai/scout_proposals.py; src/gigai/scout_proposal_records.py; tests/behaviors/scout_find_jobs/test_assess_contract.py (new)
CHANGE: extend the existing closed proposal validator (scout_proposals.py:51-85,366-485) with `requirements_matrix[] {requirement, resume_evidence[], status met|partial|gap}`, `suggestions[]` and `questions[]` (keep the existing evidence/question discipline), bounded lengths, no new schema file. Export EXACTLY these two functions for B-2, which is written in parallel and will call them: `parse_assessment_proposal(raw: Mapping[str, object]) -> AssessmentResult` (the contracts DTO; raises the contracts' FindJobsContractError on invalid model output) and `save_assessment_revision(*, home_root, target, posting: SelectedPosting, result: AssessmentResult, producer: Producer, pinned_resume: PinnedResume) -> str` (returns a revision ref, via the existing proposal-records revision mechanism, scout_proposal_records.py:146-188,356-437). Old proposal records must still read, so version the method/shape rather than coercing.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_assess_contract.py tests/behaviors/scout_proposals_tools/test_scout_proposals.py tests/behaviors/scout_proposals_tools/test_scout_proposal_records.py -q` passes, with valid/invalid matrix cases, bounds, the save → read round trip, and the old records still readable.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/b1-assess-matrix.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
