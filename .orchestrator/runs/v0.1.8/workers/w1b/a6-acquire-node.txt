CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (A-6): the acquire node, which orchestrates the clients and saves each run as an existing acquisition batch.
OWNED: src/gigai/scout_market_acquisition.py (new); src/gigai/scout_acquisition_records.py; src/gigai/scout_acquisition_cli.py; tests/behaviors/scout_find_jobs/test_acquire_network.py (new)
CHANGE: implement `acquire_node(context: NodeContext, input: AcquireInput, *, http_client, exa: ExaSearchClient, ats: ATSBoardClient, watchlist: WatchlistClient, home_root, target) -> AcquireOutput` (the keyword deps get bound by I-3 with functools.partial, so the registry sees the (context, input) signature). Rule: if input.rows is non-empty, those supplied rows are used as-is (test/supplied mode); otherwise fetch live: (1) if config.sources.exa: Exa search → rows whose (provider, board_token) are auto-added to the watchlist with first_seen provenance; (2) if config.sources.ats: for every active watchlist board, ats.list_board → rows; (3) keep only rows matching config.roles (A-3's matches_roles, or a local equivalent if A-3 hasn't landed: import lazily behind the protocol); (4) store the batch through the EXISTING acquisition-records service (immutable input + digest, dedup; read scout_acquisition_records.py first, and extend it only additively); (5) diff vs the prior batch for the same query_key with contracts.diff_url_sets over URL→content digest; (6) select new/edited + role-matched rows, cap = input.selection_cap, rule = input.selection_rule → selected_postings, and give every other candidate its reason; (7) return a valid AcquireOutput. Per-source failures become FailureRows; the node fails only if every enabled source fails. The clients are injected protocols: don't import A-1/A-3/A-5 modules at top level (they're being written in parallel); tests use fakes.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_acquire_network.py tests/behaviors/acquisition/test_public_acquisition_lifecycle.py tests/behaviors/acquisition/test_public_import_state.py -q` passes, with tests for the supplied-rows mode, the live mode with fake clients, the watchlist auto-add, dedup across two runs, an edit detected by content hash, cap enforcement with over_cap reasons, and a partial source failure.
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/a6-acquire-node.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
