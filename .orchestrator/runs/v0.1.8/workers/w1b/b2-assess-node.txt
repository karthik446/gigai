CONTEXT: Scout find-jobs graph, wave 1b. The contract authority is src/gigai/scout_find_jobs_contracts.py (the frozen I-0 code: import from it, never redefine or edit its DTOs). The design is in docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3); the DAG and ownership are in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Fixtures: tests/behaviors/scout_find_jobs/fixtures/*.json (read-only for you). Other workers are editing other files in this same worktree at the same time: touch ONLY your owned files. If you need a change to the contracts module or to another packet's file, don't make it; use `orca orchestration ask` with the exact change you need.
TARGET (B-2): the assess node: model target choice, cap, pinned inputs, hosted network policy.
OWNED: src/gigai/scout_proposal_execution.py; src/gigai/scout_tailor_selection.py; tests/behaviors/scout_find_jobs/test_assess_model_policy.py (new)
CHANGE: implement `assess_node(context: NodeContext, input: AssessInput, *, home_root, target, config) -> AssessOutput`. Replace the Ollama-only gate (scout_proposal_execution.py:143-146) with the explicit input.model_target ∈ {ollama_local, codex_cli, openrouter_api} resolved via adapters/factory.py; a missing target or credential fails loudly; no fallback ever. Build InvocationPolicy (current construction at :206-223): local ⇒ local_allowed=True, network_allowed=False; hosted ⇒ network_allowed=True, offline=False (model_execution.py:74-86,241-246). Read the pinned resume bytes by exact record/revision (no latest lookup) and each selected posting's text from its acquisition batch record. One model call per selected posting (≤ cap) with a prompt you write (asking for JSON matching the matrix/suggestions/questions shape), then parse via B-1's `parse_assessment_proposal(raw)` and save via B-1's `save_assessment_revision(...)`. Those exact names are frozen; import them lazily, since B-1 is being written in parallel, and your tests may monkeypatch them. Model unavailable/denied ⇒ not_assessed rows with model_unavailable/model_denied, not a crash. Producer/usage go in the output.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_assess_model_policy.py tests/behaviors/scout_proposals_tools/test_scout_proposal_execution.py -q` passes, proving: local ⇒ network_allowed False; hosted ⇒ True; an unknown target or missing credential ⇒ loud failure with no fallback; cap respected; a denied/unavailable model ⇒ not_assessed reasons; the exact pinned resume revision is used (with the adapter faked via the existing deterministic adapter or MockTransport).
EXCLUSIONS: owned files only; no schema/storage migration; no git stash/reset/clean/add/commit; no live network, provider or model calls (all HTTP goes through an injected httpx.Client, and tests use httpx.MockTransport); focused tests only, never `make test`. Say what you READ vs EXECUTED.
ON FINISH OR QUESTION: write .orchestrator/workers/b2-assess-node.md (state, files, exact test output, choices made, READ vs EXECUTED), then worker_done. Questions go through `orca orchestration ask`, never a local prompt.
