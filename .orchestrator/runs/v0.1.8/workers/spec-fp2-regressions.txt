TARGET (fix pack 2): the full `make test` source lane found 5 failures after the find-jobs work (log: .orchestrator/logs/065645-test-make-test-pre-m1.log; read the failure sections). Find the ROOT CAUSE of each before changing anything. The existing behavior of non-find-jobs graphs must be exactly preserved; that was a design promise (D11: "every other graph keeps today's rule").
FAILURES:
F1 tests/behaviors/scout_proposals_tools/test_scout02_acceptance_negative_paths.py::test_both_graphs_run_independently_in_one_approved_gig, and
F2 tests/behaviors/scout_proposals_tools/test_scout02_graph_set_flow.py::test_two_graph_propose_approve_plan_history_and_run: launch_run(wait=True) on existing Scout graphs now ends 'interrupted' instead of 'succeeded' (interrupted = the run child exited non-zero, run.py ~846-870). Suspects: I-3's child-process wrapper/re-registration in src/gigai/scout_find_jobs_bindings.py (it wraps I-2's worker entry), I-2's _execute_goal/_validate_scheduler_policy changes in src/gigai/run.py, or I-1's new graph in the shared graph set (src/gigai/scout_materialization.py). Reproduce, capture the child's actual exception (e.g. temporarily print the child traceback), and fix at the source. Registration/bindings must be a no-op for runs of other graphs and must not require find-jobs env/config to exist.
F3 tests/behaviors/scout_proposals_tools/test_scout05_bundled_tools.py::test_candidate_copies_bundled_tool_assets_and_pins_pending_manifest_to_real_goals: expects 1 goal and now sees 3 (the find-jobs-functional goals). Decide from the test's intent: if the pending manifest should NOT include the functional graph's goals, fix the materialization; if the approved design legitimately adds them, update the test's expectation narrowly and explain in the worker file why the assumption changed. Don't loosen unrelated assertions.
F4 tests/behaviors/integrity_canonical/test_canonical_ownership.py::test_canonical_module_owns_all_product_sha256_implementation: src/gigai/scout_market_acquisition.py imports hashlib and calls sha256. Replace it with the canonical helpers (canonical.py / the contracts' content_hash or digest).
F5 tests/behaviors/scout_find_jobs/test_m1_end_to_end.py: httpx ReadTimeout under 14-way xdist load. Make it robust: longer client timeouts in the test and/or polling with a generous deadline. If it must not run in parallel, use the repo's existing serial marker or lane mechanism (grep conftest/pyproject for how other heavy tests do it); don't skip it.
OWNED FILES: src/gigai/run.py, src/gigai/scout_find_jobs_bindings.py, src/gigai/scout_materialization.py, src/gigai/scout_market_acquisition.py, tests/behaviors/scout_find_jobs/test_m1_end_to_end.py, tests/behaviors/scout_proposals_tools/test_scout05_bundled_tools.py (F3 only, if justified), .orchestrator/workers/fp2.md. Nothing else runs in parallel right now, so you're the only editor.
ACCEPTANCE: each of the 5 tests passes individually; then `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs tests/behaviors/scout_proposals_tools tests/behaviors/integrity_canonical tests/behaviors/runtime_run_authority -q -n 8` passes (use xdist to reproduce the load); paste the counts. The coordinator then re-runs the full make test.
EXCLUSIONS: no test weakening except as F3 allows; no live network; no git stash/reset/clean/add/commit. Say READ vs EXECUTED, and state the ROOT CAUSE of F1/F2 explicitly in the worker file. Write the worker file, then worker_done.
