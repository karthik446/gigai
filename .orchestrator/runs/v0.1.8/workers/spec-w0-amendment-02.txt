TARGET: Write docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md, which makes the Phase 3 find-jobs graph (acquire -> assess -> present, COMPLETE edges) functional per the operator decisions below, and freezes packet contracts so four implementation packets can run in parallel. Build on P3-scout-graph-integration-trace.md (same dir) and Amendment 01; do not repeat them, cite them.

OPERATOR DECISIONS (2026-09-22, record verbatim in intent):
D1 Acquire = query-based market search, no hand-picked companies.
 - Discovery via Exa search API (key from env EXA_API_KEY only; fail loudly if missing; never log/store the key). Use a publish-date filter, a domain filter for boards.greenhouse.io / jobs.lever.co / jobs.ashbyhq.com, merged related-role queries, and automatic backoff (a query with 0 new URLs for 3 runs: 4h -> 12h -> 24h).
 - HiringCafe via its robots-allowed public sitemaps ONLY (jobs-sitemap.xml, job-posting-sitemap.xml, priority-jobs-sitemap.xml, /job/ pages). Never call hiring.cafe /api/search-jobs. You may fetch ONE sitemap once to record what fields a posting exposes (board/company/ATS link?); no crawling.
 - Auto-built watchlist: every discovered company with a Greenhouse/Lever/Ashby board token is added automatically (source + first-seen provenance). This is the only new persisted record type allowed; justify its minimal shape.
 - Hourly polling of watchlisted boards via their free public APIs; filter locally against the operator's roles (5 roles expected).
 - Every search run is stored as an existing acquisition batch (immutable input + digest, scout_acquisition_records.py). Change detection = normalized URL set (tracking params stripped) vs the previous batch for the same query key; content hash at fetch detects edited postings; only unseen URLs are fetched.
D2 Assess: explicit per-run model target among EXISTING adapters (adapters/factory.py): ollama_local (default), codex_cli (Luna, already configured), openrouter_api (key added later; must fail loudly without one). Replace the hard Ollama-only gate at src/gigai/scout_proposal_execution.py:143-146 with an explicit per-run choice. No silent fallback ever; producer/model identity goes in the node receipt.
 - Assessment output must include: a requirements x latest-resume matrix (requirement, resume evidence, met/partial/gap), suggestions, and questions. Keep it inside the existing untyped assessment `proposal` object, validated in code; a new schema file only if you justify why code validation cannot work. Trace how the "latest resume" is selected today (scout_tailor_selection.py, document-selection records) and state the rule.
D3 Present/UI: Vite + React run with `yarn dev` in a new top-level ui/ dir, plus a localhost-only Python API (check pyproject.toml deps first; prefer stdlib or existing deps). A "Run workflow" button runs one graph traversal (replaces cron for now). Show acquisition rows/failures, assessment matrix, suggestions, questions, per-node status. This overrides the v0.1.8 README "no new frontend" line; record that.
D4 Accept the P3 trace's recommended defaults for its open decisions 2-9 (new Run continuation, additive run-details node receipt, graph binding as specified, aggregate status precedence, explicit unavailable-usage). EXCEPTION: acquisition now uses the network, so define the acquire node's capability/effect and the direct operator consent explicitly without weakening existing scheduler checks.

CHANGE: The amendment must contain: decisions; one packet table (Integration seam, A acquire, B assess, C present+UI) with EXACT disjoint owned file lists; callable signatures and node input/output contracts; the watchlist record shape; acceptance per packet as exact focused test commands (uv run --locked --extra test pytest <paths> -q), never full make test; open risks; non-claims. Target <= 250 lines, tables over prose.

ACCEPTANCE: the file exists, every code claim cites file:line you actually read, owned-file lists are disjoint, and every packet has an exact acceptance command.

EXCLUSIONS: documentation only, no source/test/schema edits; do not edit any other doc; no git stash/reset/clean/add; no live provider or Exa calls (the single HiringCafe sitemap fetch is the only network read allowed); do not run the full test suite (the operator is running it). Say what you READ vs EXECUTED.

ON FINISH OR QUESTION: also write .orchestrator/workers/w0-amendment-02.md with state (done/blocked), the output path, any question for the operator, and READ vs EXECUTED.
