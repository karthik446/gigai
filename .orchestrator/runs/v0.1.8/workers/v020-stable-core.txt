TARGET (v020-stable-core): create docs/development/v0.2.0/ as a DISCUSSION space (not a plan, not a decision) for what it takes to make GigAI's core a stable, library-like foundation that gigs build on. The operator (2026-09-23), verbatim in intent: "0.2.0 should be a stable gig core. After 0.2.0 we use versioning 0.2.1 and so on, so we ship versions sooner. We need to figure out how to build this like a library. It's a 0.2.0 folder: spike, work and discussion. We don't know anything yet on how to stabilize. I mean we do, but there's a lot of core and a lot of cleanup to do before we can call it stable." Not a blocker for v0.1.8.
FILES (owned, nothing else): docs/development/v0.2.0/README.md (purpose; the versioning intent: 0.2.0 = stable Gig core, then frequent 0.2.x releases; the gig architecture rule, verbatim from docs/development/v0.1.9/README.md; how v0.1.9's S16–S19 feed into it; open questions); docs/development/v0.2.0/spikes/README.md; docs/development/v0.2.0/spikes/S20-stable-core-as-a-library.md; .orchestrator/workers/v020-stable-core.md.
S20 (ticket format per docs/development/v0.1.8/spikes/README.md; Status / Problem / Intended change (proposed, not decided) / Tasks / Acceptance / Investigate / Open questions / Non-claims / Change log) INVESTIGATES, with file:line evidence from src/gigai (core = everything outside src/gigai/scout):
1. The public API surface today: what gigs actually use from core (derive it from src/gigai/scout's imports of core: AST inventory), vs what's internal. Propose a stable surface (e.g. a `gigai.api` / `gigai.sdk` namespace or explicit `__all__`) and what becomes private (`_`-prefixed or an internal package).
2. Stability contracts: semver policy for 0.2.x (what's allowed in a patch vs minor while below 1.0), deprecation policy (warnings, one-minor grace), the CLI as an API (flags/JSON output stability; CLI errors print JSON on stdout), on-disk formats (journal/records/schemas: versioning and migration; 82 schemas; the S14 schema-inventory spike in docs/development/v0.1.8/spikes/), and the plugin/registration interface from S16 as the gig-facing contract.
3. Cleanup inventory before "stable": size of core (LOC per module; run.py ~5k lines), duplicated helpers (S17 found research.py/research_v3.py duplication), legacy/versioned modules (`*_v2`, `*_v3`), dead code candidates (list them; don't delete), schemas that could merge. Rank by risk to stability.
4. Library packaging: typed API (py.typed, mypy/pyright strictness on the public surface), docs for gig authors (a "write your first gig" guide outline), an example minimal gig to prove the API (e.g. a trivial "hello gig" used in tests), how gigs get distributed (in-repo vs separate packages via the entry points from S16), and compatibility testing (a gig built against 0.2.0 still works on 0.2.x).
5. Release cadence and mechanics for frequent 0.2.x: what makes a release cheap (the CI speed work: .orchestrator/runs/v0.1.8/workers/ci-speed.md; annotated tags; changelog discipline; release_check.py), and what gates remain.
6. A proposed PATH from 0.1.9 → 0.2.0 as themes, not dates, marked "for discussion".
Keep it honest: separate what's known (with evidence) from what's unknown; list the operator's decisions needed. Target ~250–400 lines for S20; the README is brief.
Test budget: none (docs only; AST/LOC scripts are fine). No code edits; no git add/commit. Say READ vs EXECUTED. Write the worker file, then worker_done.
