TARGET (commit-plan-v2): regenerate the release commit plan and PR body for the FINAL tree. Since the first plan (.orchestrator/runs/v0.1.8/release/commit-plan.md): all Scout code moved into src/gigai/scout/ (find_jobs/ subpackage, ui/, data/); CI/tooling fixes landed (verify_installed_g03/g22, release.yml, run_ci_tests.py guard); polish (API 500s, UI waiting states); a golden-digest graph test; docs/development/v0.1.9/ (S16–S19 spikes); and .orchestrator/ was reorganized (README.md, runs/v0.1.8/ archive, helper scripts moved into .claude/skills/gigai-orchestrator/).
DO: (1) Rewrite .orchestrator/runs/v0.1.8/release/commit-plan.md as ordered, disjoint, logical commits covering EVERY path in `git status --porcelain --untracked-files=all`, including deletions (old tests/test_*.py, old src/gigai/scout_*.py, old src/gigai/data/scout/**, old root ui/ if tracked). Suggested order: S11 test reorg + runner; v0.1.8 planning docs; find-jobs contracts; core runner seam + graph_node_registry; the Scout package move (src/gigai/scout_* → src/gigai/scout/, data, tests' imports) as ONE commit so renames are detected; acquire; assess; present + API; UI; bindings + M1 e2e + runbook; CI/tooling fixes; polish; docs v0.1.9 spikes; orchestrator skill + workpad. Where a file belongs to several (e.g. a moved module later edited), put it in the latest commit that touches it and note it. Use the existing .orchestrator/runs/v0.1.8/release/verify_coverage.py (update it if needed) and paste its output: 0 unassigned, 0 duplicates. Each commit gets a conventional message ending with the line `Co-Authored-By: Claude Opus 5.5 <noreply@anthropic.com>`.
(2) Rewrite .orchestrator/runs/v0.1.8/release/pr-body.md with the FINAL, verified numbers ONLY from these sources (don't use the old S11 numbers): final make test at .orchestrator/runs/v0.1.8/logs/111655-test-make-test-final-post-reorg.log (source lane 1885 passed / 0 failed / 1 skipped; 23 installed verifiers + 38 installed tests pass; EXIT 0; read the log to confirm). What works: find-jobs graph end to end offline (test_m1_end_to_end), the UI dry run in Chrome, and the v0.1.7 release-blocker root cause fixed (g03 + release.yml). What's unproven: the live M1 click (the operator will test from the published v0.1.8), the full CI matrix run. The release flow: merge → `git tag v0.1.8 && git push origin v0.1.8` → release workflow (~1h45m). Known follow-ups: the 0.2.0/v0.1.9 ledger (core↛gig decoupling S16, classes S17, interview prep S18, test diet S19), the g04 two-process init flake. Include the architecture rule (gigs import core; core never imports a gig). End with: 🤖 Generated with [Claude Code](https://claude.com/claude-code)
(3) Secret scan the final tree (dcap_, API keys, sk-/ghp_/gho_, private keys, and the operator's email — it starts "[operator-email]"): report hits; redact only inside .orchestrator/**.
OWNED: .orchestrator/runs/v0.1.8/release/*, .orchestrator/workers/commit-plan-v2.md. No git add/commit/push/stash/reset/clean. No test runs needed. Say READ vs EXECUTED. Write the worker file, then worker_done.
