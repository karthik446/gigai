TARGET: Write a NEW roadmap, docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md, whose goal is to get the Scout find-jobs graph (acquire -> assess -> present, COMPLETE edges) working end to end from a UI "Run workflow" button, decomposed into SMALL, well-defined packets that cheaper agents can run in parallel. Do NOT rewrite the old roadmap.

OWNED FILES (nothing else):
1. NEW docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md
2. docs/development/v0.1.8/roadmaps/v0.1.8-scout-search-first-roadmap.md: ONLY insert, directly under the H1, a short block: "**Status: Obsolete (2026-09-22).** Superseded by [v0.1.8 find-jobs functional roadmap](v0.1.8-find-jobs-functional-roadmap.md). Kept for history; do not plan from it." Change nothing else in that file (it has uncommitted work).
3. docs/development/v0.1.8/README.md: ONLY re-point the "Release roadmap" section to the new roadmap (old one listed as obsolete), and amend the line near :65 about "no new frontend" to say the operator overrode it on 2026-09-22 for a local Vite UI (link the new roadmap). Nothing else.
The phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md file is owned by ANOTHER worker who is writing it now. Do not create or edit it.

INPUTS TO READ: the old roadmap (history, gates and non-claims worth keeping), phase-2/evidence/P3-scout-graph-integration-trace.md, P2-FREEZE-04 Amendment 01, the phase-2 tickets, spikes/README.md, and the operator decisions below. Amendment 02 is the contract source for the four packets (Integration seam, A acquire, B assess, C present+UI). If it exists by the time you write the packet section, align with it exactly. If not, cite it by path and add an open item "reconcile with Amendment 02", and do not invent contracts it will own.

OPERATOR DECISIONS (2026-09-22):
D1 Acquire = query-based market search, no hand-picked companies: Exa discovery (EXA_API_KEY from env, fail loudly), HiringCafe via robots-allowed public sitemaps only (never /api/search-jobs), an auto-built watchlist of Greenhouse/Lever/Ashby boards (the only new persisted record type), hourly polling of those boards' free public APIs filtered against ~5 roles, each search run stored as an existing acquisition batch, with change detection by normalized URL set and a content hash at fetch.
D2 Assess: explicit per-run model target from existing adapters (ollama_local default, codex_cli Luna, openrouter_api once a key exists), with no silent fallback. The output carries a requirements x latest-resume matrix (met/partial/gap), suggestions and questions, inside the existing assessment proposal object.
D3 Present: Vite + React UI in a top-level ui/ dir (`yarn dev`) plus a localhost-only Python API, and a "Run workflow" button that runs one traversal (in place of cron for now). It shows acquisition rows and failures, the matrix, suggestions, questions and per-node status.
D4 Accept the P3 trace defaults; the acquire node's network effect needs explicit operator consent.
D5 Implementation proceeds now by the operator's go-ahead. PUBLISHING v0.1.8 still requires v0.1.7 to actually ship; keep that gate and don't claim v0.1.7 shipped.
Later, out of scope here: an interview-preparation goal graph (the second Gig graph), cron scheduling, OpenRouter key setup.

REQUIRED CONTENT (tables over prose, <= 250 lines):
- Goal and done-definition: the operator clicks Run workflow, and real postings flow through assess to a visible matrix. State the narrowest proofs: source tests, installed wheel, one live run with operator consent.
- A packet DAG: break the four Amendment-02 packets into small work items (roughly 1-5 owned files, one callable contract, one focused test command each). Each item row has: id, title, depends-on, owned files (disjoint across ALL rows), contract/interface consumed and produced, acceptance as an exact command (uv run --locked --extra test pytest <paths> -q, or `cd ui && yarn build` / a yarn test for UI), and a suggested route using the cheapest capable model: claude-haiku-4-5 for mechanical or small, claude-sonnet-5 for well-specified implementation or UI, gpt-5.6-luna medium for moderate logic, gpt-5.6-luna max only for the integration seam and cross-cutting logic, gpt-5.6-terra medium for review.
- Waves: which items run in parallel in each wave, the gate between waves (Terra review plus the coordinator's focused re-run), and where the coordinator's full `make test` runs (TEST tab, once per wave end, not per item).
- Stubs and fixtures that let items run in parallel before their producers land (e.g. a fixture acquisition batch, a stub assessment result), so no item waits on another's code unless it is listed in depends-on.
- Gates kept from the old roadmap that still apply (privacy, restart/no-duplicate, model-off visibility, no silent hosted fallback, installed proof, v0.1.7 prerequisite for publish). Drop what no longer applies and say so in one line each.
- Open risks and non-claims. A change log with a single entry for 2026-09-22.

ACCEPTANCE: the new file exists; the old roadmap diff is only the inserted block (check with `git diff -U0 -- docs/development/v0.1.8/roadmaps/v0.1.8-scout-search-first-roadmap.md` and paste the output into your worker file); the README diff touches only the two spots; owned-file lists in the DAG are disjoint (list any overlap you find); every code claim cites a file:line you actually read.

EXCLUSIONS: documentation only, no source/test/schema/ui code; no git stash/reset/clean/add/commit; no network or provider calls; don't run the test suite. Say what you READ vs EXECUTED.

ON FINISH OR QUESTION: also write .orchestrator/workers/w0c-roadmap.md with state (done/blocked), output paths, the git diff -U0 output for the old roadmap, any question for the operator, and READ vs EXECUTED.
