TARGET (release-docs): three things for the v0.1.8 release PR.
(1) VERSION + RELEASE CHECKS: bump the project version to 0.1.8 everywhere it must match (pyproject.toml [project].version; `uv lock` so uv.lock's own-package version matches, using --locked-compatible steps; any __version__ or version constant in src/gigai; tests/tools that pin the version string). Read tools/release_check.py fully (`verify-tag` and anything else release.yml or pull_request.yaml calls) and make the repo satisfy EVERY check for tag v0.1.8. Report the list of conditions and how each is met (e.g. an annotated tag is required: release.yml:31-33 checks `git cat-file -t refs/tags/<tag>` = tag; a CHANGELOG entry format; the version equals the tag). Add the v0.1.8 entry to CHANGELOG.md in its existing format: Scout find-jobs (acquire via Exa + Greenhouse/Lever/Ashby with an auto watchlist → assess with a requirements × resume matrix, suggestions and questions, local-model default and explicit hosted targets → present via a localhost API + Vite UI with a consent dialog); the Scout package move to gigai.scout; the S11 behavior test layout + make test runner; the release CI fixes (the setup verifier/workflow model target); the wheel-lane interpreter safety guard.
(2) README.md REWRITE: it is ALSO the PyPI long description (pyproject readme = "README.md"), so: absolute GitHub links only (https://github.com/karthik446/gigai/blob/main/...), no relative links, no images that won't render. SIMPLE and BRIEF (aim for ~120–180 lines). Sections: what GigAI is (a local, user-controlled agent runtime; a Gig = a self-contained goal-graph package built ON GigAI; the rule: gigs import core, core never imports a gig) · Scout, the first Gig (find-jobs: what it does, what stays local, consent before network or hosted models) · Install (pip / uv tool, Python ≥3.11) · Quickstart for humans (short) · **For agents** (the operator: "it's mostly agents doing this, such as Claude or Codex, so give ACCURATE instructions and setup"): exact, copy-pasteable, non-interactive commands for setup (`gigai setup --non-interactive …` with endpoint/model-target flags), binding a project, installing and approving Scout, `gigai reference add --kind resume …`, the find-jobs.json schema with an example, EXA_API_KEY, starting the API (`python -m gigai.scout.find_jobs.present_api --home … --target …`), the fact that **the UI is not in the wheel: it runs from a source checkout (`src/gigai/scout/ui`, `yarn dev`)**, where run outputs/receipts live, how to read failures, and the exit/JSON conventions (CLI errors print JSON on stdout). Plus: Development (uv, `make test`, focused tests, layout src/gigai core vs src/gigai/scout gig, `.orchestrator/` = coordination record), Status (v0.1.8, what's proven, what isn't), License. Take the current README's still-true essentials; drop marketing and stale "example Gigs" that don't exist. VERIFY EVERY COMMAND against the real CLI: run `uv run --locked gigai <cmd> --help` for each command you document and paste the evidence into your worker file; cross-check with docs/development/v0.1.8/runbooks/M1-find-jobs.md (keep the two consistent: if the runbook is wrong, report it; don't edit it). Check that the long description renders: `uvx twine check dist/*` after `uv build` (build into a temp outdir, not dist/ if that would clobber anything important).
(3) Report the EXACT release commands for the operator (annotated tag), e.g. `git tag -a v0.1.8 -m "GigAI v0.1.8" && git push origin v0.1.8`, confirmed against release.yml and release_check.py.
OWNED: pyproject.toml (version only), uv.lock (lock refresh only), CHANGELOG.md, README.md, any version constant file in src/gigai and version-pinning tests/tools you find (list them), .orchestrator/workers/release-docs.md. Test budget: ONLY the tests that pin the version or read the README/CHANGELOG (find them; exact selectors), plus `uv run --locked python tools/release_check.py <its local-check subcommand, if any>`. No make test; no git tag/add/commit/push. Say READ vs EXECUTED. Write the worker file, then worker_done.
