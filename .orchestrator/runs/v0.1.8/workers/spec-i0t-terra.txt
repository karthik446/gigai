TARGET: Independent READ-ONLY code review of I-0, the shared contracts that every wave-1b packet will build against, so interface mistakes found now are cheap. Files: src/gigai/scout_find_jobs_contracts.py, tests/behaviors/scout_find_jobs/test_contracts.py, tests/behaviors/scout_find_jobs/fixtures/*.json, plus the worker report .orchestrator/workers/i0-contracts.md. Authority: docs/development/v0.1.8/phase-2/evidence/P2-FREEZE-04-amendment-02-find-jobs-functional.md (Rev 3) and the roadmap DAG in docs/development/v0.1.8/roadmaps/v0.1.8-find-jobs-functional-roadmap.md. Coordinator notes (don't re-report them): .orchestrator/reviews/i0-coordinator.md.

OWNED FILES (nothing else): .orchestrator/reviews/terra-i0.md; .orchestrator/workers/i0t-terra.md

REVIEW FOR:
1. Can each downstream row build against this without inventing shapes? Walk I-1, I-2, A-1, A-3, A-5, A-6, B-1, B-2, C-1, C-2 and C-4 and name any field or type a row will need that is missing, ambiguous or wrongly typed. Include how present/UI gets matrix rows, not-assessed reasons, per-node statuses and the pinned resume identity; how acquire output feeds the assess selection; and how NodeReceipt maps onto run-details.
2. Does it match the amendment (D1–D11, consent envelope, effects, routes, watchlist shape)? List mismatches with file:line.
3. Test strength: does test_contracts.py actually reject drift (unknown/missing/wrong-type/bad-enum per DTO, or only a sample)? Re-run it: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_contracts.py -q`.
4. Correctness bugs in the helper functions (normalize_url, parse_board_url, diff_url_sets, aggregate_status, digest).
OUTPUT: a table (id | severity blocker/major/minor | file:line | finding | fix), then the verdict: "freeze as-is", "freeze after listed fixes", or "rework".
EXCLUSIONS: read-only; no edits outside the owned files; no git stash/reset/clean/add; no network; focused test only. Say READ vs EXECUTED. Write the worker file, then worker_done.
