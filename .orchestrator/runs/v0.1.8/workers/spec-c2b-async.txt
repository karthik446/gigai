TARGET (C-2 follow-up): POST /api/run must not block on the whole traversal. Today the handler joins the start_run thread (your own flagged choice), so a real run (minutes of acquire + assess) would hang the request.
CHANGE (your files only: src/gigai/scout_present_api.py, tests/behaviors/scout_find_jobs/test_present_ui.py): change the Backend protocol to `start_run(run_request, config_bytes, on_run_allocated: Callable[[str], None]) -> None`. The backend runs the whole traversal and calls on_run_allocated(run_id) once the run exists (I-2's launch_find_jobs_run gets the same callback; I-3 wires them together). The handler starts a daemon thread calling start_run, waits on a threading.Event set by on_run_allocated (timeout 30s), then returns 202 {run_id}. If start_run raises BEFORE allocation, return the mapped error (FindJobsContractError ⇒ 422 with its code; a consent/loopback refusal ⇒ 403; a config digest mismatch ⇒ 409). If it raises AFTER allocation, the POST is already answered and the failure shows in GET /api/runs/{id} (the backend's run_status). A timeout without allocation ⇒ 504 with code run_start_timeout (add 504 to your handling; flag it in your worker file as needing a ROUTES status update in contracts; do NOT edit contracts). Keep the ::1 + 127.0.0.1 loopback acceptance.
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_present_ui.py -q` passes, with new tests: a fake backend that allocates and then sleeps 2s ⇒ POST returns 202 in well under 2s; a raise before allocation ⇒ mapped 4xx; a raise after allocation ⇒ 202, then status shows failed (via the fake); allocation timeout ⇒ 504 (use a short injectable timeout in tests).
Write .orchestrator/workers/c2b-async.md, then worker_done.
