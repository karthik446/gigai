CONTEXT: GigAI 0.1.8.1 hotfix, driven by the operator's UAT of the published 0.1.8 (log: /Users/kar/orca/workspaces/gigai/v0.1.8-uat.md; read U12, U19, U20, U22, U25). Worktree: /Users/kar/orca/workspaces/gigai/gigai-v0.1.8.1. Other workers will build on your contract changes, so keep them additive and backward compatible.
TARGET (C0): extend the frozen find-jobs contracts in src/gigai/scout/find_jobs/contracts.py.
1. FindJobsConfig: add optional `countries: list[str]` (ISO-3166 alpha-2 codes like "US"; empty = no country filter) and `visa_sponsorship_required: bool` (default false). A config file WITHOUT these keys (all existing find-jobs-config:1 files, e.g. the operator's ~/scout-search/find-jobs.json) must still parse, with the defaults. Choose between a schema_version "find-jobs-config:2" that also accepts :1, and optional keys under :1; document the choice. The digest of an old file must stay stable if parsed unchanged (or explain why not).
2. PostingRow: add optional `text: str | None` (the posting description as plain text) and `sponsorship: "offered" | "not_offered" | "unknown" | None` (derived from posting text). Old acquire outputs without these keys must still parse.
3. NotAssessedReason: add `location_mismatch`, `sponsorship_excluded`, `model_output_invalid` (keep all existing values).
4. AssessmentResult: add optional `sponsorship: "offered" | "not_offered" | "unknown"` (the model's read of the posting, for the UI badge). Old outputs must still parse.
5. Update fixtures (tests/behaviors/scout_find_jobs/fixtures/*.json) only where needed to cover the new fields; keep old-shape fixtures parsing (add explicit old-shape round-trip tests).
OWNED: src/gigai/scout/find_jobs/contracts.py; tests/behaviors/scout_find_jobs/test_contracts.py; tests/behaviors/scout_find_jobs/fixtures/*.json; .orchestrator/workers/c0-contracts.md
ACCEPTANCE (the test budget: only these): `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_contracts.py -q` passes, incl. new tests: an old config file without the new keys parses with defaults; the new fields round-trip; old PostingRow/AssessmentResult JSON without the new keys parses; new enum values accepted. Also `uv run --locked python -c "from gigai.scout.find_jobs import contracts"`.
EXCLUSIONS: owned files only; no git add/commit (the coordinator commits after verifying); no network; no full suites. Say READ vs EXECUTED. Write .orchestrator/workers/c0-contracts.md, then worker_done.
