CONTEXT: GigAI 0.1.8.1 hotfix from the operator UAT of published 0.1.8 (log: /Users/kar/orca/workspaces/gigai/v0.1.8-uat.md; read the findings named below). Worktree: /Users/kar/orca/workspaces/gigai/gigai-v0.1.8.1. C0 has already extended src/gigai/scout/find_jobs/contracts.py (FindJobsConfig.countries + visa_sponsorship_required; PostingRow.text + sponsorship; NotAssessedReason location_mismatch/sponsorship_excluded/model_output_invalid; AssessmentResult.sponsorship): import those, never edit contracts.py (ask via orca orchestration ask if you need a change). Real live data to learn from (read-only): the operator run outputs under ~/.gigai-scout/workpads/projects/*/gigs/*/runs/run_*/outputs/acquire.json. Other workers are editing other files in this worktree right now: touch only your owned files.
TARGET (P2 assess): fix U25 (use the posting text + a real prompt), U22 (tolerant parse + per-posting isolation), U21 (record the real failure cause), U12 (sponsorship in the assessment).
1. scout/proposal_execution.py `assess_node`: read the posting text from PostingRow.text (C0) instead of the missing text/content keys (`_read_pinned_postings` ~:216-233); if a selected posting has no text, mark it not_assessed (reason model_output_invalid is wrong for this; use the closest existing reason or ask) and continue.
2. A real prompt (`_assess_prompt` ~:184): role/title, company, location, the posting text (bounded), the resume text, the candidate constraints (visa sponsorship required yes/no from the sealed config), and a precise JSON schema with a short example: matrix rows {requirement: str, resume_evidence: [str], status: met|partial|gap}, suggestions [str], questions [str], sponsorship offered|not_offered|unknown. Ask for 5–12 concrete requirements drawn from the posting, not generic ones.
3. Tolerant normalization BEFORE strict validation (at the model boundary, not in contracts): extract JSON from fenced/prose output; resume_evidence string → [string]; null → []; status synonyms (yes/partially/no, meets/partial/missing, case) → met/partial/gap; suggestions/questions string → [string]; drop unknown keys. Keep the strict contract validation after normalization.
4. Per-posting isolation: if one posting's answer is still invalid after normalization, retry ONCE with the validation error in the prompt; if still invalid, not_assessed with model_output_invalid and continue with the others. The node fails only if EVERY selected posting fails at the model (then raise a specific error).
5. U21: in src/gigai/run.py's registered-node failure path (~:4402-4415 `_write_registered_failure_receipt` / `raise RunError("registered node execution failed") from exc`): put the exception class and a redacted, bounded message (no resume/posting text, no secrets, ≤ 300 chars) into the failure receipt + run-details goal errors, and write the full traceback to runs/<run_id>/logs/<goal_slug>.log. Keep the fail-closed behavior.
OWNED: src/gigai/scout/proposal_execution.py; src/gigai/scout/proposals.py (only the parse entry if normalization belongs there; prefer proposal_execution); src/gigai/run.py (ONLY the registered-node failure path lines); tests/behaviors/scout_find_jobs/test_assess_model_policy.py, tests/behaviors/scout_find_jobs/test_assess_contract.py, tests/behaviors/scout_find_jobs/test_run_seam.py (only for the failure-receipt assertion); .orchestrator/workers/p2-assess.md
ACCEPTANCE: `uv run --locked --extra test pytest tests/behaviors/scout_find_jobs/test_assess_model_policy.py tests/behaviors/scout_find_jobs/test_assess_contract.py tests/behaviors/scout_find_jobs/test_run_seam.py -q` passes, with NEW tests: the posting text reaches the prompt; the U22 real failure (resume_evidence as a string) is normalized and passes; a garbage answer → one retry → not_assessed model_output_invalid while the other postings succeed; all-fail → a specific node error; the failure receipt carries the exception class + a redacted message, and the log file has the traceback; the sponsorship field is parsed.
EXCLUSIONS: owned files only; no git add/commit (the coordinator commits after verifying); no live network in tests (httpx.MockTransport only); the test budget is ONLY the listed test files (never directories, -n, or make test). Say READ vs EXECUTED. Write .orchestrator/workers/p2-assess.md, then worker_done. Questions go through orca orchestration ask.
