# v0.1.1 publication attempt

`v0.1.1` was tagged on `24c2695cdc6a447a6e1ab913acafd35b85f566c7` on
2026-08-05. Its exact-tag CI, artifact build, provenance attestation, fresh
wheel/source-distribution smoke checks, and package-only TestPyPI upload passed.

The TestPyPI clean-install proof then failed before invoking GigAI. `uv` uses a
safe first-index resolver by default; with TestPyPI configured first, it found
TestPyPI's stale `jsonschema==3.1.2b0` and did not consult PyPI for GigAI's
required `jsonschema>=4.23` dependency. Production PyPI publication was skipped.

The public `v0.1.1` tag remains immutable. The `v0.1.2` recovery places PyPI
before TestPyPI in the clean-install command: GigAI's exact unreleased version
is found on TestPyPI, while its public dependencies resolve from PyPI under
uv's safe first-index policy.
