# G18 terminal handoff

G18 is complete and accepted for the implemented provider boundary.

The full pre-G18 chain remains citable:

- S16-EVAL fixed corpus, judge methodology, and normative bar;
- S18-01 through S18-05 and S22-01 accepted decision records;
- the S18/S22 tranche terminal handoff;
- the pre-G18 contract-impact review; and
- the additive 19-to-21 schema amendment.

The accepted runtime set is deterministic, OpenAI API, and OpenRouter API
through G11's model port and factory. The implementation preserves exact
selected-reference digests, redaction and credential-reference attestations,
normalized terminal outcomes, usage evidence, comparison disagreement, and
bounded handoff parentage. Automatic fallback, hidden context, retries,
provider racing, and target effects remain prohibited.

## Downstream sequencing

G22 is now startable for the local proposal interview, subject to its own
S22-01 boundary and acceptance criteria. G19 remains gated on G22 and must
also cite S16-EVAL because its review verdict can authorize target effects.

Unsupported provider families may become separate implementation Goals after
their feasibility evidence is converted into a concrete support contract. They
are not silently included in G18 by virtue of the shared model port.

## Operator boundary

No provider endpoint, live credential, background worker, or target mutation
was used to accept G18. A live proof, if desired, must be a separately named,
explicitly authorized operator action identifying the exact provider, model,
credential reference, selected references, network permission, and resulting
invocation record.
