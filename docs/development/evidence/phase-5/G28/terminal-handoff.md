# G28 Terminal Handoff

- Status: Complete
- Next consumer: G27 runtime implementation
- Subsequent consumer: G24 human UAT, then G25 alpha-readiness review

## What G27 may consume

G27 may build on these committed contracts and evidence:

- `gigai eval` and `tools/run_g28_tier.py` for separate contract,
  integration, installed-E2E, and behavioral-eval execution;
- the 31-resource installed schema set, including
  `role-reference.schema.json`;
- namespaced role resolution, explicit legacy classification, and owner-aware
  review-reference/Goal Graph executor validation;
- setup-selected create model persistence and truthful readiness display; and
- the browser-first no-flag create path proven from an initialized non-Git
  target through a fresh installed replay.

## Boundaries that remain in force

- Deterministic fixture behavior is plumbing evidence, not candidate-judge
  quality evidence.
- Detected Codex/Claude executables remain unsupported until a separate
  accepted adapter contract and live compatibility evidence exist.
- Role selection grants no capability, credential, network, provider, Run,
  proposal, approval, version, or target-effect authority.
- G27 must preserve G26 session/proposal authority and its accepted
  completion-audit gate; discovery output remains subordinate evidence.
- G24 owns real operator inspection, SQLite/journal inspection, reinstall and
  upgrade observation, and human verdicts. G28 does not pre-answer those.

## Start condition for G27

Before G27 runtime work begins, confirm that G26's completion audit and human
closeout requirements have been accepted under the current project gate. Then
activate G27 against the accepted discovery-manifest contract and carry the
G28 behavioral-eval reports forward as plumbing baselines, adding adjudicated
discovery cases rather than treating the deterministic fixture as a quality
judge.

