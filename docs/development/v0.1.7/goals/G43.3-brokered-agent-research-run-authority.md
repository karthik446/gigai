# G43.3 — Brokered Agent Research Run Authority

**Version:** v0.1.7
**Status:** Proposed — contract defined; blocked by S43.3 Reject and incomplete G43.1/G43.2 prerequisites
**Depends on:** completed G43, completed G43.2, and S43.3 Adopt decision
**Unblocks:** G45.2 only after implementation and acceptance evidence

S43.3 concluded **Reject** for the current Codex CLI and Claude Code adapters;
see its [decision record](../../evidence/v0.1.7/S43.3/decision-record.md).
This contract remains intact, but it must not be activated or implemented
against those adapters without a new feasibility gate reaching Adopt.

## Outcome

G43.3 adds a generic, additive authority path for an agent-driven research Run.
It does not change G43's completed review profiles. It introduces a new
`run-plan:2` contract and the new `research-standard@1` profile, which permits
only Plan-bound, broker-mediated actions from closed participant runtimes.

```text
approved Gig + selected graph + sealed research Run Plan v2
  -> direct Run confirmation
  -> closed primary-research participant
  -> authenticated broker action lifecycle
  -> closed independent research-verification participant
  -> deterministic resolve and domain-owned outcome
```

`run-plan:1`, the `focused@1`, `standard@1`, `deep@1`, and `var@1` profiles,
and every historical v1 byte retain their exact existing semantics. In
particular, `standard@1` continues to authorize zero tool calls and its
existing four-phase review vocabulary. A v1 reader never projects itself as a
research Plan.

## Run Plan v2 and research profile

`run-plan-v2.schema.json` has schema ID `urn:gigai:schema:run-plan:2`. It is an
additive artifact with a new writer/reader/validator; it does not loosen v1
validation. Its required v2 fields include the existing sealed identity,
source, effect, budget, and consent bindings plus `research_contract`,
`broker`, and `research_budget`. It permits the profile ID
`research-standard` at profile version `1` only.

`research-standard@1` has exactly two provider participants and one
deterministic resolver:

| Participant/phase | Count | Role | Independence rule |
|---|---:|---|---|
| `research` | 1 | `researcher` | primary group |
| `research_verify` | 1 | `research_verifier` | different group and target from researcher |
| `resolve` | 0 provider | GigAI deterministic resolver | no provider target |

The phase order is exactly `research`, `research_verify`, `resolve`. The
researcher may propose typed broker actions; the research verifier may issue
its own independent typed actions and validates only sealed evidence. Neither
role may self-verify the sole evidence it produced. Disagreement, unavailable
verification, a malformed evidence record, or a missing required independent
action ends at `resolve` as a domain-visible inconclusive outcome; it does not
silently add an adjudicator or reuse a target.

The profile's immutable maximum envelope is 80 provider model calls, 40 broker
actions, 100,000 provider tokens, USD 20.00 aggregate reserved cost, 16 MiB
aggregate broker-response bytes, 30 minutes wall time, one pass, and serial
broker dispatch. A graph/policy may lower any ceiling but cannot raise it. A
sealed v2 Plan must additionally allocate finite non-zero or zero limits to
each participant and action type; the participant allocations sum within the
profile envelope. There are no defaults, overflow pools, or implicit retries.

Every agent response is one provider model call, including a response that
requests an action, receives an action result, terminates, fails, or is
cancelled. A broker action is separately counted; the model call that requests
it and the subsequent model call receiving its result are both counted. The
Plan rejects a budget that cannot reserve every permitted provider and broker
action before Run start.

## Authenticated broker lifecycle

G43.3 defines the generic broker protocol; a later provider/domain contract
defines the allowed action names and canonical private payload.

`broker-action-request.schema.json` has schema ID
`urn:gigai:schema:broker-action-request:1`. A request requires `schema_version`,
`request_id`, `run_id`, `run_plan`, `participant_id`, `role`, `phase`,
`action_type`, `action_ordinal`, `payload`, `payload_digest`,
`idempotency_key`, and `requested_at`. It travels only through the
participant's S43.3-proven ephemeral authenticated channel. `request_id`
matches `broker_request_<uuidv4>`; the idempotency key is canonical Plan digest,
participant ID, phase, action ordinal, action type, and payload digest.

GigAI, not the participant, materializes this private request artifact from the
authenticated channel message before validation. `payload_digest` must match
those captured canonical bytes. A later provider-specific contract may turn an
accepted payload into a more detailed private request envelope; a participant
does not receive a workpad-write capability merely to request an action.

Before dispatch, GigAI verifies the authenticated channel, matching Run/Plan
digest, participant/role/phase, serial ordinal, capability/action type,
captured payload/digest, policy validator, and remaining reservations. It
writes an ordered broker event and reserves the action's count, worst-case
cost, response bytes, and wall time. Only then may it dispatch one action.

`broker-action-event.schema.json` has schema ID
`urn:gigai:schema:broker-action-event:1`. It requires `schema_version`,
`event_id`, `run_id`, `request`, `sequence`, `state`, `reservation`,
`dispatch_attempts`, `response_ref`, `actual_usage`, `reconciliation`,
`terminal_code`, and `created_at`. `state` is exactly `reserved`, `dispatched`,
`completed`, `failed`, `cancelled`, or `rejected`; its semantic transitions are
strictly forward. The event does not expose a credential value or auth header.

The identical idempotency key returns the recorded terminal event and never
dispatches again. A distinct retry is a new action ordinal and consumes a new
reservation. A timeout, cancellation, or transport failure after dispatch
remains one attempted action: it reconciles known actuals, reserves the
unknown portion under the sealed no-report rule, and cannot be hidden as a
free retry. Cancellation prohibits new model turns and broker actions after its
ordered terminal event.

## Accounting, evidence, and authority

The Plan seals the S43.3 adapter-feasibility evidence, broker implementation
identity, allowed capability/policy reference, role/target assignments,
per-role/action budgets, reservation table, no-report rule, and every selected
input. Provider or tool facts are not inferred from discovery output.

Every model turn and broker event is durable private Run evidence with a
monotonic Run sequence. A projection may redact content but must preserve IDs,
ordering, participant/role, action type, reservation, actual/reconciled usage,
and terminal code. An unobservable action, unreported actual that violates the
sealed no-report rule, bypass attempt, cross-participant request, or action
after cancellation fails the Run closed.

Direct operator `gigai run --confirm` is the only action that starts this
lifecycle. A model output, request payload, proposal, environment default,
previous confirmation, or replay cannot create or replay Run consent.

## Acceptance evidence and stop conditions

G43.3 is complete only when it proves:

- v1 Run Plans, readers, bytes, profiles, and `standard@1` behavior remain
  unchanged, while v2 accepts only the new research profile and phase/role
  vocabulary;
- all required participant, target-distinctness, phase-order, model-call,
  broker-action, byte, cost, and wall-time constraints fail closed;
- the broker rejects forged, impersonated, cross-Run, cross-Plan, malformed,
  stale, duplicate, over-budget, post-cancellation, and unsupported-action
  requests before dispatch;
- reservations occur before dispatch; actual/reconciled accounting, retries,
  timeouts, cancellation, and unknown usage follow the sealed lifecycle;
- every model turn and broker event is observable in deterministic order; and
- the completed S43.3 evidence proves the selected adapters have no direct
  research bypass surface.

Stop if a profile can reinterpret v1, an agent can reach an undeclared tool or
network surface, a broker action is unobserved or unreserved, two participants
share an authentication channel, a role verifies only itself, or a model can
manufacture consent or authority.
