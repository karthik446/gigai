name: Compatibility job

on:
  schedule:
    - cron: "17 06 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  compatibility:
    name: Full compatibility matrix
    permissions:
      contents: read
      actions: read
    uses: ./.github/workflows/pull_request.yaml
    with:
      profile: full
